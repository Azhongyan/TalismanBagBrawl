# ItemSystem BattleSandbox Board Manual Test

Status: `WAITING_USER_HANDTEST`

1. 只打开 `Scene_TalismanBag_V04_BattleSandboxPreview`，进入 Play；确认首帧现成 ItemDetailPanel 隐藏。
2. 单击 I001-I030 与 I031：必须复用同一现成详情面板，不出现旧 BuildSandbox 弹窗或第二个运行时弹窗。
3. 逐项确认标题、Meta、物品强度、已连接状态徽章、详情/调试页签文字均可见，不再只有物体而没有字。
4. 确认中文字体保持 ItemSandbox 的宋体（SimSun）观感；若本机无 SimSun，记录 Console 中明确的批准回退字体和原因，不得静默换字。
5. 横跨五个法门抽查 I001-I030：基础属性、固定词条、随机词条、核心效果、法门 Build、器类 Build、道痕、推荐摆放、旧物志中当前状态应显示的文字均可见。
6. 每件普通道品确认法门 Build 有 3 个阶段图标、器类 Build 有 2 个阶段图标；核心效果只显示当前数据行对应的图标且最多 4 个，不得沿用上一件道具图片。
7. 推荐摆放与旧物志图标均可见；滚动到对应段落时图标不得透明、被裁掉、落在视口外或显示错误资源。
8. 有 authored rows 的段落保持 BodyText 容器存在，只隐藏 plain Text renderer；Rows 不与正文重复，返回普通正文时 Body 恢复，无内容时不残留上一件道具。
9. 合法不可用/互斥/隐藏页签内容保持隐藏；不要出现全部页签或全部 inactive 子物体被一起打开。
10. 确认 I001-I030 仍是精确 Orange/道品 dev 实例与 rarityIndex 5 图片；I031 仍是系统身份和专用图片，无串名、串图或伪造摆放状态。
11. 托盘仍为 5 列、13 逻辑行且可滚动；拖拽、Ghost、旋转区、非法释放回退、800x800 可视区手感不变。
12. 普通道具可按批准形状摆放、移动、返回；托盘/拖影/棋盘使用同一图片且没有旧图叠层。
13. I031 Reset 后在道具栏且只有一件；可从道具栏到棋盘、棋盘内移动并返回，道具不可复制。
14. I031 未放置时无供能；放置后显示精确上下左右范围，边缘裁切，移动清旧范围，返回后全部范围/徽章清空。
15. 普通道具进入/离开范围时点亮、接亮、阵脉、Build 计入/进度、开窍/技能状态与重开详情同步更新，并出现一行 `【阵势韧性】` 反馈。
16. 敌人/Boss 反馈、战斗页按钮、关闭详情后的页面和既有手感保持不变。
17. Console 本包 Error/Warning 为 0，尤其无 `I031_LOCATION_UNKNOWN`、字体解析失败或安装异常。
18. 退出 Play：25 个 runtime tray slots 被移除，Content 高度/图片/范围恢复；Scene/Prefab 未被保存或改写。
