# V0.4-C1Stage1FirstClearGuaranteedRewardData01 Assignment

## 1. Assignment Identity

- Package: `V0.4-C1Stage1FirstClearGuaranteedRewardData01`
- Task class: `COMPLEX_GUARDED_ONCE / COMPONENT DATA PACKAGE`
- Primary Guard: `V0.4 Reward-Drop Guard`
- Development owner: one new, independent Reward-Drop data task
- User Review Gate: `YES`
- USER_ACCEPTED_SCOPE: `CAMPAIGN_NORMAL_LV1`; 1-1 first clear grants exactly one `I002@white` with `FirstClearFixed`; the Item is useful only when lit; the accepted analysis target is weak `48s/10 waves` versus lit `36s/7 waves`, with `-20%/+20%` I002 sensitivity at `38s/34s`.
- User hand test: `NOT_REQUIRED` for this isolated data package; the later end-to-end 1-1 -> reward -> rearrange -> 1-2 package owns the user experience hand test.
- Git: `FORBIDDEN`
- Expected terminal marker: `C1_STAGE1_FIRST_CLEAR_GUARANTEED_REWARD_DATA01_PASS`

This file is the one-time boundary freeze. The development task must verify the
Assignment SHA supplied in its dispatch message before editing and before
returning. It must not modify this Assignment.

## 2. Player Outcome and Product Context

The accepted P0 promise is:

```text
1-1 clear
-> guaranteed meaningful first-clear reward
-> rearrange the bag so the new Item is lit
-> 1-2 result changes visibly
-> want to continue building
```

This package creates only the authoritative static Reward data for the first
arrow above. It does not make a battle emit StageClear, generate an Item
instance, grant or claim the reward, put it in Inventory, persist it, show UI,
or run 1-2.

Product context is exactly `CAMPAIGN_NORMAL_LV1`. `DEV_SHOWCASE_LV40` is
diagnostics-only and must resolve no product reward data.

## 3. Upstream Accepted Dependency

The Item prerequisite is terminal accepted:

- Package: `V0.4-C1Lv1StarterItemBaseline01 / PACKAGE_COMPLETE`
- Source Assignment SHA-256:
  `62d567cbc22ffc9662589f86e126d626bb45173f06bd24f8264ba7a71fd884b8`
- Item projection schema: `C1Lv1StarterItemFactProjection.v1`
- Profile revision: `C1LV1_STARTER_ITEM_BASELINE_R1`
- Reward candidate identity: `I002@white`
- I002 projection canonical:
  `9830747edfb3de3318910795820588b2114d28bf108e79fb5b76ef9f6cf664d3`
- Item catalog canonical:
  `d0dd84f6e2f59344d3a49416d883eb5f4f84b408ab85a96a8dcf6751de97e357`

The Reward data must obtain the Item fact through
`C1Lv1StarterItemBaselineCatalog.TryGetProjection(...)`. It may pin and verify
the stable projection/revision/canonical identities above. It must not copy,
store, restate, compare, or expose `82/2` or `28/2` as Reward-owned runtime
fields or constants.

Protected upstream source hashes at Assignment freeze:

| source | SHA-256 |
|---|---|
| `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1Lv1StarterItemBaseline.cs` | `A4C454FFCC0A3B5CE1BF2181A39B79CA73C809FD81FD5D3AA3257C7908A140C7` |
| `Assets/_Game/Scripts/TalismanBag/V04/RewardDrop/Contracts/RewardDropContractPrimitives.cs` | `60385BC84807B88122006948AB402FBA39C5D3EC5721D6A387DA842F3C7E05E3` |
| `Assets/_Game/Scripts/TalismanBag/V04/RewardDrop/Contracts/RewardDropContractValidator.cs` | `ED60A14BABEF252BE4D9E646EA9AD16712B770F119D67E7BDE2182170522A796` |
| `Assets/_Game/Scripts/TalismanBag/V04/RewardDrop/Contracts/RewardResult.cs` | `13A6698BD8D3B15CE21222DF0EDFB66D69E7F3B2BF09F08608AF5CD0E8687CCE` |
| `Assets/_Game/Scripts/TalismanBag/V04/RewardDrop/Contracts/RewardClaimAndDedupeIdentities.cs` | `318573D6FC2378B33C7E0BFE8A2357328EAD2971D2B9BDC5F9C3466906F2494C` |

These files are read-only dependencies, not part of the write whitelist.

## 4. Technical Director Decision

- Authoritative owner: Reward-Drop owns first-clear reward content and
  eligibility metadata; Item owns Item identity, stats and projection facts.
- Carrier: one new pure-C# immutable Reward Config/Catalog under
  `V04/RewardDrop/Data/FirstClear`.
- Reuse: consume the accepted Item Fact Projection and P0A immutable Reward
  contracts. Do not introduce a second Roll or Item generator.
- Scene/Prefab/ScriptableObject impact: none.
- Unity serialization/mobile/APK impact: none in this package; no runtime
  binding or asset inclusion claim.
- Stop condition: if the implementation needs an Item stat, Item mutation,
  StageFlow binding, generated instance, ledger, Save, UI, Scene or ordinary
  pool, stop and return `SCOPE_EXPANSION_BLOCKED`.

## 5. Required Runtime Data

Create one tightly scoped runtime source file in namespace:

`TalismanBag.V04.RewardDrop.Data.FirstClear`

The file may contain the immutable definition, validation/result types and
static catalog/provider required by this one data package.

Required fixed identities:

```text
Definition schema:
  C1Stage1FirstClearGuaranteedRewardDefinition.v1
Catalog schema:
  C1Stage1FirstClearGuaranteedRewardCatalog.v1
Reward policy revision:
  C1_STAGE1_FIRST_CLEAR_GUARANTEED_REWARD_R1
Launch context:
  CAMPAIGN_NORMAL_LV1
Chapter:
  bone_aspect_chapter_1
Stage:
  1-1
Acquisition mode:
  FirstClearOnline
Grant kind:
  FirstClearFixed
Reward subject ID:
  I002@white
Reward subject kind:
  OrdinaryGeneratedItem
Reward subject source catalog:
  C1Lv1StarterItemFactProjection.v1
Reward subject classification version:
  1
Source definition ID:
  c1.first_clear.1-1.i002.white.v1
Source rule version:
  1
Quantity:
  1
Claim scope ID:
  campaign_normal_lv1.bone_aspect_chapter_1.1-1.first_clear_fixed
Dedupe policy ID:
  reward.first_clear.entitlement.v1
```

Requirements:

1. The catalog exposes exactly one real data row for 1-1.
2. It resolves the accepted `I002@white` Item Fact Projection by context,
   base identity, rarity-version identity and profile revision, then validates
   the released projection and catalog canonical signatures.
3. The Reward definition may expose the upstream projection object or its
   stable identity/revision/canonical references. It must not declare or copy
   Item damage, cooldown, shape, lighting rule, affix, seed or instance data.
4. Construct the definition's `RewardSubjectIdentity` with the explicit
   `OrdinaryGeneratedItem` classification. Do not infer classification from
   the string `I002` or a display name.
5. `FirstClearFixed` is the guarantee. Do not add probability, weight,
   roll-count, pool, pity, decay or offline timing fields.
6. The stable claim scope identifies one first-clear entitlement for this
   stage and policy. It is independent of any individual `stageClearId`.
   Future Save/Claim integration must combine the scope with its authoritative
   player/profile identity; this package must not invent that identity or
   write a ledger.
7. A future integration may correlate a trusted `StageClearFact`, but this
   package must not create, subscribe to, publish, bind or consume a live
   StageClear event. It must not infer first-clear truth from
   `clearSequence`, Scene labels or Enemy content.
8. Validation fails closed for any context, chapter, stage, acquisition mode,
   grant kind, subject classification, Item projection identity, revision or
   canonical mismatch.
9. `DEV_SHOWCASE_LV40`, repeat challenge, offline patrol, ordinary random,
   Boss special, system/story/cosmetic subjects and malformed/unknown Item
   projection requests all produce no valid data definition.
10. Canonicalization uses explicit ordered fields, ordinal identity semantics,
    invariant culture, UTF-8 and lowercase SHA-256. No time, GUID, random,
    filesystem, Scene or global-state input.
11. Public runtime data is constructor-only/read-only; collections are
    defensively copied. Runtime code is pure C# and has no `UnityEngine` or
    `UnityEditor` dependency.

## 6. Physical and Semantic Separation

This package is physically limited to `RewardDrop/Data/FirstClear`. It creates
no ordinary pool folder or data, no repeat/offline/Boss data and no runtime
service.

The verifier must prove the following all resolve zero valid definitions:

- `RewardAcquisitionMode.RepeatChallengeOnline`;
- `RewardAcquisitionMode.OfflinePatrol`;
- `RewardGrantKind.OrdinaryRoll`;
- `RewardGrantKind.BossFirstClearGuaranteed`;
- `RewardSubjectKind.SystemRhythmSpecial`;
- `RewardSubjectKind.StoryCritical`;
- `RewardSubjectKind.CosmeticSkin`;
- `RewardSubjectKind.BossExclusive`;
- `DEV_SHOWCASE_LV40` and every diagnostics-only context.

These exclusions are based on explicit contract classifications and context,
not `itemId` name or prefix inference.

## 7. Exact Write Whitelist

Only these new files may be created or modified by the development task:

```text
Assets/_Game/Scripts/TalismanBag/V04/RewardDrop/Data.meta
Assets/_Game/Scripts/TalismanBag/V04/RewardDrop/Data/FirstClear.meta
Assets/_Game/Scripts/TalismanBag/V04/RewardDrop/Data/FirstClear/C1Stage1FirstClearGuaranteedRewardData.cs
Assets/_Game/Scripts/TalismanBag/V04/RewardDrop/Data/FirstClear/C1Stage1FirstClearGuaranteedRewardData.cs.meta
Assets/_Game/Scripts/TalismanBag/Editor/V04/RewardDrop/C1Stage1FirstClearGuaranteedRewardData01Verifier.cs
Assets/_Game/Scripts/TalismanBag/Editor/V04/RewardDrop/C1Stage1FirstClearGuaranteedRewardData01Verifier.cs.meta
Docs/V0.4/Reports/C1Stage1FirstClearGuaranteedRewardData01Report.md
Docs/V0.4/Reports/C1Stage1FirstClearGuaranteedRewardData01Spec.csv
Docs/V0.4/Reports/C1Stage1FirstClearGuaranteedRewardData01CanonicalDeterminism.csv
```

The development task must not modify this Assignment after dispatch.

## 8. Forbidden Scope and Writes

Do not create, modify, bind, enable or invoke:

- any Item source, profile, Workbench, Item generation, Roll/RNG, affix, seed,
  instance or rarity implementation;
- existing RewardDrop contracts or their verifier/reports;
- DropRequest, DropRollResult or a second deterministic Item Roll core;
- ordinary drop pools, weights, percentages, pity or duplicate-pressure rules;
- repeat challenge, offline patrol or Boss-exclusive reward content;
- StageFlow, ChapterFlow, Battle, Enemy, formal route or live StageClear;
- RewardResult production, Reward claim/commit, Inventory, Save, migration or
  capacity/overflow behavior;
- Scene, Prefab, Resources, ScriptableObject asset, UI, WorldMap or APK;
- `CoreLoopLabRewardProfile`, V0.2/V0.3 reward implementations or dev-showcase
  reward fixtures;
- AGENTS, `Docs/LOCKED/**`, ROADMAP, CURRENT, Package Queue or Notion;
- Git commit, tag, branch, push, reset, checkout, staging or cleanup.

## 9. Required Verifier and Evidence

Add one Editor-only verifier in the allowed path. It writes only the three
allowed reports and verifies at least:

### Positive

- catalog schema and exactly one row;
- exact context/chapter/stage/mode/grant/subject/source/quantity/scope/policy;
- upstream projection query succeeds with the frozen schema/revision;
- actual I002 projection and Item catalog canonicals equal the released values;
- Reward subject classification is explicitly `OrdinaryGeneratedItem`;
- repeated reads and reversed fixture enumeration produce byte-identical
  canonical payload/signature for at least 100 repetitions;
- two different synthetic trusted StageClear identities for 1-1 still map to
  the same first-clear claim scope, proving entitlement identity is not a
  per-run event key. This is a fixture only and must not bind StageFlow.

### Negative

- every context/mode/grant/subject exclusion in section 6;
- wrong chapter/stage, missing identity, malformed Item version, wrong profile
  revision, wrong projection canonical and wrong catalog canonical;
- I031 query and a non-I031 fixture carrying the same special classification
  fail for the same explicit-category reason;
- no Item stat literals/fields, no probability/weight/pity fields, no
  generated Item instance, no second Roll, no live StageClear subscription;
- public mutation surface, random/time/GUID APIs and forbidden namespaces;
- exact protected dependency hashes from section 3 remain unchanged;
- scoped write whitelist and Assignment SHA remain unchanged.

### Evidence

- `Report.md`: changed files, verification result, canonical identities,
  protected dependency hashes, limitations and terminal marker.
- `Spec.csv`: compact positive/negative assertion matrix.
- `CanonicalDeterminism.csv`: only the necessary repeat and mismatch evidence.

Accepted TTK/attack-wave numbers may be repeated only in the report as
non-executable design evidence. They must not become Reward runtime fields or
verifier calculations.

## 10. Verification and Process Ownership

- Run a focused non-Unity C# compile/syntax check covering the new runtime data
  and its exact pure-C# dependencies.
- Run the dedicated deterministic verifier without Unity batch, Play mode,
  Scene opening/building, Builder tools or asset serialization.
- If no valid non-Unity compile entry is available, return
  `FOCUSED_COMPILE_ENTRY_BLOCKED`; do not start Unity merely to manufacture a
  PASS.
- Any tool process started by this task must record its PID/command/log, use a
  bounded timeout, and be cleaned before terminal return.
- No user hand test is required for this isolated package.

## 11. Completion Definition

Return `DEV_COMPLETE / QA_PASS / USER_HANDTEST_NOT_APPLICABLE` only when:

1. all whitelist files exist and no other file changed for this package;
2. the Assignment SHA matches before and after;
3. the one real 1-1 fixed reward definition derives from the accepted Item
   Projection and contains no copied Item stats;
4. every negative context/mode/grant/category fixture fails closed;
5. focused compile/syntax check and deterministic verifier pass;
6. protected dependency hashes remain unchanged;
7. no owned process remains;
8. the three reports are self-consistent and contain the terminal marker:
   `C1_STAGE1_FIRST_CLEAR_GUARANTEED_REWARD_DATA01_PASS`.

No automatic follow-on integration is authorized. P2 StageClear integration
remains held, and UI/Inventory/Save/claim are separate Owner packages.
