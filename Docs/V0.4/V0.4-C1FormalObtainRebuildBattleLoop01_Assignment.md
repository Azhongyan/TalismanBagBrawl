# V0.4-C1FormalObtainRebuildBattleLoop01 Assignment

Status: `COMPLEX_GUARDED_ONCE / BOUNDARY_FROZEN / READY_FOR_DEV`

## 1. Outcome

Complete one honest `CAMPAIGN_NORMAL_LV1` vertical loop in the existing formal route:

`WorldMap 1-1 -> released formal 1-1 Battle application -> fixed first-clear I002@white RewardResult -> Item entitlement -> player rearranges I001/I002 around I031 -> released formal 1-2 Battle application -> result -> return WorldMap`.

This package integrates released owners. It must not recreate Item, Reward, Battle, Enemy, navigation, or persistence truth.

## 2. Product Context And Frozen Experience

- Product context: `CAMPAIGN_NORMAL_LV1`.
- User Review Gate: `YES / USER_ACCEPTED_SCOPE` from the accepted P0 loop above.
- Producer/balance gates are already complete. Do not retune any data.
- Frozen evidence:
  - 1-1, I001 lit: victory / 16s / 3 enemy applications / player HP 76.
  - 1-2 weak, I001 lit + I002 unlit: victory / 48s / 10 enemy applications / player HP 20.
  - 1-2 target, I001 lit + I002 lit: victory / 36s / 7 enemy applications / player HP 44.
  - Target arrangement is 25% faster and avoids 3 enemy applications.
  - First-clear reward is exactly the released `I002@white` instance `c1fi_1_1_i002_white_v1`.
- No 1-3..1-10, Boss, story, tutorial, VFX polish, random Drop, durable Save, or APK work.

## 3. Classification And Owners

- Class: `COMPLEX_GUARDED_ONCE / FINAL FORMAL INTEGRATION`.
- Primary Guard: Mainline Flow Guard.
- Development Owner: one new visible local task created for this Assignment.
- Mainline owns phase orchestration and formal Scene composition only.
- Item remains sole owner of entitlement, roster, placement, rotation, lighting, and `C1FormalItemBattleInputSnapshot`.
- Reward contracts/data and Item instance carrier remain sole sources of fixed first-clear reward facts. The integration may assemble the released immutable StageClear/Reward/Claim facts, but may not invent Item identity or introduce a second Reward policy.
- Battle remains sole owner of battle calculation/result. Mainline only translates the accepted Item battle snapshot into the released formal Battle request.
- Existing navigation remains the only WorldMap/Unified route owner.
- Unified Scene owns authored slots and serialized references only.
- User decision point: `NONE`.

## 4. Released Dependencies — Read Only

All are terminal and must be consumed without reimplementation:

1. `V0.4-C1FormalLaunchContextAndStageBackbone01` — user Fresh Play PASS.
2. `V0.4-C1Lv1StarterItemBaseline01` — Item facts complete.
3. `V0.4-C1Lv1EarlyEncounterBalance01` — Enemy facts complete.
4. `V0.4-C1Stage1FirstClearGuaranteedRewardData01` — fixed reward definition complete.
5. `V0.4-C1Lv1FormalBattleApplication01` — 77/77 PASS.
6. `V0.4-C1FixedFirstClearItemInstanceCarrier01` — package complete.
7. `V0.4-C1FormalItemSessionAndArrangementAuthority01 REV02` — 234 focused checks PASS and three prefabs validated.

Required Item seams:

- `C1FormalItemSessionAuthority.Create / Submit / ConsumeEntitlement / Reset / CreateBattleInputSnapshot`.
- `C1FormalItemArrangementPresenter.Bind / Unbind / PublishCurrent / TryHandleCardDrop`.
- `C1FormalItemBoard.prefab`, `C1FormalItemTray.prefab`, `C1FormalItemCard.prefab`.

## 5. Exact Allowed Writes

Existing files allowed for surgical modification:

```text
Assets/_Game/Scripts/TalismanBag/UnifiedBattle/UnifiedBattleFormalSceneHost.cs
Assets/_Game/Scripts/TalismanBag/BattleBridge/Formal/C1Lv1FormalBattleApplicationAdapter.cs
Assets/_Game/Prefabs/TalismanBag/UnifiedBattle/UnifiedBattlePageShell.prefab
Assets/_Game/Scenes/Scene_TalismanBag_V04_UnifiedBattlePageShell.unity
```

New files allowed:

```text
Assets/_Game/Scripts/TalismanBag/UnifiedBattle/C1FormalObtainRebuildBattleLoop.cs
Assets/_Game/Scripts/TalismanBag/UnifiedBattle/C1FormalObtainRebuildBattleLoop.cs.meta
Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1FormalObtainRebuildBattleLoopTests.cs
Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1FormalObtainRebuildBattleLoopTests.cs.meta
Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1FormalObtainRebuildBattleLoopSceneAuthoring.cs
Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1FormalObtainRebuildBattleLoopSceneAuthoring.cs.meta
```

The Assignment is Guard-owned and read-only to development.

## 6. Task-Start Baselines

These exact current bytes are the surgical baseline. Existing dirty state belongs to the user/previous accepted packages and must be preserved:

```text
UnifiedBattleFormalSceneHost.cs|511572CB57772015890768632F1D9B8E71D9BDC103537AED52F0EE444CD4EA4A
C1Lv1FormalBattleApplicationAdapter.cs|D54125939F04DEE97DC4A60DF6257D90F8DF5D38F9112C856342A3F90A295649
UnifiedBattlePageShell.prefab|15280C4AD42266951E3FD7F72F68BEB9D5311E696ABDED32802671352F1CA801
Scene_TalismanBag_V04_UnifiedBattlePageShell.unity|FC5E64037CACB2EA00ACC7BA614D404E3AB26D5FFB3CC091DF0BF15B4458F1D9
C1FormalItemSessionAndArrangementAuthority.cs|935A699E038866F3AD5B870A8982A1100FBBDB666C76C23457638D0206160317
C1FormalItemArrangementPresenter.cs|6EB87259CACA6132E44FF5139B67D19E6DCC6ED7C9988877171792338373EE08
C1FormalItemBoard.prefab|984EEA4E7A33D86B29F16A1C5B59D26C08889D79E5A8F6E9D1C9B969F41DEFDD
C1FormalItemTray.prefab|416E534214B429A21A2EC7EC431EDDB324DF3A4EDAADE87155E2817CF35A5605
C1FormalItemCard.prefab|9D97B2B612F65FD0D1574FE13C690416523D7FE19A740F3854C4FC1AAAD06868
C1FixedFirstClearItemInstanceCarrier.cs|EFE734E86E60B54ECEED7A6518EB84CA9407E61E2898BF681EE2E855A8B5E76D
C1Stage1FirstClearGuaranteedRewardData.cs|3D3501203162FF8F5EF40467B3846E22403EC515B02B2207BB17B0D2FA9B9F4C
EditorBuildSettings.asset|CF244226FFD286638DEC957BBE5A9ABCA1DE52E9D529D71D406B5D03DE805932
```

If an allowed existing path drifts after task intake and before first write, stop with `WRITE_CONFLICT`. Do not reset or overwrite it.

## 7. Required Behavior

### 7.1 Formal session and 1-1

1. Accept only the already-consumed, valid `CAMPAIGN_NORMAL_LV1 / 1-1` launch context.
2. Create exactly one memory-only `C1FormalItemSessionAuthority` from the launch/session envelope.
3. Mount the released Board and Tray prefabs into the existing Unified `BoardArea` and `ItemTrayArea` slots and bind one presenter. Do not create fallback hierarchy at runtime.
4. Initial visible/authoritative arrangement is released I031 + I001 with I001 lit.
5. Create a Battle input snapshot from Item authority and run 1-1 through the released Battle adapter. Do not pass `layoutKind` or `i002Lit` from UI/Mainline.

### 7.2 Fixed first-clear reward and rebuild

1. On the unique accepted 1-1 victory, create one immutable StageClear fact using the accepted launch/result identities.
2. Resolve the released fixed reward definition and Item carrier; construct the valid fixed `RewardResult`, claim identity, and dedupe identity without copying Item facts or adding RNG.
3. Submit the entitlement exactly once to `ConsumeEntitlement`. Duplicate same facts are accepted unchanged; mismatch/stale facts fail closed.
4. Only after entitlement is accepted may I002 appear in the formal Tray and the flow enter `REBUILD_FOR_1_2`.
5. Player arrangement commands go only through the released Item presenter/authority. Mainline never writes placement, rotation, occupied cells, or `isLit`.
6. Existing authored summary/button surface may change its product label by phase: claim/rebuild, start 1-2, final return. Do not add debug panels or redraw the UI.

### 7.3 1-2 and completion

1. Resolve the existing validated 1-2 StageConfig identity in memory. It remains unavailable as a separate WorldMap challenge in this package.
2. Create the 1-2 Battle context from the accepted session lineage without publishing a second WorldMap transit and without mutating the 1-2 config asset.
3. Create a fresh Battle input snapshot from the current Item authority immediately before 1-2.
4. The Battle adapter must derive I001/I002 rows and lit facts only from that snapshot. It must reject missing/extra/wrong Item identities, stale signatures, invalid source placement, or wrong session/context.
5. Run the released 1-2 Battle application and display the accepted result. Weak and target arrangements must preserve the released traces; no fabricated middle result.
6. Final button returns to WorldMap through `TalismanSceneNavigationOwner` and clears only the memory-only formal session/loop state.

### 7.4 Lifecycle and dedupe

- Exactly one active formal loop, Item authority, presenter, reward entitlement, Battle binding, and stage result per session/generation.
- Disable/destroy/return unbind listeners and clear memory-only session state; no stale entitlement or StageClear survives a new launch.
- Same button/callback/frame cannot produce duplicate reward, Item, Battle application, or navigation.
- Any failed contract validation leaves the previous accepted phase/state visible and unchanged with one concise product-safe diagnostic.

## 8. Forbidden Writes And Behavior

- No modification to Item authority/presenter/views/prefabs, ItemSystemSnapshot, Item values, shape, lighting, catalog, Build or I031 truth.
- No modification to Reward contracts/data/carrier, claim policy, Enemy profiles, Battle engine/contracts, StageConfig assets, WorldMap Scene, navigation owner, Save, Inventory, Drop, Progression, RunFlow, BuildSettings, ProjectSettings, BattleSandbox, CoreLoopLab, V02/V03 formal content, or Unified layout geometry outside the exact prefab mounting/reference delta.
- No `ItemDevSessionRosterAvailability`, legacy V02 inventory/reward service, `PlayerPrefs`, static persistent campaign state, Scene-name inference, display-name inference, runtime `Find`, runtime fallback GameObject creation, OnGUI, dev panel, fake card, fake `isLit`, fake Battle result, or second Battle clock.
- Do not add Unified to BuildSettings; it is already handled by the released backbone baseline and `EditorBuildSettings.asset` must remain unchanged.
- No Git/worktree/reset/checkout/cleanup.

## 9. Patch Budget

- Existing modified files: at most 4, exactly from the whitelist.
- New files including metas: at most 6, exactly from the whitelist.
- Scene/Prefab serialization: one owner, one explicit authoring pass.
- If a new source, second Prefab, StageConfig/WorldMap/BuildSettings edit, existing Item/Reward contract edit, or runtime hierarchy fallback is required, stop before writing it and return `ARCHITECTURE_REASSESS_REQUIRED` with the first missing seam.

## 10. Verification

Required before terminal:

1. Scoped current-source Runtime and Editor compile.
2. Deterministic tests for:
   - valid 1-1 start from formal Item snapshot;
   - unique StageClear -> released fixed RewardResult -> idempotent I002 entitlement;
   - weak and target 1-2 Item snapshots translating to exact released Battle traces;
   - stale/mismatch/duplicate callbacks, entitlement, StageClear and Battle input rejection;
   - session reset/return/new launch lifecycle;
   - no Save/Inventory/Drop/Progress writes.
3. One Unity authoring/import pass to mount the released Board/Tray prefabs and serialize exact presenter/host references. Do not run a repeated authoring loop.
4. Static validation: no missing scripts/references, no fallback hierarchy, no dev panel, no runtime Find, Board/Tray each mounted once, Item card prefab remains Item-owned, protected hashes unchanged except exact allowed files.
5. `git diff --check` may be used only as a read-only whitespace check; no other Git operation.
6. Return `USER_MANUAL_HANDTEST_READY`, not product PASS. The user performs one normal Editor continuous handtest:
   `WorldMap 1-1 -> 1-1 result -> claim I002 -> move I002 unlit/lit -> start 1-2 -> observe corresponding released result -> return WorldMap`.

Not required:

- Unity batch, repeated Fresh Play, screenshot/keyboard harness, APK, Save persistence, full chapter regression, VFX/animation polish.

## 11. Process Ownership

- Work directly in `F:\Porject\TalismanBagBrawl`; no worktree.
- Record the exact owned Unity PID/command/log/timeout for the single authoring pass and clean only owned processes/children.
- Do not wait on, close, or manipulate an external/user Unity process. If serialization is occupied, complete disjoint code/tests and return one exact `EXTERNAL_ENVIRONMENT_BLOCKED` event.
- Terminal package must leave no owned Unity/Tuanjie/ShaderCompiler/Bee process or lock.

## 12. Completion And Stop

Complete only when the full path is `USER_MANUAL_HANDTEST_READY`, all scoped tests pass, the one authoring pass succeeds, protected boundaries hold, and no owned process remains.

Stop at:

- `ITEM_TRUTH_DUPLICATION_RISK` if Mainline must calculate entitlement/placement/lighting;
- `REWARD_POLICY_DUPLICATION_RISK` if a valid released RewardResult cannot be assembled without changing Reward truth;
- `BATTLE_INPUT_TRANSLATION_BLOCKED` if the released adapter cannot consume `C1FormalItemBattleInputSnapshot` within the exact whitelist;
- `SCENE_SERIALIZATION_CONFLICT` for verified concurrent writes to Unified Scene/prefab;
- `ARCHITECTURE_REASSESS_REQUIRED` for any required scope outside this Assignment.

Do not dispatch follow-on packages from the development task.
