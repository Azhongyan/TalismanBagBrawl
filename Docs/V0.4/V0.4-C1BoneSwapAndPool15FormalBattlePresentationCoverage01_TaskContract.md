# V0.4-C1BoneSwapAndPool15FormalBattlePresentationCoverage01 Task Contract

## 1. Classification and ownership

- Classification: `CONTAINED_ONE_GUARD / VISUAL_PROFILE_TECHNICAL_PREREQUISITE`
- Primary Guard: Animation/VFX/Shared Presentation Guard
- User-facing Delivery Owner: Mainline Guard for `V0.4-C1Stage1To5CorePlayabilitySlice01`
- Product context: `CAMPAIGN_NORMAL_LV1`
- User Review Gate: `NOT_REQUIRED` — this is an exact technical dependency inside the already accepted 1-1 through 1-5 slice.
- PlayerVisibleDelivery: `NONE`
- MilestoneCompletionEvidence: `NO`
- Fresh Play: `NOT_APPLICABLE_UNTIL_MAINLINE_BOUND`
- No Git, worktree, Notion, new Scene, or hidden development task.

## 2. Plain-language outcome

The existing formal Battle presentation table gains:

1. one exact third enemy row for `换骨残相`, so an authoritative Bone Swap Remnant actor can bind to an authored enemy slot instead of being rejected or disguised as another enemy;
2. exact causal presentation-style lookup for the existing I001 starter plus every released Pool15 ordinary Item, so the existing source-pulse/ribbon/halo carrier can retain the exact Item artwork and style identity.

This package does not mount or launch the final route. Mainline remains responsible for the WorldMap-to-Unified real path and final Fresh Play.

## 3. Unity Technical Director decision

| Question | Decision |
|---|---|
| Reusable body/template | Reuse the existing `FormalBattleEnemySlot` and `FormalBattlePresentationRoot` Prefabs; create no new Prefab. |
| Visible content inside the carrier | Existing enemy slot Image/HP/name/outline receives one new profile row; existing causal source proxy/ribbon receives expanded Item-style rows. |
| Data that stays outside | Enemy identity/HP/target/action/death, Bone Swap logic, Item placement/lit truth, damage, reward, stage flow and save truth. |
| Runtime feeder | `FormalBattlePresentationRoot -> FormalBattleEnemySlotView.Bind(snapshot, profile)` for enemies; authoritative Item/Battle cues -> existing causal source resolver and visual carrier for Items. |
| Animation technique | Existing Texture2D sprite-sequence playback. `d1_3` remains a replaceable presentation source; animation never owns Battle timing or results. |
| Serialization | One bounded write to the existing `FormalBattlePresentationProfile.asset`; no Scene/Prefab/ProjectSettings serialization. |
| Unity lease | Required only for one profile authoring/import pass after atomic CLOSED_CLEAN preflight. Offline source/compile work may proceed first. |
| QA | Scoped Runtime + Editor compile; one source-profile-only Unity authoring/import; serialized profile validation. No visual PASS or Fresh Play claim. |

## 4. Immutable identity and source inventory

### Bone Swap Remnant

- `contentId = bone_aspect_enemy_c1_03_bone_swap_remnant`
- `runtimeProfileId = bone_aspect.runtime.c1.bone_swap_remnant.operator.v1`
- `displayName = 换骨残相`
- Existing replaceable visual source root (read-only):
  `Assets/_Game/Resources/anim/Enemy/d1/d1_3`
- Available state folders: Idle 25 frames, Attack 25, Skill 25, Hit 24, Death 24.
- Formal row uses Idle / Attack-or-Skill presentation / Hit / Death only because the existing formal slot has those four visual states. It must not add a gameplay state owner.

### Causal Item coverage

The exact style roster is the union of the starter `I001` and the released Pool15:

`I001,I002,I003,I004,I007,I010,I011,I013,I014,I015,I022,I024,I025,I026,I028,I030`

- I001 and I002 keep their current serialized colors, segment counts, widths, timing and artwork references exactly.
- New rows use the existing five FaMen presentation families and exact white-rarity Item artwork at `Assets/_Game/Resources/item_daoju/<FaMen>/<ItemId>/<ItemId>_1.png`.
- Styles may share a family grammar, but lookup remains one unique row per exact authoritative `baseItemId`.
- No Item number, effect, reward eligibility, rarity or lit truth is copied into Presentation.

## 5. Allowed writes

Modify only:

1. `Assets/_Game/Scripts/TalismanBag/Presentation/FormalBattle/FormalBattlePresentationProfile.cs`
2. `Assets/_Game/Scripts/TalismanBag/Editor/Presentation/FormalBattle/FormalBattlePresentationPrefabAuthoring.cs`
3. `Assets/_Game/Resources/V04/FormalBattlePresentation/FormalBattlePresentationProfile.asset`
4. this Task Contract

Existing `.meta` GUIDs must remain unchanged. Do not write any animation PNG/meta or Item artwork PNG/meta.

## 6. Start baselines

- `FormalBattlePresentationProfile.cs`: `753A8B362B25BDD74A15B9172E4F8977C8A5C6A90EB03661EDD2CA4CB1C440DA`
- `FormalBattlePresentationPrefabAuthoring.cs`: `BE09F676C46032F33B90C15E3EF54DB3234276DAA193F388F23B65D079D95BAD`
- `FormalBattlePresentationProfile.asset`: `9F832C81BC85D04EB1B962C31651948ADD1BC54DC1E3BFF756E8202AF5A13D60`

Read-only protected anchors:

- `C1ExactBattleSandboxItemCardView.cs`: `7DCE9F491E75EE28B5BD8012CFC48A6C7538CF66326472D3F2703CF8267FB442`
- `C1ExactBattleSandboxItemArrangementPresenter.cs`: `EC6A8E413C81CC76311C625E650FBEAB38229E3AEE11D61DB35A78DC9FA1455B`
- `C1ExactBattleSandboxItemBoardView.cs`: `6AF479DF845644250CE9DAF606D22D137C9B09B805B173A617BF5355D2FE8EB3`
- `C1Pool15FormalItemArtworkAndSourceAnchorBinding.cs`: `6B2F80C1D2F432F499D2609F17F8AD6B1545FBDC4B3290EB6B1646563C317F31`
- `C1ExactBattleSandboxItemCard.prefab`: `C4D449A1C298BF5B2A91072A4BEA2A67AFD5E1B1D713A4D769CB843D059DDD8C`
- Unified Battle Scene: `D432B6E492EDF0CB0EEA8B62E66E27A6EE3F7773C8BBCC8AD886A86A1A0BDD63`

Shell/Mainline may evolve under its own active package; this task must snapshot the current Shell before its authoring operation and prove it did not modify it. Do not treat later legitimate Mainline bytes as a permanent historical baseline.

## 7. Required implementation

1. Expand `FormalBattlePresentationProfile` validation from two to exactly three valid unique enemy rows.
2. Add the exact Bone Swap Remnant row using the immutable identities above and ordered, bounded selections from the existing `d1_3` state folders. Do not fallback to host/hound identities.
3. Preserve the existing host and hound rows byte-semantically (identities, frame choices, timings and calibration) except for array growth.
4. Expand causal-style validation to the exact 16-ID roster above. IDs and artwork references must be unique/non-null.
5. Preserve I001/I002 current style values exactly. New styles are selected by authoritative Item catalog FaMen, not by Chinese display text or Battle result inference.
6. Add a bounded source-profile-only authoring/validation entry point. It may write only the profile asset; it must not rewrite any Battle presentation Prefab, Unified Shell or Scene.
7. Validation must assert:
   - exact three enemy identities;
   - Bone Swap Remnant exact content/runtime IDs and all four nonempty clips;
   - exact 16 causal IDs, unique rows and non-null source artwork;
   - existing I001/I002 values unchanged;
   - existing causal timing unchanged;
   - no runtime hierarchy creation, Resources path guessing in runtime code, Item/Battle/Enemy truth write, or Scene save.

## 8. Released downstream dependency

Item Guard completed `V0.4-C1Pool15ExactCardArtworkAndSourceAnchorBinding01` and released the immutable baselines above. The exact formal Item Card/Board presentation now resolves the same 16 Item identities through Item-owned artwork truth and exposes the current placed+lit authored Board source anchor without changing Card geometry, interaction, placement, rarity or VFX ownership.

Released read-only APIs include `IsExactTargetBaseItemId`, `TryGetWhiteArtworkResourcePath`, `TryResolveArtwork`, `TryCreateCurrentSourceAnchorRequest`, and `C1ExactBattleSandboxItemArrangementPresenter.TryResolveCurrentPlacedLitSourceAnchor(...)`. This VFX package may validate compatibility with those APIs but must not edit the Item carriers.

## 9. Forbidden writes and behavior

- Enemy/Battle runtime, contracts, numbers, target/action/death truth or Bone Swap operator.
- Mainline, StageConfig, WorldMap, Unified Scene/Shell/Host, Reward, Item, Save or formal flow.
- Formal Battle Prefabs, source Card Prefab, Board/Tray, damage float, audio/VFX roots.
- New Scene, new Prefab, runtime fallback hierarchy, `GameObject.Find`, display-name inference, placeholder artwork, generated formal art, import-setting changes, or copied BattleSandbox controller.
- Changing I001/I002 accepted causal appearance or current global causal timings.

## 10. Evidence and terminal

Required evidence:

- scoped Runtime compile PASS;
- scoped Editor compile PASS;
- one bounded source-profile-only Unity authoring/import PASS when lease is clear;
- serialized validation of three enemy rows and 16 causal Item rows;
- protected write-domain review and owned process/lock cleanup.

Strongest allowed terminal:

`TECHNICAL_PREREQUISITE_COMPLETE / BONE_SWAP_AND_POOL15_FORMAL_PRESENTATION_PROFILE_COMPLETE / FAITHFUL_PARTIAL`

This terminal is not player-visible proof. Mainline owns the final normal-path Fresh Play; Item Guard owns the missing Pool15 exact Card artwork/source-anchor binding.

## 11. Stop conditions

- exact Bone Swap content/runtime identity differs from released Enemy/Battle facts;
- `d1_3` files or required frame imports are missing/corrupt;
- any exact Pool15 white-rarity artwork is unavailable to Editor authoring;
- completing the profile requires an Item, Enemy, Battle, Mainline, Scene, Prefab or ProjectSettings write;
- Unity serialization lease is occupied by a user/unknown owner.
