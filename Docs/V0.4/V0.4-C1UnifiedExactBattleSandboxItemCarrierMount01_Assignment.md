# V0.4-C1UnifiedExactBattleSandboxItemCarrierMount01 Assignment

Status: `COMPLEX_GUARDED_ONCE / REV01_DIRECT_MODULE / BOUNDARY_FROZEN / READY_FOR_DEV_RESUME`

## 1. Outcome

Complete and wire the already released exact BattleSandbox-derived Item Board, Item Tray, and reusable Item Card carriers so that the existing Unified bridge page can use them directly, with no manual component or Inspector setup.

This is a bounded partial preview package.

- `ProductContext`: `CAMPAIGN_NORMAL_LV1`
- `PlayerVisibleDelivery`: `PARTIAL`
- `MilestoneCompletionEvidence`: `NO`
- strongest allowed success status: `DIRECT_ITEM_MODULE_USER_HANDTEST_READY`
- forbidden claims: full BattleSandbox page parity, realtime battle readiness, `MILESTONE_INTEGRATED`, milestone `USER_HANDTEST_READY`, or milestone `COMPLETE`

## 2. User Review Gate

- `UserReviewGate`: `YES / ACCEPTED`
- Accepted scope: the user explicitly requested that BattleSandbox-authored Item, Tray, and Board be editor-promoted and then used in the bridge page for inspection.
- No product decision remains. Technical mounting, serialized references, validation, and QA do not require another user choice.
- Producer/Balance gate: `NOT_APPLICABLE`; no number, probability, cadence, Build threshold, reward strength, or balance fact changes.

## 3. Immutable Player Promise Card

- `MilestoneId`: `P0_FORMAL_REALTIME_LOOP_EXACT_BATTLESANDBOX_PRESENTATION`
- `PartialPackageId`: `C1_UNIFIED_EXACT_ITEM_CARRIER_MOUNT`
- `PlayerPromise`: When the user enters the existing Unified bridge page through the valid formal route, the visible and operable Board, Tray, and Item Cards are the exact accepted BattleSandbox-derived Prefabs, not the approximate C1FormalItem replacements; they are already bound to formal Item truth and require no manual setup.
- `PlayerMustDo`: enter the bridge page; inspect Board/Tray/Card; scroll Tray; select an Item; drag Tray -> Board; rotate; move Board -> Board; return Board -> Tray; observe invalid/cancel rollback.
- `PlayerMustSee`: exact Prefab images, dimensions, scale, anchors, local positions, spacing, sibling order, Board 25-cell composition, Tray mask/scroll hierarchy, Card proportions, artwork slots, placement preview, invalid state, lighting state, drag ghost, and rotate feedback.
- `MustChangeLive`: the released formal Item authority remains the single truth; roster, placement, rotation, and lighting changes update the exact carriers through `C1ExactBattleSandboxItemArrangementPresenter`.
- `PersistenceOrResetExpectation`: re-entry/rebind reconstructs from the formal session snapshot without duplicate cards or stale interaction state. Editor authoring is not a Player/APK dependency.
- `RequiredPartialRealPath`: WorldMap 1-1 Challenge -> Unified initial Item arrangement surface -> inspect and operate exact Board/Tray/Card. The user is not required to finish the battle in this package.
- `ExplicitNonCompletionCases`: approximate `C1FormalItemBoard/Tray/Card` still visible; exact Prefabs stretched, rescaled, reflowed, redrawn, or stripped of source images; placeholder BoardArea/ItemTrayArea panels cover the exact carriers; runtime-created fallback UI; copied BuildSandbox controller/fixture; compile or Prefab existence presented as visual proof.

The original full milestone remains unchanged and incomplete: WorldMap -> live visible 1-1 -> I002 -> exact rebuild -> live visible 1-2 -> WorldMap, with full BattleSandbox page parity.

## 4. Milestone Fidelity Coverage

| Capability | Owner | Concrete carrier | Package status | Required proof |
|---|---|---|---|---|
| Exact Board visible and directly usable in Unified | Formal Item authority / exact Item Presenter | current authored Unified Scene instance of `C1ExactBattleSandboxItemBoard.prefab` | OWNED_HERE | serialized mount/binding validation + user interaction |
| Exact Tray visible and scrollable in Unified | Formal Item authority / exact Item Presenter | current authored Unified Scene instance of `C1ExactBattleSandboxItemTray.prefab` | OWNED_HERE | serialized mount/binding validation + user interaction |
| Exact Card visible and interactive | Formal Item authority / exact Item Presenter | `C1ExactBattleSandboxItemCard.prefab` nested by Tray | OWNED_HERE | nested source validation + user interaction |
| Approximate Item visible layer retired from Unified | Mainline composition | old `C1FormalItem*` instances/components absent from final visible hierarchy | OWNED_HERE | Prefab inspection |
| Exact Prefab geometry preserved | Item Prefab presentation | released three exact Prefabs remain byte-identical | OWNED_HERE | before/after hashes + coordinate-space validation |
| Formal Item state binding | `C1FormalItemSessionAuthority` | `C1ExactBattleSandboxItemArrangementPresenter` | OWNED_HERE | compile + existing public seam + user interaction |
| Exact full page composition | Mainline/Shared Presentation | BattleSandbox-derived player/enemy/HUD/feedback/page carriers | DOWNSTREAM_MISSING | not claimed here |
| Realtime battle presentation | Battle/Visual | released realtime session and presentation carriers | DOWNSTREAM_UNPROVEN | not claimed here |
| Full 1-1/I002/rebuild/1-2 path | Mainline | future final integrated path | DOWNSTREAM_REQUIRED | later continuous user handtest |

Milestone verdict at dispatch: `FAITHFUL_PARTIAL`. The full milestone remains `MILESTONE_FIDELITY_BLOCKED` until downstream rows are integrated and user-proven.

## 5. Technical Director Decision

- Existing Scene: `Scene_TalismanBag_V04_UnifiedBattlePageShell`; no new Scene.
- Composition carriers: existing `UnifiedBattlePageShell.prefab` plus the current authored exact Board/Tray nested Prefab instances already present in `Scene_TalismanBag_V04_UnifiedBattlePageShell`.
- Item state owner: unchanged `C1FormalItemSessionAuthority`.
- Presentation carrier: released exact Item Prefabs and `C1ExactBattleSandboxItemArrangementPresenter`.
- Mainline responsibility: retire only the old approximate visible Item mounts/references, preserve the user's current exact Board/Tray Scene layout, and bind the existing formal loop to one exact Presenter automatically.
- Scene owns no Item truth. Prefab owns presentation. Item authority owns roster/placement/rotation/lighting. Mainline only composes and routes.
- The exact three Item Prefab assets are read-only. Their authored child hierarchy, images, masks, cell/card proportions, and source Prefab links must not be changed.
- Current Scene fact accepted as the new authored composition baseline: the user has already placed one exact Board instance and one exact Tray instance under the Unified shell slots. These are not drift to undo. Do not create second copies in the Unified Prefab, delete them, reparent them, or replace them.
- The current Scene-level root overrides are user-authored geometry and are frozen: Board `sizeDelta=800x800`, `anchoredPosition=(135.67,-47)`; Tray `sizeDelta=800x800`, `anchoredPosition=(167.39,95)`. Preserve their current anchors, pivot, scale, sibling placement, and all unrelated Scene overrides exactly.
- `BoardArea` and `ItemTrayArea` may remain as named shell slots, but only as transparent coordinate/mount containers. Existing plain placeholder graphics/labels and approximate child carriers must not remain visible over or behind the exact surfaces.
- Do not call `StretchMount`, set the exact Prefab roots to stretch, normalize their scale, rewrite their RectTransforms, or apply the old source-scene root positions over the user's current Unified layout.
- One Scene serialization pass is authorized only to add/assign the exact Presenter and host references and to remove/disable the approximate visible layer. The Scene remains composition only and gains no Item truth.
- If direct binding cannot be achieved with serialized references to the existing exact Scene instances without changing Item truth or creating a runtime finder/fallback, stop with `ITEM_OWNER_SEAM_MISSING`.
- Player/APK consumes serialized Prefab references only; no Editor bootstrap, source Scene read, runtime hierarchy creation, `GameObject.Find`, or BuildSandbox injection.

## 6. Owners

- Primary Guard: Mainline Flow Guard.
- Development Owner: existing visible Mainline task `019fc0d0-be0b-7280-bebf-5600250eec24`.
- Item state/data owner: `C1FormalItemSessionAuthority`.
- Exact Item presentation owner: released `C1ExactBattleSandboxItem*` Prefabs and Presenter/Views; read-only in this package.
- Composition/binding owner: `UnifiedBattleFormalSceneHost` and `UnifiedBattlePageShell.prefab`.
- User decision point: `NONE`.

## 7. Required Sources

Read before implementation:

1. `AGENTS.md`
2. `Docs/LOCKED/ENGINEERING_PROCESS_V2_LOCK.md`
3. `Docs/CURRENT/PROJECT_ENGINEERING_STATE_V2.md`
4. this Assignment
5. `Docs/V0.4/BattleSandboxExactBoardTrayItemEditorPromotion01_TaskContract.md`
6. `Docs/V0.4/Reports/C1ExactBattleSandboxItemPromotionReport.md`
7. `Docs/V0.4/Reports/C1ExactBattleSandboxItemPresentationParity.csv`
8. released exact Item Presenter/Views and exact Prefabs
9. current Unified host, authoring tool, shell Prefab, and Scene instance

## 8. Exact Allowed Writes

Modify only when required:

1. `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/UnifiedBattleFormalSceneHost.cs`
2. `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1FormalObtainRebuildBattleLoopSceneAuthoring.cs`
3. `Assets/_Game/Prefabs/TalismanBag/UnifiedBattle/UnifiedBattlePageShell.prefab`
4. `Assets/_Game/Scenes/Scene_TalismanBag_V04_UnifiedBattlePageShell.unity`

The Assignment is Guard-owned and must remain byte-identical during development.

Patch budget: four product paths. No new runtime, Editor, Scene, Prefab, report, or test file is authorized.

## 9. Protected / Forbidden Writes

- `Scene_TalismanBag_V04_UnifiedBattlePageShell.unity`: may change only for serialized exact Presenter/host references and approximate-layer retirement. Existing exact Board/Tray Prefab instances, their current RectTransform overrides, and all unrelated user Scene edits are protected.
- `Scene_TalismanBag_V04_BattleSandboxPreview.unity`: byte-identical.
- all three exact Item Prefabs, their metas, Presenter/Views, parity report, and Item promotion report: byte-identical.
- old approximate `C1FormalItemBoard/Tray/Card` Prefab assets: byte-identical. They may be removed only as nested visible instances from the Unified Prefab.
- formal Item authority/contracts/snapshots, Item values/artwork/shape/lighting/Build truth: read-only.
- Battle, Enemy, Reward/Drop, Save, Inventory persistence, Progress, Navigation, WorldMap, realtime Session, formal Presentation Prefabs/Profile/audio/VFX: read-only.
- ProjectSettings, BuildSettings, Packages, AGENTS, LOCKED, Queue, Notion: read-only.
- no new Scene, runtime fallback hierarchy, duplicate Item owner, BuildSandbox controller/runtime fixture, Lv40 profile, debug panel, OnGUI, display-name inference, or Git/worktree operation.
- preserve every unrelated dirty-worktree change; no reset, checkout, cleanup, commit, tag, or push.

## 10. Intake Baselines

- `UnifiedBattleFormalSceneHost.cs`: `C0B6B68E15E0803896AE3D0248611C8F944428BA7DAAF554A345D401CFC5D898`
- `C1FormalObtainRebuildBattleLoopSceneAuthoring.cs`: `CCD9BD6958C81B7A8BA077C741E197D270F135CA85792ABAA5A8196F70A3A2A0`
- `UnifiedBattlePageShell.prefab`: `5E64146954350BD4F16877C9328DE371B0A16A2C3F2FFD7855727E71ABFBFF34`
- Unified Scene REV01 authored-layout baseline: `9CE82A4334F33AD4A6D0F016282EA30715CAFBA658159511AC7BA222B376B722`
- BattleSandbox Scene: `1D7A367B803F9A7477E261A90E05D58FBAB2F9FAA1F3464A86806B8A9FAF9FCA`
- exact Board Prefab: `46949A5FECAAF94960D2F2A31C6715F0C1297CDAEAD1C71FF749959762FBB7A8`
- exact Tray Prefab: `AE444A9BB96E84DB5409A444DADF13787575E377EE93956FA0B31E608F1751E1`
- exact Card Prefab: `B03AB01A3EDD0CBD4A4128DAF35445E1F2FBFFE4269967613188314439E10B46`
- exact Presenter: `61DED546476469A4691B806525FDBE76AA6B393BA7F40F36B2B7DFB160B1D3CB`
- approximate Board Prefab: `984EEA4E7A33D86B29F16A1C5B59D26C08889D79E5A8F6E9D1C9B969F41DEFDD`
- approximate Tray Prefab: `416E534214B429A21A2EC7EC431EDDB324DF3A4EDAADE87155E2817CF35A5605`
- approximate Card Prefab: `9D97B2B612F65FD0D1574FE13C690416523D7FE19A740F3854C4FC1AAAD06868`
- WorldMap Scene: `9F946361BA208E129A349BB9F382F40A18D33DC49D6EA4DC68D0E8605B108B3E`
- EditorBuildSettings: `CF244226FFD286638DEC957BBE5A9ABCA1DE52E9D529D71D406B5D03DE805932`

If an allowed-write baseline differs before this task writes it, stop with exact attribution; do not refresh silently.

## 11. Required Behavior

1. Retire exactly the old approximate `C1FormalItemBoard`, `C1FormalItemTray`, and `C1FormalItemArrangementPresenter` visible mount instances/components. Do not modify or delete their source Prefab assets.
2. Reuse the one exact Board and one exact Tray Prefab instance already authored in the Unified Scene. Do not instantiate duplicates into the Unified Prefab. The Tray must retain its authored nested exact Card carrier references.
3. Preserve the exact source Prefabs byte-for-byte and preserve the current Unified Scene Board/Tray root overrides as frozen in REV01. Preserve all child images, fonts, masks, ScrollRect, cell/card geometry, artwork proportions, and sibling order.
4. Use the existing BoardArea/ItemTrayArea shell identities as transparent mount surfaces only. Remove/disable their plain placeholder Image/Outline/label rendering as part of the Unified Prefab composition, without deleting the slot GameObjects or changing shell slot identity.
5. Mount the exact roots into a full-page-compatible local coordinate space. No root stretch, scale normalization, `sizeDelta` rewrite, LayoutGroup reflow, or artwork replacement.
6. Replace `UnifiedBattleFormalSceneHost` serialized Item binding types and validation with the exact Presenter/BoardView/TrayView. Preserve formal loop, navigation, reward, realtime battle, and presentation behavior unchanged.
7. Bind exactly one `C1ExactBattleSandboxItemArrangementPresenter` to the existing formal Item authority and to the current Scene exact Board/Tray Views through serialized references. Roster, placement, legal placement, rotation, lighting, rarity, selection, and reset truth remain Item-owned.
8. Authoring is idempotent: an already-correct exact mount validates/no-ops; mixed exact+approximate, duplicate exact carriers, unknown extra Item presenter, or broken references fail closed before save.
9. Update the existing authoring tool rather than creating a new runtime installer. It may perform one bounded Prefab+Scene authoring pass: remove the approximate visible layer, preserve the current exact Scene instances and geometry, add/assign one exact Presenter, bind the host, validate, and save once. It must fail closed before save on duplicates, mixed owners, missing source links, or unrelated geometry changes.
10. Do not alter the existing full-page battle presentation carriers. This package neither repairs nor certifies player/enemy/HUD/VFX/audio parity.
11. Runtime re-entry/bind is idempotent; exact cards do not duplicate and stale drag/selection state does not survive unbind/reset.
12. Direct-use acceptance must cover real formal cards/artwork, Tray -> Board, Board -> Board, Board -> Tray, four rotations, matching footprint/preview/committed occupied cells, safe invalid/bounds/collision rollback, and snapshot-driven selection/lighting/rarity/reset/re-entry refresh.
13. Player/APK uses only the serialized Scene/Prefab composition and runtime formal Item seam. No Editor-tool dependency, `GameObject.Find`, runtime-generated final UI, BattleSandbox fixture, or second Item-state owner is allowed.

## 12. Verification

Required:

- verify all allowed-write intake hashes before writing;
- scoped Runtime and Editor compile;
- one owned Unity Prefab+Scene authoring/import pass only, because serialized references to the user's current exact Scene instances are unavoidable;
- composed Scene validation: exact Board=1, exact Tray=1, exact Presenter=1, exact Card nested source present, approximate Board/Tray/Presenter visible layer=0, missing scripts=0;
- exact Item Prefab hashes unchanged before/after;
- Unified Scene changes only in the authorized serialized wiring/retirement surface; BattleSandbox Scene hash remains unchanged;
- current Unified Board/Tray root RectTransform overrides and source Prefab links remain exact before/after;
- placeholder BoardArea/ItemTrayArea graphics do not render;
- no BuildSandbox runtime dependencies, runtime fallback hierarchy, dev panel, Scene save, BuildSettings write, or Git operation;
- scoped `git diff --check` for the four allowed product files and this Assignment;
- intake note: the current `UnifiedBattlePageShell.prefab` baseline already contains Unity-serialized trailing spaces reported by `git diff --check`. Do not rewrite unrelated YAML only to normalize them. Terminal evidence must prove no new trailing-whitespace lines beyond that intake baseline; the Assignment and modified C# must pass scoped `git diff --check`.
- all task-owned Unity/Tuanjie/ShaderCompiler/bee processes and locks cleaned before terminal.

Do not run repeated batch/Fresh-Play/harness loops. If normal Editor is externally occupied, complete disjoint code/static work and return one exact environment blocker only when the single authoring pass cannot proceed.

User evidence required after technical completion:

1. Enter WorldMap and click 1-1 to reach Unified.
2. Confirm the formal roster appears as real cards with the accepted artwork/data and that no approximate C1Formal/plain placeholder layer is visible.
3. Scroll the Tray; drag I001 Tray -> Board; test all four rotations; move Board -> Board; drag Board -> Tray; make invalid bounds/collision/cancel placements and confirm rollback with no disappearance.
4. Exit/re-enter or reset once and confirm roster, selection, lighting/rarity, placement, and cards refresh from the formal snapshot without duplicates or stale drag state.
5. Report only whether this directly usable Item module matches the accepted BattleSandbox Board/Tray/Card and interaction. This remains a partial full-page milestone check.

This handtest does not prove or accept player/enemy/HUD/full-page parity, realtime battle, reward loop, or the full P0 milestone.

## 13. Stop Conditions

Stop without workaround if:

- exact carrier roots must be rescaled, stretched, reflowed, or edited to fit Unified;
- direct binding would require changing an exact Item Prefab, moving/rescaling the current authored Scene instances, or creating runtime discovery/fallback UI;
- mounting requires Item authority/Presenter source changes, copied BuildSandbox runtime, or a second Item owner;
- the old approximate visible layer cannot be removed without changing its source Prefab asset;
- another active task owns the Unified Prefab or either allowed source file;
- a Scene change outside the exact Presenter/host wiring and approximate-layer retirement surface, or any Battle, Enemy, Reward, Save, Progress, BuildSettings, formal Presentation, or full-page redesign is requested.

Return the first exact blocker: `UNIFIED_EXACT_ITEM_COORDINATE_SPACE_MISMATCH`, `ITEM_OWNER_SEAM_MISSING`, `UNIFIED_PREFAB_WRITE_CONFLICT`, or `SCOPE_EXPANSION_REQUIRED`.

## 14. Completion

The package may return `DIRECT_ITEM_MODULE_USER_HANDTEST_READY` only when all required technical checks pass and the concise direct-use handtest is available. Static Prefab parity or compile alone is non-completion.

It may return `PACKAGE_COMPLETE / FAITHFUL_PARTIAL` only after the user accepts that bounded Board/Tray/Card preview. It must never inherit or imply acceptance of the full battle-page milestone.

No Git. No worktree. No continuous monitoring.
