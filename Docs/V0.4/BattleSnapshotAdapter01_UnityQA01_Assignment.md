# V0.4-BattleSnapshotAdapter01-UnityQA01 Assignment

Status: `USER_REPORTED_UNITY_QA_PASS_CANDIDATE / GUARD_ACCEPTED_QA_PASS_CANDIDATE`

Maintainer: Codex Guard / boundary window

Date: 2026-07-07

## 0. Authority

Authoritative boundaries:

```text
AGENTS.md
Docs/LOCKED/*
Docs/ROADMAP/VERSION_ROADMAP.md
Docs/CURRENT/V0.3_PRODUCT_FLOW01.md
Docs/V0.3/V0.3_PACKAGE_QUEUE.md
Docs/ROADMAP/V0.4_BUILD_SYNERGY_ROADMAP.md
Docs/V0.4/BUILD_PACKAGE_QUEUE.md
Docs/V0.4/BUILD_PHASE2_PACKAGE_QUEUE.md
Docs/V0.4/BattleSnapshotAdapter01_Assignment.md
Docs/V0.4/UnifiedBattlePageShell01_Assignment.md
Docs/V0.4/UnifiedBattleBridge04_06_Assignment.md
User latest explicit instruction
```

This package exists because `V0.4-BattleSnapshotAdapter01` is currently only:

```text
SOURCE_STATIC_CANDIDATE
GUARD_STATIC_PASS_BATTLE_SNAPSHOT_ADAPTER01
```

It is not verified, not a stable baseline, not an archive baseline, and must not be tagged.

## 1. Package Goal

`V0.4-BattleSnapshotAdapter01-UnityQA01` verifies the already-created BattleSnapshotAdapter data-layer package inside Unity and through a minimal old-flow regression.

The goal is to answer:

```text
Does the new Contracts/Battle code compile in Unity?
Does the Editor report writer compile and run in Unity?
Do the required reports regenerate from Unity execution?
Did the old V0.2 / V0.3 golden-path surface remain unaffected?
```

This package is QA only. It must not develop new functionality.

## 2. Allowed Scope

Allowed actions:

```text
Open the Unity project.
Let Unity compile scripts.
Inspect Unity Console.
Run the BattleSnapshotAdapter01 QA menu or equivalent Editor method.
Regenerate the existing BattleSnapshotAdapter01 reports.
Perform the minimum V0.2 / V0.3 old-flow smoke / golden-path checks.
Write QA reports under Docs/V0.4/Reports.
Update only this QA package's report files if needed.
```

Allowed report outputs:

```text
Docs/V0.4/Reports/BattleSnapshotAdapterUnityQAReport.md
Docs/V0.4/Reports/BattleSnapshotAdapterUnityMenuRunReport.md
Docs/V0.4/Reports/BattleSnapshotAdapterGoldenPathSmoke.md
Docs/V0.4/Reports/BattleSnapshotAdapterUnityQALeakCheckReport.md
```

The package may also refresh these existing reports from Unity execution:

```text
Docs/V0.4/Reports/BattleSnapshotAdapterReport.md
Docs/V0.4/Reports/BattleSnapshotFieldMap.csv
Docs/V0.4/Reports/BattleSnapshotPlayerDevFieldSplit.csv
Docs/V0.4/Reports/BattleSnapshotAdapterLeakCheckReport.md
```

## 3. Strict Forbidden Scope

Do not modify:

```text
Contracts/Battle implementation, unless Unity compile fails and Guard explicitly opens a fix package.
Scene files.
Prefab files.
UI layout.
BuildSettings.
ProjectSettings.
RunFlow.
V02RunFlowController.
MainTrialFlowService behavior.
AutoCombatController.
PageState.
FormationState.
V02FormationGridFrame.
DamageText.
BossInfoPanel.
SaveData.
PlayerPrefs.
MainTrialProgressData.
RewardService.
RewardConfig.
DropTable.
StageConfig.
EnemyConfig.
BossConfig.
UpgradeConfig.
formal numeric configs.
AGENTS.md.
Docs/LOCKED/*.
```

Do not do:

```text
Do not develop UnifiedBattlePage.
Do not create UnifiedBattlePageShell.
Do not connect formal route.
Do not start Package 2 work.
Do not write save data as part of the adapter.
Do not grant rewards as part of the adapter.
Do not advance chapters as part of the adapter.
Do not auto-start 2-10 Boss.
Do not bypass BossInfoPanel.
Do not commit, tag, push, reset, or rollback.
```

If Unity compile fails, the QA window must stop and report the error. It must not silently fix code unless Guard opens a separate `BattleSnapshotAdapter01-CompileFix01` package.

## 4. Unity Compile Requirements

Must verify:

```text
Unity opens the project.
Unity Console has no red Error after compile.
Contracts/Battle DTOs compile.
V03 exporters compile.
V04 normalizer compiles.
BuildEvaluation exporter compiles.
Sandbox result mapper compiles.
BattleContractFieldValidator compiles.
BattleSnapshotAdapterReportWriter compiles.
```

Report must include:

```text
Unity version if visible.
Console Error count.
Console Warning count.
Whether all new Contracts/Battle scripts compiled.
Whether Editor report writer compiled.
Any compile errors copied or summarized.
```

## 5. Unity Menu / Report Requirements

Must run, if menu is available:

```text
Tools/Talisman Bag/V0.4/BuildSandbox/BattleSnapshotAdapter01/[QA Only] Run Validation Reports
```

Equivalent direct Editor invocation is allowed only if the menu item is not visible but the method is callable.

Must verify after running:

```text
BattleSnapshotAdapterReport.md exists and shows PASS.
BattleSnapshotFieldMap.csv exists.
BattleSnapshotPlayerDevFieldSplit.csv exists.
BattleSnapshotAdapterLeakCheckReport.md exists and leak count is 0.
Reports were regenerated by Unity execution, not only source-static shell output.
```

## 6. Minimum Golden Path Smoke

This is not full `GoldenPathBridgeRegression01`. It is only a minimum old-flow safety check for this candidate package.

Must verify at least:

```text
Old V0.2 / V0.3 battle path can still enter.
Board still displays.
Damage numbers still display.
Battle still progresses.
BossInfoPanel still appears before 2-10 Boss where applicable.
2-10 Boss remains manually triggered.
Reward flow has no obvious duplicate or missing grant.
Save / progress has no obvious abnormal mutation.
```

If the QA window cannot complete a path because of environment or setup limits, it must report:

```text
which step was not run
why it was not run
what risk remains
whether Guard / user manual handtest is required
```

## 7. Leak Check Requirements

The QA report must confirm:

```text
No formal SaveData writes were introduced by BattleSnapshotAdapter01.
No PlayerPrefs writes were introduced by BattleSnapshotAdapter01.
No MainTrialProgressData writes were introduced by BattleSnapshotAdapter01.
No RewardService calls were introduced by BattleSnapshotAdapter01.
No RunFlow / V02RunFlowController writes were introduced by BattleSnapshotAdapter01.
No BossInfoPanel changes were introduced by BattleSnapshotAdapter01.
No Scene / Prefab / UI / BuildSettings changes were introduced by this QA package.
Sandbox result still defaults to devOnly=true.
Sandbox result still defaults to shouldWriteSave=false.
Sandbox result still defaults to shouldGrantReward=false.
```

## 8. Repo / Dirty Worktree Rules

The QA window must not clean, revert, commit, or stage unrelated files.

Known pre-existing unrelated dirty / untracked examples may include:

```text
Assets/_Game/Scripts/TalismanBag/BuildSandbox/BuildGridInteractionPreviewController.cs
Docs/V0.4/Reports/MobileRotateZoneInteractionLeakCheckReport.md
Docs/V0.4/Reports/MobileRotateZoneInteractionReport.md
Docs/V0.4/Reports/MobileRotateZoneInteractionStateReport.csv
Docs/V0.4/BattleSnapshotAdapter01_Assignment.md
Docs/V0.4/UnifiedBattleBridge04_06_Assignment.md
Docs/V0.4/UnifiedBattlePageShell01_Assignment.md
```

RepoOps isolation rule:

```text
If later committing is authorized, RepoOps must commit only the package files for BattleSnapshotAdapter01 and this UnityQA01 package.
Do not include pre-existing MobileRotateZone or unrelated Guard docs unless user / RepoOps explicitly approves.
Do not tag.
```

## 9. Completion Report Required From QA Window

Completion report must include:

```text
Unity Console status.
Unity compile result.
Whether QA menu item was visible.
Whether QA menu item ran.
Regenerated report paths.
BattleSnapshotAdapterReport.md status.
BattleSnapshotAdapterLeakCheckReport.md leak count.
Minimum golden path smoke result.
Whether board displayed.
Whether damage numbers displayed.
Whether BossInfoPanel stopped before 2-10.
Whether 2-10 Boss remained manual.
Whether reward / save showed abnormal behavior.
Whether any code was modified.
Whether any scene / prefab / UI / BuildSettings file was modified.
git status.
Known risks.
```

## 10. Guard Exit Criteria

This QA package may upgrade the previous package only to:

```text
UNITY_QA_PASS_CANDIDATE
```

It still must not call the package:

```text
verified
stable baseline
archive baseline
tag-ready
```

Those require:

```text
User handtest confirmation.
Clean RepoOps commit isolation.
Explicit user approval for any tag / baseline lock.
```

After this QA package passes, Guard may allow `V0.4-UnifiedBattlePageShell01` development to proceed, still without formal route connection.

## 11. Guard Completion Intake

Intake date: `2026-07-07`

Source of completion:

```text
User reported: UnityQA01 is done and manually tested.
```

Guard intake status:

```text
USER_REPORTED_UNITY_QA_PASS_CANDIDATE
GUARD_ACCEPTED_QA_PASS_CANDIDATE
NOT_STABLE_BASELINE
NOT_VERIFIED_TAG_READY
NO_REPOOPS_COMMIT_RECORDED_BY_GUARD
NO_TAG_RECORDED_BY_GUARD
```

Meaning:

```text
BattleSnapshotAdapter01 has passed the user-reported Unity QA / manual test gate.
It may now be treated as ready to unblock UnifiedBattlePageShell01 development.
It is still not a stable baseline.
It is still not an archive baseline.
It is still not tag-ready.
RepoOps clean commit isolation remains a separate explicit step.
```

Guard must still preserve these red lines:

```text
Do not call this package verified stable baseline.
Do not tag.
Do not assume RepoOps committed it.
Do not use it as approval for formal UnifiedBattlePage routing.
Do not skip Package 2 / 3 / 4 / 5 / 6 order.
```
