# V0.4-C1Pool15ExactCardArtworkAndSourceAnchorBinding01 Task Contract

## 1. Outcome

In `CAMPAIGN_NORMAL_LV1`, every exact Pool15 ordinary white Item that is
already owned by the formal Item authority can display its authoritative
artwork through the existing exact Card and Board views. A placed and lit
instance can expose its current authored Board source anchor by exact
`itemInstanceId` for downstream formal battle presentation.

This is an Item-owned technical dependency. It does not mount or prove the
WorldMap -> Unified player route.

## 2. Product Context

- Product context: `CAMPAIGN_NORMAL_LV1`
- Parent milestone: `V0.4-C1Stage1To5CorePlayabilitySlice01`
- Locked Delivery Owner: Mainline Guard
- Primary Guard for this dependency: Item Guard
- PlayerVisibleDelivery: `NONE`
- MilestoneCompletionEvidence: `NO`
- Fresh Play: `NOT_APPLICABLE` in this package

## 3. Task Class

- `CONTAINED_ONE_GUARD`
- New bounded technical prerequisite package.
- No user product decision is required.

## 4. Technical Director Decision

- Authoritative owner: formal Item authority/catalog and its accepted Item
  instance snapshot.
- Reused visible bodies: existing
  `C1ExactBattleSandboxItemCard.prefab`,
  `C1ExactBattleSandboxItemBoard.prefab`, and
  `C1ExactBattleSandboxItemTray.prefab`.
- Runtime feeder: `C1ExactBattleSandboxItemArrangementPresenter` binds the
  authoritative roster/placement snapshot to Card/Board views and exposes a
  narrow source-anchor query.
- Prefabs remain presentation-only. They must not own Item identity, reward,
  damage, lighting legality, placement legality, or battle timing.
- No new Scene, Prefab, ScriptableObject, VFX carrier, runtime hierarchy, or
  duplicate Card system.
- Scene/Prefab/material/import serialization impact: `NONE`.
- Unity lease: `NONE`; `UNITY_NOT_REQUIRED`.

## 5. Player Promise Contribution

Owned coverage rows:

1. Exact target Item identity resolves exact white artwork in the existing
   formal Card and Board presentation.
2. Exact placed+lit `itemInstanceId` resolves the current authored Board
   source anchor after move, rotation, snapshot publication, and re-entry.
3. Tray, missing, stale, duplicate, and unlit instances fail closed as
   non-causal sources.

Still downstream:

- VFX causal style/profile application.
- Mainline runtime mounting and source-anchor consumption.
- WorldMap -> 1-1..1-5 -> reward -> arrange -> live battle Fresh Play.
- User real-path acceptance.

Explicit non-completion:

- compile or deterministic tests alone do not prove visible Card artwork in
  the mounted formal route;
- a resource path, placeholder, VFX-side mapping, copied Card, or first-row
  fallback is not completion;
- source anchors that are not correlated by exact Item instance identity are
  not completion.

## 6. Exact Target Set

The accepted ordinary white target union is exactly:

`I001,I002,I003,I004,I007,I010,I011,I013,I014,I015,I022,I024,I025,I026,I028,I030`

`I031` existing unlit/lit behavior must remain unchanged but is outside the
ordinary Pool15 target set.

## 7. Allowed Writes

Exactly these product paths may be modified or added:

1. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1ExactBattleSandboxItemArrangementPresenter.cs`
2. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1ExactBattleSandboxItemBoardView.cs`
3. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1ExactBattleSandboxItemCardView.cs`
4. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1Pool15FormalItemArtworkAndSourceAnchorBinding.cs` (new when needed)
5. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1Pool15FormalItemArtworkAndSourceAnchorBinding.cs.meta` (new when path 4 exists)
6. `Assets/_Game/Scripts/TalismanBag/Editor/ItemCampaignBaseline/C1ExactBattleSandboxItemPromotionTests.cs`

The developer may use fewer paths. No other product path is allowed.

## 8. Intake Baselines

- Presenter: `99AE7C8F54DF002F9F45814AA251B1140B399BA9474936BE11F24AF63B91D043`
- BoardView: `8B987D53F50733445D09673848283A1D24C4D1F64C18E7A9000407418052054E`
- CardView: `4607F7CB7446DCDA9585CF71FFB29F99E3C3A18A7C07E0309D80142A006C19C2`
- Focused tests: `0C7589D3DFAB2F6516B35D062B797AE9AB0CC14F1E284790CE11EF0D0F4FC176`
- Card Prefab protected: `C4D449A1C298BF5B2A91072A4BEA2A67AFD5E1B1D713A4D769CB843D059DDD8C`
- Board Prefab protected: `46949A5FECAAF94960D2F2A31C6715F0C1297CDAEAD1C71FF749959762FBB7A8`
- Tray Prefab protected: `6FA5F995433B4433B201613CA7FBA939779F6664E44E19CC02ACA060E4F4D552`

If an allowed existing path has drifted before the first write, stop with the
exact path/owner evidence. Do not overwrite concurrent work.

## 9. Forbidden Writes and Ownership

- No Scene, Prefab, meta for existing assets, material, shader, import,
  ProjectSettings, Packages, or BuildSettings write.
- No Reward/Drop, Mainline, Battle/Bridge, Enemy, Save, VFX, or formal battle
  presentation source write.
- No Item numeric facts, effect semantics, rarity rules, shape, placement,
  rotation, drag, scroll, mask, raycast, or interaction authorization change.
- No placeholder art, first-row fallback, base-name inference, directory
  probing, VFX-profile-as-Item-truth, or duplicated state owner.
- No runtime final hierarchy creation, `GameObject.Find`, or BuildSandbox
  runtime dependency.
- No Git, worktree, commit, reset, checkout, or broad cleanup.

The Item-owned artwork binding may use one deterministic exact mapping or an
existing Item catalog/resource identity. It must validate the exact target set
and fail closed; it may not search multiple candidate paths or substitute
another Item's sprite.

## 10. Required Behavior

1. Resolve every exact target base Item to its real white artwork and stable
   Item-owned artwork identity.
2. Card and Board consume the same resolver result. I001/I002 remain
   byte-semantic compatible; I031 retains its existing lit/unlit distinction.
3. Board exposes a narrow read-only exact-instance source-anchor result that
   contains enough immutable correlation for downstream presentation to reject
   stale snapshots.
4. Anchor resolution succeeds only for one current roster instance with one
   accepted placed+lit Board placement and its authored placed-artwork rect.
5. Move, rotation, accepted snapshot publication, and formal re-entry update
   the returned anchor without changing Item identity or placement truth.
6. Tray, unlit, missing, duplicate, stale-session, and stale-signature inputs
   reject without fallback or mutation.
7. Card geometry, artwork aspect, masks, scrolling, drag, rotation, placement,
   interaction authorization, and Item detail behavior remain unchanged.

## 11. Failure and Stop Conditions

- If exact artwork cannot be resolved without a new serialized Prefab/Profile
  reference, stop before Unity and return the exact missing serialized field,
  asset inventory, and minimal proposed authoring scope to Item Guard.
- If FormalBattlePresentationRoot requires a write to consume the API, release
  the Item API only; do not cross into VFX/Mainline.
- If exact instance-to-authored-anchor correlation is unavailable in the
  current Board view, return the first missing Item-owned seam; do not expose a
  guessed screen position.
- One in-scope code/test correction may remain in the same visible task.

## 12. Verification

- Current-source `Assembly-CSharp` compile against the actual Unity response
  boundary.
- Separate `Assembly-CSharp-Editor` compile against the newly built Runtime
  reference.
- Focused deterministic coverage for all 16 exact target IDs plus I031
  regression.
- Verify real artwork resource identity/path exists for every target; no
  placeholder or first-row fallback.
- Verify exact-instance anchor success across move/rotation/re-entry and
  rejection for Tray/unlit/missing/stale/duplicate inputs.
- Run existing exact Card/Board interaction regressions relevant to modified
  paths.
- Static forbidden-dependency and exact-whitelist checks.
- No Unity launch, batch, Play, Fresh Play, or serialization.

## 13. Return Contract

Expected terminal:

`TECHNICAL_PREREQUISITE_COMPLETE / PlayerVisibleDelivery=NONE / MilestoneCompletionEvidence=NO`

Development returns to Item Guard for one close review. On acceptance, Item
Guard releases the exact public artwork/source-anchor API and evidence directly
to Mainline Guard and Animation/VFX Guard. Final Fresh Play belongs Mainline.

## 14. REV02 Same-Line Growing Item Build Correction

This section supersedes conflicting capacity, write-scope, Unity, QA, and return
rules above only for the bounded same-line correction requested after the real
Mainline handtest. The original artwork/source-anchor delivery remains accepted.

### 14.1 Real-Path Failure And Bounded Outcome

- Parent milestone: `V0.4-C1Stage1To5CorePlayabilitySlice01`.
- Delivery Owner remains Mainline Guard; Item Guard is the supplier.
- Real-path status at intake: `USER_HANDTEST_FAIL`.
- Player evidence: only I001 opens detail, older Board artwork disappears as
  cumulative rewards are placed, and the authored Board/Tray presentation pools
  contain only three reusable slots.
- REV02 outcome: every accepted cumulative Item instance in this vertical slice
  remains represented by exact `itemInstanceId`, can be selected, can open the
  existing frozen detail popup with authoritative artwork and combat facts, and
  keeps its identity across Tray/Board movement, rotation, publication, and
  re-entry.
- Bounded authored capacity is exactly `7`: starting I001, five stage rewards,
  and one accepted replay reward. This is presentation capacity, not a reward,
  inventory, placement, or save limit.

Explicit non-completion includes three slots, I001-only detail, base-item
collapse, runtime-created final cards/artwork objects, a new popup, placeholder
facts/art, or compile-only player readiness.

### 14.2 Technical Director Decision

- Product context: `CAMPAIGN_NORMAL_LV1`.
- Authoritative live owner: `C1FormalItemSessionAuthority` and its exact roster,
  Tray, placement, Item-system, and entitlement snapshots.
- Immutable combat source: `C1CampaignPool15BaseCombatFacts`; I001/I002 released
  starter facts and I031 special-source behavior remain preserved.
- Reused presentation bodies: existing exact Card, Board, and Tray Prefabs plus
  the existing ItemDetail popup. No new Scene, popup, Card system, per-Item
  Prefab, ScriptableObject, or runtime final hierarchy is permitted.
- Card Prefab continues to own only artwork/input/footprint/VFX anchors. Board
  owns authored placed-artwork display anchors. Tray owns authored generic Card
  instances, mask, scrolling, and cell references. None owns Item identity,
  numbers, reward truth, placement legality, or battle timing.
- Runtime feeder remains `C1ExactBattleSandboxItemArrangementPresenter` and the
  existing detail projection/selection seam. All binding and selection remain
  exact-instance based.
- Scene impact: none. Card Prefab impact: none. Board and Tray Prefabs require
  one bounded serialized capacity update after the Unity lease is explicitly
  released.
- Mobile/APK risks: no runtime Instantiate/Find/Resources fallback hierarchy;
  unique authored references only; existing raycast, RectMask2D, ScrollRect,
  drag, rotation, and VFX behavior must remain unchanged.

### 14.3 Milestone Fidelity

- PlayerVisibleDelivery: `NONE` for this supplier package.
- MilestoneCompletionEvidence: `NO`.
- Evidence classes allowed here: contract/data, compile/static, deterministic
  runtime logic, and authored Prefab validation.
- Mainline still owns mounting, normal WorldMap entry, visible detail behavior,
  cumulative reward flow, and final Fresh Play/user acceptance.

### 14.4 REV02 Allowed Product Writes

Use fewer when possible. Exactly these paths may be changed:

1. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1FormalItemDetailProjectionAndSelection.cs`
2. `Assets/_Game/Scripts/TalismanBag/Editor/ItemCampaignBaseline/C1FormalItemDetailProjectionAndSelectionTests.cs`
3. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1ExactBattleSandboxItemBoardView.cs`
4. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1ExactBattleSandboxItemTrayView.cs`
5. `Assets/_Game/Scripts/TalismanBag/Editor/ItemCampaignBaseline/C1ExactBattleSandboxItemPromotionEditor.cs`
6. `Assets/_Game/Scripts/TalismanBag/Editor/ItemCampaignBaseline/C1ExactBattleSandboxItemPromotionTests.cs`
7. `Assets/_Game/Prefabs/TalismanBag/Items/C1ExactBattleSandboxItemBoard.prefab`
8. `Assets/_Game/Prefabs/TalismanBag/Items/C1ExactBattleSandboxItemTray.prefab`

Prefab metas are read-only. Card Prefab, Presenter, CardView, artwork/source
anchor API, Item authority, Item catalog, Pool15 facts, Mainline, Reward, Battle,
Enemy, VFX, Scene, Shell, BuildSettings, ProjectSettings, Packages, Save, Git,
and worktrees are forbidden writes.

### 14.5 Frozen Intake SHA-256

- Detail projection: `F5A1254A13E3FF64EF527C7A0727F7C04584594B7B991908737EBB9176237B9D`
- Detail tests: `D74D635A67F0C958D16374381773F0D778E36D9DABDD365993F1FC56CB6A44B0`
- BoardView: `6AF479DF845644250CE9DAF606D22D137C9B09B805B173A617BF5355D2FE8EB3`
- TrayView: `548C19747503F5B458D8BC99FB7E64C06E33C943E8784B1458943D0FAF9CB764`
- Promotion Editor: `8B4EEFBF9A4EBF86B4F6D57108F0DB284D2B823FDDA945FAC8FFCDA0F0A674A9`
- Promotion tests: `2F94C7F34FF44214FCA1B575007E0C3B6F4D9F4512F6647F77E03D346D002AEA`
- Board Prefab: `46949A5FECAAF94960D2F2A31C6715F0C1297CDAEAD1C71FF749959762FBB7A8`
- Tray Prefab: `6FA5F995433B4433B201613CA7FBA939779F6664E44E19CC02ACA060E4F4D552`

Protected exact baselines:

- Card Prefab: `C4D449A1C298BF5B2A91072A4BEA2A67AFD5E1B1D713A4D769CB843D059DDD8C`
- CardView: `7DCE9F491E75EE28B5BD8012CFC48A6C7538CF66326472D3F2703CF8267FB442`
- Presenter: `EC6A8E413C81CC76311C625E650FBEAB38229E3AEE11D61DB35A78DC9FA1455B`
- Artwork/source-anchor API: `6B2F80C1D2F432F499D2609F17F8AD6B1545FBDC4B3290EB6B1646563C317F31`
- Pool15 combat facts: `97994E47B6CCCDA7BA92D2BB53A99FCE11CC8B36523CEA7307BE3013DED3BBF9`
- Formal Item authority: `1E5D19D6CF13A28A3B35A30316F468F57DBAA893B2605535DF48EB6E09C3451B`
- Board Prefab meta: `035AB747FFD25D01BBC4B1C10A44AB4824D31D6CEF0865545599FF5E2D43FC64`
- Tray Prefab meta: `BF0E9EAEFA6D5D3123AE095F0CD55698DA437644707F7F51EEE5CE19CA6C52A4`

### 14.6 Required Behavior

1. Detail projection uses the exact roster `itemInstanceId`, base identity,
   rarity, authoritative artwork identity, and released Pool15 fact/signature.
   All exact Pool15 IDs expose honest family, trigger/cadence, target/condition,
   magnitude/unit/cap, support/maturity, placement, and lighting contribution
   facts. Unsupported/unknown facts remain explicit and never become zero or
   fake direct damage.
2. Existing I001/I002 detail facts and I031 NotApplicable behavior remain exact.
3. BoardView and TrayView validate exactly seven non-null, unique authored pool
   references and fail closed when capacity or references are missing/duplicate.
4. Board binds up to seven current placements by exact `itemInstanceId`; Tray
   binds up to seven active Tray placements by exact `itemInstanceId`. No legal
   distribution of the bounded seven-instance roster loses an older instance.
5. Two instances with the same `baseItemId` remain separately visible,
   selectable, movable, detailed, and source-anchor correlated.
6. Board/Tray transfer, rotation, preview/commit parity, invalid/cancel restore,
   scroll/mask/input authorization, re-entry, and stale-event cleanup remain
   unchanged.
7. Existing exact Card geometry, artwork aspect, footprint, rarity/VFX anchors,
   raycasts, Board geometry/cells, Tray 5x13 grid, and authored visual style are
   preserved.

### 14.7 Two-Phase Execution And Unity Lease

Phase A may begin immediately and is limited to code, Editor tooling, focused
tests, current-source Runtime/Editor compile, and deterministic verification.

Phase B is mandatory because the current serialized Board and Tray pools each
contain only three references. Do not launch, stop, wait on, poll, or control
the current user-owned/unknown Unity process. After Item Guard receives an
explicit `CLOSED_CLEAN` lease release, consume exactly one Item-owned Unity
authoring/validation lifecycle that updates only the two allowed Prefabs. No
Play, Fresh Play, Scene save, or unrelated import/serialization is allowed.

If Phase A completes while Unity remains owned externally, return
`WAITING_USER_UNITY_CLOSE` with exact code/compile state; do not claim package
completion and do not consume the authoring allowance.

### 14.8 QA And Acceptance

- Separate current-source Assembly-CSharp and Assembly-CSharp-Editor compile.
- Deterministic coverage for exact Pool15 detail projection, I001/I002/I031
  regressions, duplicates by base identity, stale/mismatch rejection, and
  instance-preserving Board/Tray/detail/source-anchor behavior for cumulative
  roster sizes 1 through 7.
- Board and Tray Prefab serialized validation: exactly seven unique references,
  no Missing Script, no runtime-created final hierarchy, metas unchanged.
- Preserve Board/Tray cross-container movement, Tray internal movement,
  rotation/hot-zone, mask/scroll, invalid/cancel restore, authorization lock,
  and current source-anchor regressions.
- No Fresh Play in this supplier package. Mainline owns final normal-path proof.

### 14.9 Stop And Return

Stop with the first exact blocker if seven authored references require Board or
Tray geometry redesign, if Pool15 facts cannot be projected without inventing
data, or if the one owned authoring pass fails objectively. Do not broaden into
Scene/Mainline/Reward/Battle/VFX or create runtime fallback UI.

Strongest terminal:

`TECHNICAL_PREREQUISITE_COMPLETE / PlayerVisibleDelivery=NONE / MilestoneCompletionEvidence=NO / FreshPlay=NOT_APPLICABLE`

## 18. REV06 Authored Seven-Layer Card Carrier Correction

### 18.1 Ownership And Player Boundary

- This is a same-package Item supplier correction in the existing visible
  development task. Mainline Guard remains the sole player-facing Delivery
  Owner.
- Product context remains `CAMPAIGN_NORMAL_LV1`.
- Player outcome contributed by this correction: each current ItemInstance
  keeps its persistent authored seven-layer rarity presentation, and an
  accepted placed-and-lit battle cue can trigger that exact instance's current
  Board-local carrier.
- This package cannot claim a visible result. Mainline still owns the normal
  WorldMap-to-Unified Fresh Play and user acceptance.
- Apply the current proportional-engineering rule prospectively: no hashes or
  hash gates, no speculative defense matrix, and only the shortest credible
  compile and lifecycle evidence.

### 18.2 Technical Director Decision

- Reuse the existing `C1ExactBattleSandboxItemCard` Prefab, Board Prefab,
  Tray Prefab, and `ItemRarityContourBloomVfx` Prefab/API. Create no Scene,
  per-Item Prefab, registry, proxy, or second presentation system.
- Item Authority and the Item presentation catalog remain the only owners of
  instance identity, artwork, rarity, placement, lighting, style, family, and
  cue lineage. The VFX carrier owns presentation only.
- `C1ExactBattleSandboxItemCardView` receives one explicit serialized
  `ItemRarityContourBloomVfx` reference. Successful bind calls `Apply` with the
  already-resolved authoritative artwork and rarity. Clear, recycle, disable,
  reset, and unbind call `Clear`.
- Board placed items are currently an Item-owned dynamic image pool, not
  `CardView` instances. Therefore the existing Board visual template also
  receives one explicit serialized nested instance of the same VFX Prefab.
  Runtime pool growth clones that authored template and its carrier; it may not
  search for or construct a VFX hierarchy.
- Each Board pool entry owns its exact carrier beside its existing Rect/Image.
  Bind applies persistent presentation per `itemInstanceId`; recycling clears
  only that entry. Move, rotation, publication, and re-entry retain exact
  instance correlation.
- `C1Pool15FormalItemSourceAnchorBinding` exposes the current Board entry's
  carrier as a read-only presentation reference. `Matches(request)` and all
  existing session/reset/arrangement/ItemSystem/catalog checks remain the
  admission boundary. Tray, unlit, stale, missing, recycled, or mismatched
  sources return no carrier.
- Formal presentation may call only `PlayTrigger(Time.unscaledTime)` after an
  accepted binding match. Item remains responsible for `Apply` and `Clear`;
  Item never owns battle timing, damage, target resolution, or cue acceptance.
- All seven VFX Graphics remain non-raycasting and maskable. Card geometry,
  ScrollRect routing, footprint, rotation, drag, placement, and dynamic pool
  physical bounds remain unchanged.

### 18.3 Phase A Code Scope

Use fewer paths where possible:

1. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1ExactBattleSandboxItemCardView.cs`
2. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1ExactBattleSandboxItemBoardView.cs`
3. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1Pool15FormalItemArtworkAndSourceAnchorBinding.cs`
4. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1ExactBattleSandboxItemArrangementPresenter.cs`
5. `Assets/_Game/Scripts/TalismanBag/Editor/ItemCampaignBaseline/C1ExactBattleSandboxItemPromotionEditor.cs`
6. `Assets/_Game/Scripts/TalismanBag/Editor/ItemCampaignBaseline/C1ExactBattleSandboxItemPromotionTests.cs`

Phase A may prepare, but not execute, one later authoring entrypoint for:

- `Assets/_Game/Prefabs/TalismanBag/Items/C1ExactBattleSandboxItemCard.prefab`
- `Assets/_Game/Prefabs/TalismanBag/Items/C1ExactBattleSandboxItemBoard.prefab`

The Card/Board Prefab metas and all other Prefabs remain read-only. Tray needs
no direct serialization because it reuses nested generic Card instances.

Forbidden writes: Item Detail, Item Authority, ItemSystem, Item numbers,
Reward, Battle, Mainline, Host, Shell, Scene, VFX source/profile/materials,
Enemy, Save, settings, Packages, BuildSettings, Git, and worktrees.

### 18.4 Phase A Acceptance And Stop

- Separate current-source Runtime and Editor compile with zero errors.
- One focused lifecycle proof covers Card bind/apply, recycle/clear, Board
  exact-instance bind, move/rotation/re-entry, per-entry clear isolation, and
  placed-and-lit binding returning the same current Board carrier.
- Preserve one direct rejection proof for Tray/unlit/stale sources. Do not add
  broad speculative matrices.
- Static authoring proof requires exactly one nested carrier on the shared Card
  template and exactly one on each authored Board pool template, with no
  raycast interception or runtime VFX hierarchy creation.
- Do not launch, stop, attach to, inspect, or serialize with Unity during
  Phase A. Return `PHASE_A_COMPLETE / WAITING_EXPLICIT_UNITY_LEASE` or the
  first exact carrier/API blocker.
- Phase B requires a later explicit Mainline `CLOSED_CLEAN` release and one
  Item-owned Card/Board authoring and validation lifecycle. First objective
  failure stops; no automatic retry.

## 17. REV05 Cumulative Reward Placement And Artwork Isolation Correction

This is a same-package code correction in the existing visible Item task. It
supersedes conflicting fixed-count or hash-based QA/reporting rules for this
correction only. Apply the current proportional-engineering rules: no hashes,
no excessive defensive matrices, and no Unity lifecycle.

### 17.1 Player Failure And Outcome

Current formal-route evidence:

- rewarded Items cannot be placed;
- adding an Item can clear older Board artwork;
- the formal placed-and-lit source can become unavailable.

Required Item outcome:

- an accepted reward instance enters the same live Formal Item Authority roster,
  Tray and placement snapshot consumed by the Presenter;
- the existing 5x5 placement rules, never prewarm count, decide placement;
- Board rendering updates and clears by exact `itemInstanceId`, so one failed
  artwork resolution affects only that instance;
- every current placed-and-lit instance retains exact source-anchor resolution.

Item Detail is frozen and out of scope.

### 17.2 Technical Director Boundary

- Product context: `CAMPAIGN_NORMAL_LV1`.
- Authoritative state owner: `C1FormalItemSessionAuthority`.
- Carrier: Item System plus Presenter/View code only.
- Reused presentation: existing Board, Tray and generic Card Prefabs, read-only.
- Runtime feeder: accepted reward entitlement -> the same Item Authority current
  snapshot -> Item Presenter -> Board/Tray views.
- No Scene, Prefab, asset, material, VFX or Editor authoring.
- External Unity is active; do not launch, stop, inspect, wait on or serialize it.
- Evidence: separate Runtime/Editor compile plus one focused credible flow test.

### 17.3 Read-Only Attribution First

Trace one accepted reward through:

`Reward grant -> entitlement acceptance -> authority.Current roster/Tray ->
Presenter.PublishCurrent -> Tray card -> PlaceFromTray -> authority.Current
placement -> Board artwork -> placed-and-lit source anchor`.

Identify the first differing owner/reference/instance key before editing. If the
formal route binds a different Authority instance and no Item API can converge
it, return that exact Mainline owner/API blocker instead of adding a second Item
state owner.

### 17.4 Write Scope

Use fewer files when possible. Product writes are limited to:

1. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1FormalItemSessionAndArrangementAuthority.cs`
2. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1FormalCampaignLootEntitlementAcceptance.cs`
3. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1ExactBattleSandboxItemArrangementPresenter.cs`
4. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1ExactBattleSandboxItemBoardView.cs`
5. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1ExactBattleSandboxItemTrayView.cs`
6. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1Pool15FormalItemArtworkAndSourceAnchorBinding.cs`
7. `Assets/_Game/Scripts/TalismanBag/Editor/ItemCampaignBaseline/C1FormalCampaignLootEntitlementAcceptanceTests.cs`
8. `Assets/_Game/Scripts/TalismanBag/Editor/ItemCampaignBaseline/C1FormalItemSessionAndArrangementAuthorityTests.cs`
9. `Assets/_Game/Scripts/TalismanBag/Editor/ItemCampaignBaseline/C1ExactBattleSandboxItemPromotionTests.cs`

Forbidden: Item Detail files, Battle, VFX, Reward selection, Mainline, Scene,
Prefab, asset, settings, Save, Git and worktree writes.

### 17.5 Acceptance And Return

- One accepted reward appears once in the current authority roster and active
  Tray and can be placed through the existing 5x5 rules.
- A second reward, including a duplicate base identity, remains a separate
  `itemInstanceId` and does not remove or overwrite older Board artwork.
- One unresolved artwork row clears/fails only its own view; all other bound
  artwork and source anchors remain intact.
- Placed-and-lit source resolution remains exact after placement/publication.
- Existing Board-to-Tray, Tray-to-Board, rotation, reset and lock behavior stays
  intact without broad regression expansion.
- Runtime and Editor current-source compilation must have zero errors.

Return `DEVELOPMENT_RESULT` to Item Guard for one close review. Strongest result
is `TECHNICAL_PREREQUISITE_COMPLETE / PlayerVisibleDelivery=NONE /
FreshPlay=NOT_APPLICABLE`. Mainline still owns mounted Fresh Play and user
readiness.

Development returns to Item Guard for one close review and direct release to
Mainline plus read-only baseline notification to Battle/VFX consumers.

## 19. REV07 Rewarded Item Drag And Existing Board Placement Correction

### 19.1 Player Failure And Ownership

- Mainline Guard remains the sole player-facing Delivery Owner. Item Guard is
  the supplier and reuses the existing visible Item development task.
- Product context remains `CAMPAIGN_NORMAL_LV1`.
- Current user evidence: a rewarded Item is visible in Tray, but its Card does
  not start or complete a drag into the existing 5x5 Board, so progression
  cannot continue.
- Required contribution: the exact rewarded `itemInstanceId` travels from the
  currently bound Tray Card through the existing Item Presenter into the
  existing Authority `PlaceFromTray` path. Existing 5x5 placement legality,
  never prewarm count, view count, pool index, or `baseItemId`, decides success.
- Item Detail remains frozen. This code-only supplier correction cannot claim
  player-visible completion; Mainline still owns mounted Fresh Play and user
  acceptance.

### 19.2 Technical Director Boundary

- Authoritative state owner: `C1FormalItemSessionAuthority`.
- Reused visible bodies: the existing generic Card, Tray ScrollRect content,
  and Board presentation pool. No new Scene, Prefab, pool, or placement system.
- Runtime input feeder: the Item Presenter binds one current Card to one exact
  instance, receives Card drag callbacks, resolves the Board pointer/cell, and
  submits the existing Authority placement candidate.
- Card/Tray/Board Prefabs remain presentation-only and read-only. Reward,
  Battle, Mainline, VFX and Item Detail remain outside this write domain.
- External user-owned Unity is active. This correction must not launch, stop,
  inspect, wait on, attach to, or serialize with Unity.

### 19.3 Read-Only Attribution First

Trace one real accepted reward through:

`Authority active Tray row -> Tray dynamic Card Bind -> Card pointer/drag
callbacks -> Presenter active interaction -> Board coordinate/raw-cell
resolution -> PlaceFromTray candidate -> Authority result -> PublishCurrent ->
Board exact-instance artwork and placed-and-lit source carrier`.

Identify the first dropped instance/listener/source-container/authorization or
pointer-state transition before editing. If the real formal route bypasses the
existing Presenter or supplies no exact Board pointer/cell API, return one
precise Mainline/API blocker instead of adding a second path.

### 19.4 Allowed Code Scope

Use fewer paths where possible:

1. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1ExactBattleSandboxItemCardView.cs`
2. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1ExactBattleSandboxItemTrayView.cs`
3. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1ExactBattleSandboxItemBoardView.cs`
4. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1ExactBattleSandboxItemArrangementPresenter.cs`
5. `Assets/_Game/Scripts/TalismanBag/Editor/ItemCampaignBaseline/C1ExactBattleSandboxItemPromotionTests.cs`
6. `Assets/_Game/Scripts/TalismanBag/Editor/ItemCampaignBaseline/C1FormalCampaignLootEntitlementAcceptanceTests.cs`

Forbidden: Item Authority semantics, Item Detail, Reward selection, Battle,
Mainline, Scene, Prefab, VFX, Save, settings, Packages, BuildSettings, Git and
worktrees. Preserve the already accepted seven-layer carrier fields and
lifecycle; no Unity serialization belongs to REV07.

### 19.5 Required Behavior

1. A current dynamic Tray Card has exactly one current drag route and submits
   its bound `itemInstanceId`. Rebind/recycle/reset removes stale gesture state
   and callbacks before another instance is bound.
2. A legal rewarded instance reaches the existing `PlaceFromTray` command with
   the same instance, raw grabbed cell, rotation, footprint and Board target.
3. On accepted placement, the same Authority snapshot removes the instance
   from active Tray, publishes its Board placement/lit state, preserves all
   other artwork, and exposes its current Board-local presentation carrier.
4. On a real illegal target, Authority rejects with the existing placement
   reason and the same instance remains in Tray. No unrelated instance or view
   mutates.
5. Existing Tray/Board movement, rotation, invalid/cancel restore, ScrollRect
   arbitration, raycasts, masks, duplicate-base instances and authorization
   locking remain intact.

### 19.6 Proportional QA And Return

- Separate current-source Runtime and Editor compile with zero errors.
- One credible accepted-reward flow: active Tray Card drag -> legal 5x5 cell ->
  Authority placement/lit -> Presenter publication -> old/new artwork retained
  -> current Board-local source carrier.
- One illegal-cell flow keeps the same exact instance in Tray and reports the
  existing rejection reason.
- Do not add a broad matrix, hashes, Unity batch, Prefab validation or Fresh
  Play to this code-only correction.

Development returns `DEVELOPMENT_RESULT` or one exact owner/API blocker to Item
Guard. Item Guard performs one close review and releases the result directly to
Mainline. Strongest terminal remains `TECHNICAL_PREREQUISITE_COMPLETE /
PlayerVisibleDelivery=NONE / FreshPlay=NOT_APPLICABLE`.

## 15. REV03 One-Time Source-Anchor Harness Correction

This section supersedes REV02 only for the failed source-anchor assertion,
its one allowed Editor-test write, and one final replacement authoring pass.
All REV02 product behavior, ownership, capacity, Prefab, protection, and
milestone boundaries remain unchanged.

### 15.1 Objective Failure And Attribution

- The REV02 replacement lifecycle entered the frozen authoring method and then
  failed at `POOL15_MOVE_ROTATION_SOURCE_ANCHOR_REFRESH_FAILED`.
- The authoring transaction restored the original Board and Tray Prefab bytes;
  the success marker was absent and no product file remained changed.
- The runtime source-anchor API intentionally accepts only a current exact
  placed-and-lit Item instance.
- The focused test's `FindValidCandidate` helper checks placement legality only.
  It does not require the moved candidate to remain in the authoritative
  `LitRangeCells`, but the compound assertion immediately requires a non-null
  placed-and-lit source binding. A legal unlit move therefore makes the test
  expectation self-contradictory without proving a runtime source-anchor bug.
- This is the one automatic QA-harness correction allowed by the routing rules.
  No runtime behavior, public API, Item truth, or Prefab authoring algorithm may
  change in REV03.

### 15.2 Exact Allowed Write

REV03 may change exactly one product path:

`Assets/_Game/Scripts/TalismanBag/Editor/ItemCampaignBaseline/C1ExactBattleSandboxItemPromotionTests.cs`

Frozen intake SHA-256:

`F520B0EAFAF5DC7285A644BB3E98F8B87F6D8EBA51F25F69FFECE93A6B433C77`

All six REV02 Phase A terminal hashes, both original Board/Tray Prefab hashes,
their metas, Card Prefab, CardView, Presenter, artwork/source-anchor API,
Pool15 facts, Formal Item authority, and current Battle sources are protected
read-only baselines during the code correction.

### 15.3 Required Harness Behavior

1. For the I001 move-and-rotation source-anchor case, select a different legal
   Board candidate that is also proven lit by the authoritative current
   Item-system lighting facts. Do not hardcode an Item ID exception or invent a
   second lighting rule.
2. After commit, separately assert the current placement, formal 270-degree
   rotation, exact Item-system placement, and `isLit=true` before resolving the
   source anchor.
3. Split request creation, resolution, binding presence, anchor cell, rotation,
   authored source-anchor reference, and request-match checks into distinct
   diagnostics so a later failure identifies the first concrete layer.
4. Preserve the stale pre-move request rejection and add or retain explicit
   proof that a current legal-but-unlit placement does not become a causal
   source anchor.
5. Preserve every existing Board/Tray/detail/artwork/duplicate-instance,
   movement, rotation, invalid/cancel, interaction authorization, and capacity
   assertion. Do not weaken the Player Promise or convert an unlit Item into a
   battle source.

### 15.4 QA And Final Authoring Authorization

Before Unity:

- exact one-file diff and frozen-hash reconciliation;
- separate current-source Assembly-CSharp and Assembly-CSharp-Editor compile,
  both zero errors;
- focused deterministic/static proof for lit-candidate selection, unlit
  rejection, split diagnostics, and all retained REV02 coverage.

After those gates pass and a fresh atomic `CLOSED_CLEAN` process/lock/hash
preflight passes, Item Guard authorizes exactly one final owned Unity lifecycle
using the existing frozen execute method:

`TalismanBag.Editor.ItemCampaignBaseline.C1ExactBattleSandboxItemPromotionEditor.ExecuteRev02GrowingItemBuildFromCommandLine`

Only the existing Board and Tray Prefabs may be serialized by that lifecycle.
The required success marker remains:

`C1_POOL15_GROWING_ITEM_BUILD_REV02_AUTHORING_VALIDATION_PASS`

Success requires exact authored capacity seven, unique valid references,
instance-based duplicate safety, all contracted focused suites, protected
hashes, and clean owned-process/lock exit. On the first objective failure,
restore/retain the protected Prefab inputs, stop, and return one exact blocker.
No second harness correction or further authoring retry is authorized.

### 15.5 Completion Boundary

Maximum terminal remains:

`TECHNICAL_PREREQUISITE_COMPLETE / PlayerVisibleDelivery=NONE / MilestoneCompletionEvidence=NO / FreshPlay=NOT_APPLICABLE`

Mainline remains the unique Delivery Owner and owns the normal WorldMap Fresh
Play and user-visible acceptance.

## 16. REV04 Dynamic ItemInstance Presentation And Data-Driven Style Correction

This section supersedes every conflicting fixed-capacity, exact-seven,
per-Item artwork-table, Unity-menu, QA, whitelist, and return rule in sections
14 and 15. Safe accepted Phase A work remains valid. The former REV02/REV03
exact-seven menu, execute method, success marker, and authoring order are
permanently retired and must not be invoked, requested, or treated as a
release gate.

### 16.1 Authorization, Ownership, And Classification

- User authorization: accepted architecture and implementation retask.
- Classification: `COMPLEX_GUARDED_ONCE / SAME_PACKAGE_ARCHITECTURE_CORRECTION`.
- Same visible development task:
  `019fd569-f5f3-7bd2-aadf-80a63765f446`.
- Delivery Owner remains Mainline Guard. Item Guard is the supplier and owns
  this package's Task Contract, development collection, same-package
  correction, proportional QA, and immutable dependency release.
- Product context: `CAMPAIGN_NORMAL_LV1`.
- PlayerVisibleDelivery: `NONE` for this supplier package.
- MilestoneCompletionEvidence: `NO`.
- Fresh Play: `NOT_APPLICABLE` here; Mainline owns the final WorldMap normal
  path and user acceptance.
- No new task, package, Scene, Git branch, worktree, hidden task, or second
  Item owner is permitted.

### 16.2 Player Promise Card And Fidelity Boundary

- Player promise: every legitimate current-run ItemInstance remains visible
  and selectable while moving between Board and Tray; duplicate base IDs stay
  distinct; new Item definitions do not require slot-count code, per-Item
  Prefabs, Host capacity edits, or VFX Item-ID edits.
- Player must see: all currently legal placed Items on the 5 x 5 Board and all
  active Tray Items in the existing scrolling 5 x 13 Tray, with exact artwork,
  detail identity, source-anchor correlation, selection, drag, rotation, and
  restoration behavior.
- Live truth owner: `C1FormalItemSessionAuthority`, its ItemSystem snapshot,
  exact roster, Board placements, Tray placements, lighting, session token,
  reset generation, and canonical signatures.
- Explicit non-completion: seven as a semantic cap; the eighth Item clearing
  the whole view; fixed per-Item artwork/VFX tables; baseItemId collapse;
  runtime fallback hierarchy; one Prefab per Item; compile-only player
  readiness; or a Fresh Play claim from this package.
- Mainline still owns formal Host/Shell assembly and final normal-path proof.
  VFX still owns visual style assets and playback. Neither may copy Item
  identity, artwork eligibility, effect-family truth, or placement state.

Milestone fidelity verdict at dispatch: `FAITHFUL_PARTIAL`. The package owns
only the Item presentation/data capability rows and cannot close the parent
player-visible milestone.

### 16.3 Technical Director Decision

- Existing reusable Prefabs: exact Card, Board, and Tray. No new Scene and no
  per-Item Prefab.
- Card Prefab remains one reusable local body carrying artwork/input/footprint/
  selection/rarity-VFX anchors. It never owns inventory, reward, placement,
  damage, battle timing, or effect execution.
- Board Prefab carries the 25 cells, authored layers, input surfaces, and at
  least one reusable inactive placed-artwork template. Board gameplay truth
  remains the authoritative 5 x 5 ItemSystem placement snapshot.
- Tray Prefab carries its frame, `ScrollRect`, viewport, mask, 65 authored
  cells, `Content`, `ItemCardLayer`, and generic Card template/prewarm entries.
  Tray gameplay truth remains the authoritative active Tray placements.
- Runtime feeder remains `C1ExactBattleSandboxItemArrangementPresenter`.
  BoardView and TrayView own bounded reusable presentation pools keyed by exact
  `itemInstanceId`; they do not become state owners.
- `InitialPrewarmCount = 7` is private performance tuning only. It must not be
  used to admit/reject roster, Board, Tray, source-anchor, Host, VFX, or Item
  definitions.
- Board pool demand is the current legal placed-instance count and is bounded
  by the physical 25 cells. Tray pool demand is the current active Tray row
  count and is bounded by the physical 65 cells. Full roster count may exceed
  either visible container count and must not clear valid bindings.
- Pool growth occurs only on snapshot demand changes, never per frame. Pool
  entries are reused across publication/move/rotation/re-entry where their
  exact instance remains present. Recycling clears identity, selection,
  artwork, raycast/CanvasGroup state, callbacks/listeners, pointer/drag/rotate
  residue, source-anchor correlation, and local VFX state before reuse.
- This is bounded reuse of known authored templates, not runtime fallback UI.
  `GameObject.Find`, hierarchy search by Item ID, per-frame instantiate/destroy,
  `Resources.LoadAll`, and inferred gameplay state are forbidden.

### 16.4 Item-Owned Presentation Data Seam

Create the smallest immutable Item presentation catalog/config keyed by exact
`baseItemId@rarityKey`.

Each accepted row carries:

- direct authoritative Sprite reference and stable `artworkIdentity`;
- authoritative `effectFamilyKey`, correlated to the released Item combat
  profile/facts where applicable;
- reusable `presentationStyleKey`;
- authoritative `cueIdentity`;
- row schema/version, source profile signature, and row/catalog canonical
  signatures;
- optional explicit lighting-artwork variants required by special I031
  behavior without making I031 an ordinary causal Item.

The catalog exposes exact unique lookup and an immutable runtime snapshot.
Unknown, duplicate, stale, missing-artwork, rarity-mismatched, family-mismatched,
or signature-mismatched rows fail closed without partial binding.

`C1Pool15FormalItemArtworkAndSourceAnchorBinding` must stop using fixed target
ID and resource-path tables. Exact source-anchor requests remain instance,
session, reset, arrangement, ItemSystem, and instance-signature based. Accepted
bindings additionally carry the catalog artwork identity, effect-family key,
presentation-style key, cue identity, catalog signature, and row signature.
Placed-and-lit admission, move/rotation/re-entry refresh, Tray/unlit rejection,
and stale request rejection remain exact.

VFX consumes the returned `presentationStyleKey` after an accepted formal
Battle cue. VFX must not infer style from Item ID, display name, FaMen, lit
state, or artwork path. Adding an Item that uses an existing grammar requires
only Item definition/profile data, one Item presentation catalog row, and its
artwork asset. A genuinely new visual grammar may add one VFX-owned style data
row downstream, never an Item-ID code branch.

This package does not migrate the full static `ItemInnerDataCatalog` into asset
authoring and does not change reward eligibility, Item numbers, Battle effects,
Save, or persistence.

### 16.5 REV04 Allowed Product Writes

Use fewer paths where possible. Phase A may write only paths 1-12. Phase B may
write paths 13-16 only after an explicit clean Unity serialization lease.

1. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1ExactBattleSandboxItemBoardView.cs`
2. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1ExactBattleSandboxItemTrayView.cs`
3. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1ExactBattleSandboxItemCardView.cs`
4. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1ExactBattleSandboxItemArrangementPresenter.cs`
5. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1Pool15FormalItemArtworkAndSourceAnchorBinding.cs`
6. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1FormalItemPresentationCatalog.cs` (new)
7. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1FormalItemPresentationCatalog.cs.meta` (new)
8. `Assets/_Game/Scripts/TalismanBag/Editor/ItemCampaignBaseline/C1ExactBattleSandboxItemPromotionEditor.cs`
9. `Assets/_Game/Scripts/TalismanBag/Editor/ItemCampaignBaseline/C1ExactBattleSandboxItemPromotionTests.cs`
10. `Assets/_Game/Scripts/TalismanBag/Editor/ItemCampaignBaseline/C1FormalItemPresentationCatalogTests.cs` (new)
11. `Assets/_Game/Scripts/TalismanBag/Editor/ItemCampaignBaseline/C1FormalItemPresentationCatalogTests.cs.meta` (new)
12. `Assets/_Game/Scripts/TalismanBag/Editor/ItemCampaignBaseline/C1FormalItemDetailProjectionAndSelectionTests.cs`
13. `Assets/_Game/Prefabs/TalismanBag/Items/C1ExactBattleSandboxItemBoard.prefab`
14. `Assets/_Game/Prefabs/TalismanBag/Items/C1ExactBattleSandboxItemTray.prefab`
15. `Assets/_Game/Resources/V04/ItemPresentation/C1FormalItemPresentationCatalog.asset` (new)
16. `Assets/_Game/Resources/V04/ItemPresentation/C1FormalItemPresentationCatalog.asset.meta` (new)

Board/Tray Prefab metas are read-only. Card Prefab is read-only. A new Board
artwork Prefab is not authorized: use the existing inactive authored child as
the template unless objective evidence proves it cannot preserve exact Board
geometry and source-anchor behavior; in that case stop with the first missing
carrier instead of broadening.

Forbidden writes include Item authority, ItemSystem, Item definitions/catalog,
Pool15 eligibility/numeric facts, Battle, VFX, Mainline, Host, Shell, Scene,
Reward, Enemy, Save, settings, Packages, BuildSettings, reports, Git, and
worktrees.

### 16.6 Frozen REV04 Intake And Protected Baselines

Allowed existing-file intake SHA-256:

- BoardView: `E94F5A8EBC0FBBB0A85A07A6F517A122BD714A9AF0D1120D3CAC1CB3B36B4BEC`
- TrayView: `2DEEDC012CEE578FA84187543EEF53A734787E938BC2C6078D4BE6FB0342799F`
- CardView: `7DCE9F491E75EE28B5BD8012CFC48A6C7538CF66326472D3F2703CF8267FB442`
- Presenter: `EC6A8E413C81CC76311C625E650FBEAB38229E3AEE11D61DB35A78DC9FA1455B`
- artwork/source-anchor binding: `6B2F80C1D2F432F499D2609F17F8AD6B1545FBDC4B3290EB6B1646563C317F31`
- promotion Editor: `D2D3750E72390A5A2FE069677EA6F3DDB5E6AE3F86E66F45E20CC6C660D6BBA5`
- promotion tests: `F9CCAF85DE776B4DDD11AA63CA90BE0743B534897C9EAE148107A2F6CDF94EA5`
- detail projection tests: `8249C39AED3AA4A59467323615A69BC559BAE69DE20944B31A811BCB9FD51F0B`
- Board Prefab: `46949A5FECAAF94960D2F2A31C6715F0C1297CDAEAD1C71FF749959762FBB7A8`
- Tray Prefab: `6FA5F995433B4433B201613CA7FBA939779F6664E44E19CC02ACA060E4F4D552`

New paths 6, 7, 10, 11, 15, and 16 must be absent at intake.

Protected exact SHA-256:

- detail projection: `91830ED7F5D72EAB54F26EFE4B714E8AE4BECA185002042DCA58ADD62A6E588F`
- Card Prefab: `C4D449A1C298BF5B2A91072A4BEA2A67AFD5E1B1D713A4D769CB843D059DDD8C`
- Board Prefab meta: `035AB747FFD25D01BBC4B1C10A44AB4824D31D6CEF0865545599FF5E2D43FC64`
- Tray Prefab meta: `BF0E9EAEFA6D5D3123AE095F0CD55698DA437644707F7F51EEE5CE19CA6C52A4`
- Pool15 carrier: `E65C64970F43E3D67ADDF5698D4A1F574CD72ED5CCBC2316EE14916B46B253E3`
- Pool15 combat facts: `97994E47B6CCCDA7BA92D2BB53A99FCE11CC8B36523CEA7307BE3013DED3BBF9`
- Item definition: `A99F1CC4BC0CB655790F4040174A711CCE4E43BBBE20D69712FE2470CA14BC49`
- Item catalog: `606ACDC6538EEDA86F7631840A6ACBB47284368F198EF413293BB844833776C9`

Any intake mismatch must stop before product write and return the exact owner
and path. Do not overwrite or reconcile an external owner's change.

### 16.7 Required Runtime Behavior

1. Board and Tray admit exact container rows independently of full roster size.
   Board validates at most 25 legal current placements; Tray validates at most
   65 active current Tray rows. Every visible row must match exactly one roster
   instance and base identity. Duplicate instance IDs or mismatches fail closed.
2. Dynamic pools retain a one-to-one active `itemInstanceId` mapping. Duplicate
   base IDs remain separate. Stable instances reuse their existing pool entry
   across publication where possible.
3. Demand growth and recycling preserve Board/Tray geometry, 5 x 13 scrolling,
   mask, raycasts, artwork aspect, raw grabbed-cell identity, preview/commit,
   rotation/hot-zone, invalid/cancel restore, detail selection, and interaction
   authorization.
4. Reset, unbind, lock, recycle, destroyed view, stale callbacks, and pool
   shrink clear all interaction and presentation residue synchronously.
5. Source-anchor resolution uses the live exact-instance pool mapping, not
   array position. Tray, unlit, missing, recycled, stale, duplicate, and
   mismatched requests reject without fallback or mutation.
6. A synthetic future Item with an existing style key resolves through only a
   catalog row and artwork object in focused tests. No target-ID, resource-path,
   Host-capacity, or VFX Item-ID table edit is allowed.
7. Existing I001/I002 artwork/detail behavior and I031 special lit/unlit,
   non-ordinary-causal behavior remain exact.

### 16.8 Two-Phase Execution And Unity Lease

Phase A is authorized now and includes only code, immutable catalog contracts,
Editor tooling, focused deterministic tests, and separate current-source Runtime
and Editor compilation. It must not launch, stop, poll, wait on, or control any
Unity/Tuanjie/ShaderCompiler/Bee process and must not write Prefabs or assets.

Phase A terminal is:

`PHASE_A_COMPLETE / WAITING_EXPLICIT_UNITY_LEASE / PlayerVisibleDelivery=NONE`

Phase B remains unauthorized until Mainline releases one explicit atomic
`CLOSED_CLEAN` Unity serialization lease after its Host and VFX consumer gates
accept the dynamic provider contract. Then reuse this same visible task for one
bounded Item-owned authoring/validation lifecycle affecting only paths 13-16.
No old REV02/REV03 menu, execute method, marker, or Shell refresh menu may be
used. A revised idempotent dynamic-presentation authoring entrypoint and marker
must be frozen by Item Guard before that lifecycle.

### 16.9 Phase A QA

- Separate current-source Assembly-CSharp and Assembly-CSharp-Editor compile,
  zero errors; do not use a combined assembly as evidence.
- Deterministic pool admission/binding for 0, 1, 7, 8, and 15 instances;
  legal one-cell Board fixtures through 25; active Tray fixtures through 65;
  mixed Board/Tray distributions; full roster larger than either container.
- Duplicate base identities, duplicate instance rejection, stable pool-entry
  reuse, demand growth, recycle, reset/unbind/lock, stale callback, and no
  duplicate listener proof.
- Scroll/mask/raycast, raw-cell input, drag, rotation, move, invalid/cancel,
  detail, source-anchor, unlit, and re-entry regressions.
- Catalog null/duplicate/stale/signature/rarity/family/style/cue/artwork rejection
  matrix and exact defensive immutable snapshots.
- Synthetic future Item using an existing style through data/art only.
- Static proof removes fixed 16-ID artwork/resource tables, exact-seven menu and
  marker strings, semantic capacity checks, per-Item Prefab branches, runtime
  finder/fallback hierarchy, and per-frame allocation paths.
- Whitelist, protected-hash, conflict-marker, encoding, and temporary-artifact
  cleanup checks.

### 16.10 Stop And Return

Stop at the first exact blocker if:

- the current inactive Board artwork child cannot safely serve as a reusable
  pool template without a new carrier;
- runtime cloning cannot preserve Card ScrollRect/input/VFX lifecycle without
  changing the protected Card Prefab;
- Item facts cannot supply an exact effect-family/cue correlation without
  copying or inventing Battle/VFX truth;
- a required Host/VFX change is discovered; return that immutable dependency
  to Mainline without writing outside Item scope;
- any external owner changes an intake/protected path.

Development returns Phase A evidence or one exact blocker to Item Guard. Item
Guard performs one close review and releases the immutable dynamic-provider and
presentation-catalog inventory to Mainline. Strongest final package status,
only after a later accepted Phase B, remains:

`TECHNICAL_PREREQUISITE_COMPLETE / PlayerVisibleDelivery=NONE / MilestoneCompletionEvidence=NO / FreshPlay=NOT_APPLICABLE`

## 20. REV08 Rewarded ItemInstance Combat Fact Hydration

This section adds one Item-owned data correction to the same package lineage
and visible development task. It supersedes older hash-gate and oversized QA
requirements for this correction only. Do not calculate or report file hashes.

### 20.1 Player Boundary And Ownership

- Product context: `CAMPAIGN_NORMAL_LV1`.
- Mainline Guard remains the sole player-facing Delivery Owner. Item Guard is
  the supplier and owns this technical correction.
- Player contribution: a legitimately rewarded ordinary Item carries its real
  Item-owned combat definition into the same formal roster instance and Battle
  input row that already own its placement and lighting state.
- This package does not prove live damage or effects. Mainline and Battle still
  own formal consumption and normal WorldMap Fresh Play.
- Item Detail remains frozen.

### 20.2 Technical Director Decision

- Authoritative static truth remains `C1CampaignPool15BaseCombatFacts`.
- Authoritative live owner remains `C1FormalItemSessionAuthority` and its exact
  ordinary `itemInstanceId` roster snapshot.
- The carrier is pure Item code: the existing immutable ordinary-instance
  snapshot plus the existing `C1FormalItemBattleItemRow`. No Scene, Prefab,
  ScriptableObject, UI, VFX, Editor authoring, or Unity lifecycle is required.
- Reward acceptance resolves the exact combat fact once at the Item hydration
  boundary. The accepted immutable fact remains attached to that instance and
  is published unchanged in the Battle row.
- Battle continues to own clocks, targets, effect execution, damage
  application, and cues. Item publishes definition facts only.
- Existing Card, Board, Tray, artwork, source-anchor, drag, placement, and VFX
  carriers remain presentation-only and unchanged.

### 20.3 Allowed Writes

Use fewer paths when possible. REV08 may modify only:

1. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1FormalItemSessionAndArrangementAuthority.cs`
2. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1FormalCampaignLootEntitlementAcceptance.cs`
3. `Assets/_Game/Scripts/TalismanBag/Editor/ItemCampaignBaseline/C1FormalItemSessionAndArrangementAuthorityTests.cs`
4. `Assets/_Game/Scripts/TalismanBag/Editor/ItemCampaignBaseline/C1FormalCampaignLootEntitlementAcceptanceTests.cs`

The current Pool15 combat facts are read-only input. Mainline, Reward, Battle,
Enemy, Save, Item Detail, presentation code, Prefabs, Scenes, settings, Git,
and worktrees are forbidden writes.

### 20.4 Required Behavior

1. A valid Pool15 reward must resolve one non-null immutable
   `C1CampaignPool15BaseCombatFact` for its exact `baseItemId` before Item
   roster mutation. Missing facts reject the entitlement atomically; no zero
   or placeholder effect is created.
2. The same accepted `C1FormalItemOrdinaryInstanceSnapshot` retains that fact
   and its existing source profile/catalog lineage without changing
   `itemInstanceId`, entitlement identity, Tray placement, or Board placement.
3. `C1FormalItemBattleItemRow` publishes the same immutable fact for the same
   exact instance, including family/variant/category, trigger, condition,
   target, cadence/offset, component operation/stat/magnitude/unit/cap, and
   source lineage already owned by Item data.
4. Duplicate rewards with the same `baseItemId` remain distinct instances but
   carry equivalent immutable definition facts.
5. Existing fixed I001 behavior remains unchanged. The released fixed I002
   reward path remains accepted and may carry the same Item-owned I002 fact.
6. Battle input correlation may include the already existing combat-fact
   canonical identity, but this correction must not add file hash gates or a
   second combat-definition owner.

### 20.5 Proportional QA And Return

- Separate current-source Runtime and Editor compilation, both with zero
  errors.
- One real accepted nonstarter Pool15 reward proves that the exact Authority
  instance and its Battle row expose a nonempty authoritative combat fact.
- One duplicate-base reward case proves separate `itemInstanceId` values with
  equivalent definition facts.
- Preserve the existing fixed I001/I002 authority and reward acceptance
  regressions. Do not add a broad rejection matrix or Unity run.
- Fresh Play is `NOT_APPLICABLE` to this supplier correction. Mainline owns the
  normal-path player proof.

Expected terminal:

`TECHNICAL_PREREQUISITE_COMPLETE / PlayerVisibleDelivery=NONE / MilestoneCompletionEvidence=NO / FreshPlay=NOT_APPLICABLE`

## 21. REV09 Formal Item Action Nian Cost Fact

This section adds one Item-owned balance/input correction to the same package
lineage and visible development task. Current proportional-engineering rules
apply: do not calculate or report file hashes, do not add broad defensive
matrices, and do not launch Unity.

### 21.1 Player Boundary And Technical Director Decision

- Product context is `CAMPAIGN_NORMAL_LV1`.
- Mainline Guard remains the sole player-facing Delivery Owner. Item Guard is
  the supplier for this technical dependency.
- The current concrete formal active action is the existing periodic direct
  action used by the released I001/I002 Lv1 projections. Other Pool15 facts
  remain event-triggered or passive and must not be turned into active actions.
- Item owns the immutable action definition and its Nian cost. Battle owns the
  live Nian state, spend/refund ordering, action execution, targets, damage,
  ledgers, and cues.
- Carrier is pure Item code: the existing Lv1 Item balance source plus one
  immutable action-cost fact on `C1FormalItemBattleItemRow`. No Scene, Prefab,
  asset, UI, VFX, Editor authoring, or Unity lifecycle is required.

### 21.2 PLAYTEST_V1 Balance Decision

- Action definition: `C1_CAMPAIGN_LV1_PERIODIC_DIRECT_DAMAGE_ACTION`.
- Resource key: `nian`.
- Cost: `1` Nian per accepted action.
- Timing correlation: the existing 2-second cadence and first offset already
  owned by the I001/I002 formal action projection.
- Source revision: `C1_CAMPAIGN_LV1_ITEM_ACTION_COST_PLAYTEST_V1`.

One is the smallest positive integral cost. At the existing 2-second cadence
it is 0.5 Nian per second for one active source, and it uses the same one-point
unit as the released I002 refund and I010 cost-reduction facts. Costs 2 and 3
would consume 20 or 30 Nian in the first 20 seconds for one source before the
formal resource loop has player-path evidence, so they are not selected for
this first bounded playtest profile.

### 21.3 Allowed Writes

Use fewer paths when possible. REV09 may modify only:

1. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1Lv1StarterItemBaseline.cs`
2. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1FormalItemSessionAndArrangementAuthority.cs`
3. `Assets/_Game/Scripts/TalismanBag/Editor/ItemCampaignBaseline/C1Lv1StarterItemBaselineVerifier.cs`
4. `Assets/_Game/Scripts/TalismanBag/Editor/ItemCampaignBaseline/C1FormalItemSessionAndArrangementAuthorityTests.cs`
5. `Assets/_Game/Scripts/TalismanBag/Editor/ItemCampaignBaseline/C1FormalCampaignLootEntitlementAcceptanceTests.cs`

Mainline, Reward, Battle, Enemy, Save, Item Detail, presentation code,
Prefabs, Scenes, settings, packages, Git, and worktrees are forbidden writes.

### 21.4 Required Public Behavior

1. Expose one immutable formal action-cost fact with exact base/action identity,
   `resourceKey=\"nian\"`, positive cost `1`, source revision, source Item
   profile lineage, and the existing cadence/first-offset correlation.
2. Publish that fact on the exact `C1FormalItemBattleItemRow` for fixed I001,
   fixed I002, and a legitimately rewarded I002 instance. The row's existing
   `itemInstanceId` remains the live instance identity; the definition cost is
   shared without collapsing duplicate instances.
3. The Item projection boundary must reject missing or mismatched action/base,
   rarity, source profile, or timing facts. It must not create a zero-cost or
   placeholder action.
4. All other Pool15 rows keep a null action-cost fact unless their current Item
   data already defines this exact active action. Passive-only rewards remain
   passive.
5. Preserve direct damage, cooldown, lighting, reward acceptance, roster,
   placement, REV08 combat facts, artwork/VFX, drag, and Item Detail behavior.
6. Do not reference or reuse I031, Lv40, dev-only, BattleSandbox, or Shougunu
   Nian cost types or values.

### 21.5 Proportional QA And Return

- Separate current-source Runtime and Editor compilation with zero errors.
- One exact fixed I001 row and one legitimate rewarded I002 row expose the
  positive Item-owned cost under the same action definition while preserving
  distinct instance IDs.
- One missing/mismatched action lineage case rejects at the Item projection
  boundary. Keep existing fixed Item and reward regressions focused.
- No Unity or Fresh Play. Mainline owns the later normal-path player proof.

Expected terminal:

`TECHNICAL_PREREQUISITE_COMPLETE / PlayerVisibleDelivery=NONE / MilestoneCompletionEvidence=NO / FreshPlay=NOT_APPLICABLE`

## 22. REV10 Formal I031 Nian Capacity Fact

This section adds one Item-owned balance/input correction to the same package
lineage and visible development task. Current proportional-engineering rules
apply: do not calculate or report file hashes, do not add broad defensive
matrices, and do not launch Unity.

### 22.1 Player Boundary And Technical Director Decision

- Product context is `CAMPAIGN_NORMAL_LV1`.
- Mainline Guard remains the sole player-facing Delivery Owner. Item Guard is
  the supplier for this technical dependency.
- Item owns I031's immutable formal Nian source and capacity definition.
  Existing `C1FormalItemSessionAuthority` remains the source of the exact
  I031 roster identity, placement, and ItemSystem lineage.
- Battle owns live current Nian, spend/refund ordering, action execution,
  targets, damage, ledgers, and cues.
- Carrier is pure Item code: one immutable I031 capacity fact on the existing
  `C1FormalItemBattleInputSnapshot`. No Scene, Prefab, asset, UI, VFX, Editor
  authoring, or Unity lifecycle is required.
- Existing Card, Board, Tray, Item Detail, and presentation carriers remain
  unchanged.

### 22.2 PLAYTEST_V1 Balance Decision

- Resource key: `nian`.
- Initial Nian: `27`.
- Maximum Nian: `27`.
- Source revision:
  `C1_CAMPAIGN_LV1_I031_NIAN_CAPACITY_PLAYTEST_V1`.
- Source profile:
  `campaign.normal.lv1.item.i031.nian-capacity.playtest.v1`.

The current formal periodic action first resolves at 2 seconds and then every
2 seconds. The longest accepted 1-1 through 1-5 playtest target is 55 seconds,
so one always-present I001 source can resolve at most 27 times, through 54
seconds. Initial and maximum 27 are therefore the smallest bounded values that
let the mandatory starter action remain executable for the full target window
without assuming a particular reward inventory. I002 refund and I010 reduction
remain downstream modifiers and are not used to shrink the base capacity.

### 22.3 Allowed Writes

Use fewer paths when possible. REV10 may modify only:

1. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1FormalItemSessionAndArrangementAuthority.cs`
2. `Assets/_Game/Scripts/TalismanBag/Editor/ItemCampaignBaseline/C1FormalItemSessionAndArrangementAuthorityTests.cs`

Mainline, Reward, Battle, Enemy, Save, Item Detail, presentation code,
Prefabs, Scenes, settings, packages, Git, and worktrees are forbidden writes.

### 22.4 Required Public Behavior

1. Expose one immutable formal I031 Nian capacity fact with schema, product
   context, `resourceKey="nian"`, initial and maximum values, source revision,
   source profile, exact special I031 identity, stable placement identity and
   anchor, and the current session/arrangement/ItemSystem lineage.
2. Publish the fact through `C1FormalItemBattleInputSnapshot` only when the
   current authority contains exactly the accepted special I031 roster row and
   its stable Board placement is exactly correlated with the current
   ItemSystem snapshot.
3. A missing, moved-to-Tray, stale, or mismatched I031 source yields no
   capacity fact. Do not emit zero or a placeholder.
4. Do not reference or reuse `BattleSandboxNianResourceSnapshot`,
   `I031NianSourceSnapshot`, Lv40 Orange, DevSession, Shougunu, or any dev-only
   Nian type or value.
5. Preserve REV08 combat facts, REV09 action-cost facts, reward acceptance,
   roster and placement behavior, drag, artwork/VFX, and Item Detail.
6. Do not add Battle resource state or spend/refund behavior to Item code.

### 22.5 Proportional QA And Return

- Separate current-source Runtime and Editor compilation with zero errors.
- One real current authority Battle input exposes the formal capacity fact
  with initial/max `27`, exact I031 source placement, and current lineage.
- One missing or moved I031 source produces no capacity fact while preserving
  the rest of the Battle input.
- One stale or mismatched projection request rejects at the Item boundary.
- Confirm the public API has no dev-only or BattleSandbox type reference.
- No Unity or Fresh Play. Mainline owns the later normal-path player proof.

Expected terminal:

`TECHNICAL_PREREQUISITE_COMPLETE / PlayerVisibleDelivery=NONE / MilestoneCompletionEvidence=NO / FreshPlay=NOT_APPLICABLE`

## 23. REV11 Real I004 Formal Source Binding And Carrier Proof

This section is a same-package Item supplier close for the real rewarded I004
path. Current proportional-engineering rules apply: do not calculate or report
file hashes, do not launch or control Unity, and do not expand Item Detail.

### 23.1 Player Boundary And Technical Director Decision

- Product context is `CAMPAIGN_NORMAL_LV1`.
- Mainline Guard remains the sole player-facing Delivery Owner. Item Guard is
  the exact source-provider supplier.
- Mainline has already proven the rewarded I004 remains in the current Item
  Authority as placed and lit, Battle applies `TARGET_SPREAD` damage, and the
  emitted cue preserves the exact reward `itemInstanceId` through the Host.
- Reuse the existing Board dynamic pool, Item presentation catalog,
  `C1Pool15FormalItemSourceAnchorBinding`, Presenter provider API, and the
  authored `ItemRarityContourBloomVfx` carriers already present in the shared
  Card and Board templates. No new Scene, Prefab, registry, fallback UI, or
  runtime hierarchy is allowed.
- Item owns artwork/rarity `Apply` and carrier lifecycle. VFX owns accepted-cue
  `PlayTrigger`; Battle owns timing, targeting, damage, and cue emission.
- Closing Prepare only moves the existing surface and disables input; it does
  not create a second Item source or require a new Item serialization pass.

### 23.2 Allowed Writes

Read-only proof is preferred. If and only if the exact I004 flow exposes a
real Item-owned code break, use the minimum subset of:

1. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1ExactBattleSandboxItemArrangementPresenter.cs`
2. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1ExactBattleSandboxItemBoardView.cs`
3. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1ExactBattleSandboxItemCardView.cs`
4. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1Pool15FormalItemArtworkAndSourceAnchorBinding.cs`
5. `Assets/_Game/Scripts/TalismanBag/Editor/ItemCampaignBaseline/C1ExactBattleSandboxItemPromotionTests.cs`

Mainline, Battle, VFX, Reward, Scene, Prefab, Item Detail, settings, Git, and
worktrees are forbidden writes.

### 23.3 Required Proof And Return

1. The exact I004 presentation row resolves authoritative artwork plus
   `TARGET_SPREAD`, presentation style, and cue lineage.
2. The current placed-and-lit I004 request resolves the active Board pool entry
   by exact `itemInstanceId`, not by base ID or view index.
3. The returned binding matches the request and exposes the current non-null
   Board `sourceAnchor` and the same applied Board-local
   `presentationCarrier`.
4. Tray, unlit, stale, and mismatched requests remain rejected. A failure in
   one pool entry clears only that entry and preserves other Board artwork.
5. Run separate current-source Runtime and Editor compilation and one focused
   real-I004 provider/carrier proof. Do not add a broad matrix or run Unity.

Expected terminal:

`TECHNICAL_PREREQUISITE_COMPLETE / PlayerVisibleDelivery=NONE / MilestoneCompletionEvidence=NO / FreshPlay=NOT_APPLICABLE`
