# V0.4-C1StageThemeBackgroundMainlineBinding01 — Task Contract V2

Status: `FROZEN_DISPATCH_READY / OFFLINE_DEV_ACTIVE / UNITY_AUTHORING_LEASE_GATED`

## 1. Player-visible outcome

In `CAMPAIGN_NORMAL_LV1`, entering the existing Unified battle page through the real WorldMap route selects the authored Stage Theme background from immutable `BattleLaunchContext.StageThemeProfileId`. Current stages 1-1 and 1-2 both show the accepted interior background. Rejection, teardown, return and re-entry clear presentation state so no stale frame or second background survives.

This package binds the released background carrier into the normal route. It does not map exterior/night to a campaign stage, redesign the background, or complete the wider campaign milestone.

## 2. Classification and ownership

- Task class: `COMPLEX_GUARDED_ONCE / MAINLINE_STAGE_THEME_BINDING`
- Primary and user-facing Owner: Mainline Flow Guard `019f45d7-a33b-7630-a049-976481bb7b6d`
- Dependency helper: Animation/VFX Guard; its source-carrier package is terminal
- Authoritative theme selection: existing Chapter 1 StageConfig and immutable `BattleLaunchContext.StageThemeProfileId`
- Authored visual content: released `C1StageThemeBackgroundProfile` assets
- Runtime playback only: existing `C1StageThemeBackgroundPresenter`
- Runtime orchestration/binding: existing `UnifiedBattleFormalSceneHost`
- Scene owner: existing Unified Scene only; no new Scene and no Scene write

## 3. Technical Director carrier decision

- Reuse the existing `C1ExactBattleSandboxGameBackground.prefab` body already nested in `UnifiedBattlePageShell.prefab`.
- Create one Mainline-owned immutable registry/config asset with direct serialized references to exactly the three released Profiles. Asset location under `Resources` is allowed only as an authored asset location; runtime `Resources.Load`, path guessing and string-to-path construction are forbidden.
- Add one narrow Host binding from `consumed.StageThemeProfileId` through the registry to the existing Prefab-local presenter.
- The Prefab visibly contains only the existing full-screen non-raycast UGUI background body and its presentation-only presenter. It does not own stage identity, navigation, Battle, HP, damage, Item, Enemy, Reward, Save or progression truth.
- Apply the resolved Profile only after a valid launch context is consumed and before live formal presentation begins. Reset on every rejection after consumption, dormant state, teardown, disable and re-entry.
- Existing Scene remains unchanged. Only the existing reusable Shell Prefab receives serialized Host references to the registry and the already-nested presenter.

## 4. Released dependency inventory (read-only)

- `Assets/_Game/Prefabs/TalismanBag/UnifiedBattle/C1ExactBattleSandboxGameBackground.prefab`
  - intake SHA-256 `7B8F5AFF95BF681107A9A331D82903D2FCBF743D33B7F69027D650BD3B838FE6`
- `Assets/_Game/Scripts/TalismanBag/Presentation/StageThemes/C1StageThemeBackgroundPresenter.cs`
  - intake SHA-256 `9CF80B1D1940718F7889850F81E5EBBCC6FCB59F1107F64BDE58E5AF12D5D653`
- `Assets/_Game/Scripts/TalismanBag/Presentation/StageThemes/C1StageThemeBackgroundProfile.cs`
  - intake SHA-256 `CDF2C511B1C7FB77ECC04470FFA4F93849F4196C1090970ED325B8982BD86DD1`
- Interior Profile `campaign.normal.lv1.theme.bone_aspect.c1`
  - `Assets/_Game/Resources/V04/StageThemeBackgrounds/C1StageThemeBackground_Interior.asset`
  - intake SHA-256 `5E9D35B032AE56CCEDA3B9D79E7E828168968AB7A7C53161EA0D94DD79188523`
- Exterior Profile `campaign.normal.lv1.theme.bone_aspect.c1.exterior`
  - `Assets/_Game/Resources/V04/StageThemeBackgrounds/C1StageThemeBackground_Exterior.asset`
  - intake SHA-256 `9F5185D05258DB8C44BE4AC88EABE2E463AC14444412EF6CE6F898985E4CF75C`
- Night Profile `campaign.normal.lv1.theme.bone_aspect.c1.night_animated`
  - `Assets/_Game/Resources/V04/StageThemeBackgrounds/C1StageThemeBackground_NightAnimated.asset`
  - intake SHA-256 `7E154C55798C2740A0E22779970D524768A08D3615D9C6341700155E2B20E529`

## 5. Exact allowed writes

Existing runtime and authored composition:

- `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/UnifiedBattleFormalSceneHost.cs`
  - intake SHA-256 `36DDA986BE2EE4262D038B1D102FE00D0E86A6790E4C04F4A83B2708DB4D8B54`
- `Assets/_Game/Prefabs/TalismanBag/UnifiedBattle/UnifiedBattlePageShell.prefab`
  - intake SHA-256 `BA8A421C1CE0260F076106BCE3E540C4E0475BC365A9153F3E5E7F90B079A586`

New Mainline registry/runtime files:

- `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/StageThemes.meta`
- `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/StageThemes/C1StageThemeBackgroundRegistry.cs`
- `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/StageThemes/C1StageThemeBackgroundRegistry.cs.meta`
- `Assets/_Game/Resources/V04/StageThemeBackgrounds/C1StageThemeBackgroundRegistry.asset`
- `Assets/_Game/Resources/V04/StageThemeBackgrounds/C1StageThemeBackgroundRegistry.asset.meta`

New narrow Editor authoring and focused tests:

- `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1StageThemeBackgroundMainlineBindingAuthoring.cs`
- `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1StageThemeBackgroundMainlineBindingAuthoring.cs.meta`
- `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1StageThemeBackgroundMainlineBindingTests.cs`
- `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1StageThemeBackgroundMainlineBindingTests.cs.meta`

No other project file may be written without returning an exact boundary blocker to the Primary Guard.

## 6. Protected boundaries

- Unified Scene is byte-read-only:
  - `Assets/_Game/Scenes/Scene_TalismanBag_V04_UnifiedBattlePageShell.unity`
  - SHA-256 `D432B6E492EDF0CB0EEA8B62E66E27A6EE3F7773C8BBCC8AD886A86A1A0BDD63`
- Background Prefab, Presenter and all three released Profile assets are read-only.
- Stage 1-1 and 1-2 configs are read-only and must retain the existing interior id:
  - `CampaignStage_1-1.asset` SHA-256 `B9E88D0860F686F6BD95AEBF70686246F97897A2BD6F42B47A3470D90C589D81`
  - `CampaignStage_1-2.asset` SHA-256 `EF084C7D5678EFA48E51D7C7074498EEE5CBA3478D2D76E2D26EEDD379ECF706`
- No BattleLaunchContext, StageConfig, Item, Enemy, Battle, Reward, Save, VFX, Navigation, ItemDetail, Scene, BuildSettings, ProjectSettings or Git write.
- No new Scene, second background owner, runtime hierarchy creation, `GameObject.Find`, `Resources.Load`, path guessing, fallback sprite substitution, source-Prefab Apply/Unpack or broad Shell rebuild.

## 7. Required runtime behavior

1. Registry contains exactly three distinct direct Profile references and validates unique exact Profile ids.
2. Registry resolution is ordinal, immutable and fail-closed. Empty/unknown/duplicate/mismatched ids return one exact diagnostic and never choose a fallback.
3. Host authored validation requires exactly one registry and exactly one `C1StageThemeBackgroundPresenter` on the same existing background object as `gameBackgroundImage`; Image remains non-raycast and existing geometry remains unchanged.
4. After formal context consumption, Host resolves `consumed.StageThemeProfileId` and applies that Profile before opening the live formal presentation. Any resolution/apply failure resets the background, rejects admission and logs the exact diagnostic.
5. Dormant, rejected, disabled, teardown, WorldMap return and re-entry paths call `ResetPresentation()` idempotently.
6. Current 1-1 and 1-2 both resolve only the interior Profile. Exterior/night remain available in the registry but are not mapped to a stage here.
7. No per-frame registry allocation, no profile cloning, no material cloning and no duplicate presenter/background root.

## 8. Authoring strategy

- One narrow Editor tool creates/updates only the registry asset and the two Host serialized references in loaded `UnifiedBattlePageShell.prefab` contents.
- Preflight requires the exact intake Shell state, exactly one nested released background Prefab source, exactly one Presenter, exact non-raycast Image identity, and all protected dependency hashes.
- Preserve all unrelated Shell hierarchy, component order, fields, overrides and nested Prefab links. No broad authoring method may be called.
- Save/reload validation proves the registry references, Host references, unique nested background identity, zero Missing Script, unchanged background Prefab and unchanged Unified Scene.
- A rerun on the exact final state is validation-only and idempotent; partial/conflicting states fail closed.

## 9. Unity lease

- Dispatch-time state: no project Unity/Tuanjie/ShaderCompiler/bee Editor process was found, but `Temp/UnityLockfile` exists and `Library/EditorInstance.json` is absent.
- Treat this as an external-or-stale serialization lease. The development task must not remove the lock, start/stop/control Unity, or serialize while it exists.
- Disjoint code, registry type, Editor authoring code, tests and offline compile may proceed immediately.
- Immediately before authoring, require project Editor process count zero and both `Temp/UnityLockfile` and `Library/EditorInstance.json` absent. If not clear, stop at `AUTHORING_READY / EXTERNAL_UNITY_OWNER` with the exact lease evidence.
- Once clear, at most one owned Unity authoring/import lifecycle is allowed. Record command/PID/log/marker and clean only that owned lifecycle.

## 10. QA and acceptance

Offline/deterministic:

- Runtime and Editor scoped compile PASS.
- Focused registry matrix: all three ids resolve exact refs; unknown/empty/duplicate/mismatched ids reject.
- Host lifecycle checks: valid apply, apply failure reset, context rejection reset, teardown/reset/re-entry reset and no duplicate background owner.
- Static proof: no Resources/path lookup, runtime hierarchy fallback, second owner or protected-domain write.

Authored carrier:

- One bounded Shell authoring pass after lease release.
- Serialized inspection proves exactly one registry ref, one presenter ref, one background Prefab instance, non-raycast Image, unchanged geometry, zero Missing Script and no Scene delta.

Player-visible evidence:

- After authored binding, Mainline owns one normal-path Fresh Play: WorldMap -> 1-1 -> Unified, observe the interior background, leave/return/re-enter and confirm no stale/duplicate background.
- Direct Unified entry, synthetic BattleLaunchContext, BattleSandbox fixture and compile-only evidence are not substitutes.
- If reliable UI operation/observation is unavailable, report `USER_FRESH_PLAY_REQUIRED` with the shortest exact steps instead of claiming PASS.
- Appearance quality feedback returns to Animation/VFX as dependency feedback; Mainline remains Owner of normal-route binding.

## 11. Mobile/APK, lifecycle and raycast risks

- Registry uses direct serialized references so Player/APK inclusion does not depend on editor-only discovery.
- Existing one-Image background remains `raycastTarget=false` and below interactive composition.
- Animated playback remains bounded to the released eleven-position sequence; the registry does not preload cloned textures or instantiate visual roots.
- Reset/disable/re-entry must cancel stale sequence state through the existing presenter.

## 12. Stop conditions

Stop with the first exact blocker if implementation requires any of:

- modifying StageConfig/profile ids or deciding which future stage uses exterior/night;
- Scene serialization, source background Prefab modification or a second background owner;
- Resources/path guessing, runtime hierarchy fallback or synthetic launch context;
- Item, Enemy, Battle, Reward, Save, VFX or navigation truth changes;
- controlling/removing an external or unknown Unity owner/lock;
- more than one product authoring lifecycle or blind Fresh Play input.

## 13. Terminal cap

Internal maximum before normal-path observation:

`STAGE_THEME_BACKGROUND_MAINLINE_BINDING_INTERNAL_PASS / FAITHFUL_PARTIAL`

After normal-path Fresh Play or exact user gate:

`STAGE_THEME_BACKGROUND_USER_HANDTEST_READY / FAITHFUL_PARTIAL`

- PlayerVisibleDelivery: `PARTIAL`
- MilestoneCompletionEvidence: `NO`
- This package never claims the wider P0 campaign milestone complete.
