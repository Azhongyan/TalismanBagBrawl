# V0.4-C1UnifiedExactBattleSandboxItemDetailPopupMount01 Task Contract

## 1. Outcome

In the real `CAMPAIGN_NORMAL_LV1` route, clicking a real Tray or Board Item opens exactly one authored, reusable, BattleSandbox-derived Item Detail popup. It uses the released exact standalone Prefab, shows the selected formal Item instance and artwork through the existing formal projection, supports its authored scrolling and close control, and fully releases input when closed.

PlayerVisibleDelivery: PARTIAL until normal-path Fresh Play and user visual acceptance.
MilestoneCompletionEvidence: NO until the real path is observed.

## 2. Product Context

- `CAMPAIGN_NORMAL_LV1`

## 3. Task Class

- `COMPLEX_GUARDED_ONCE`
- New player-visible Prefab mount/integration package.

Reason: one protected authored Shell Prefab must replace an obsolete nested visual body while preserving real Item data ownership, input lifecycle, nested Prefab identity, and the existing formal route.

## 3A. User Review Gate

- Required: YES, after internal authoring/compile/runtime evidence.
- Scope already explicitly accepted: mount the exact BattleSandbox Item Detail Prefab in Unified; no redesign or simplified substitute.
- No further technical/product approval is required before dispatch.

## 4. Owners

- Primary Guard / user-facing Owner: Mainline Guard `019f45d7-a33b-7630-a049-976481bb7b6d`
- Development Owner: one visible direct-local development task created for this package
- Item fact/data owner: `C1FormalItemDetailProjectionAndSelection` and its released combat-detail projection
- Presentation source owner/dependency: Animation/VFX Guard; released exact standalone Prefab only
- Runtime mount/open-close owner: `C1ExactBattleSandboxItemDetailPresenter` in Mainline
- Composition owner: existing `UnifiedBattlePageShell.prefab`
- User Decision Point: NONE

The Mainline Guard remains the single user-facing Owner for this integration node. Animation/VFX is a dependency helper and retains only later visual-parity attribution.

## 5. Player Promise Card

- MilestoneId: `C1_UNIFIED_EXACT_BATTLESANDBOX_ITEM_DETAIL_POPUP_MOUNT`
- ProductContext: `CAMPAIGN_NORMAL_LV1`
- PlayerPromise: From WorldMap into Unified, clicking a real Item opens the exact BattleSandbox-derived detail popup with the actual selected Item's artwork, identity, combat facts and placement/lighting state; closing and re-entry never leave a stale or input-blocking popup.
- PlayerMustDo: enter through WorldMap, click a real Tray Item and a real Board Item, scroll the authored detail content, close it, open another Item, return and re-enter.
- PlayerMustSee: exactly one exact popup body; real selected artwork/name; released direct damage, cooldown/trigger and current Unplaced/Unlit/Contributing state; authored hierarchy, proportions, masks, scrolling and close control.
- MustChangeLive: selection changes the shown formal projection; close/reset clears it and restores Item input.
- PersistenceOrResetExpectation: no stale Item, duplicate popup, duplicate listener, raycast interception or lingering open state across close, reset, teardown and re-entry.
- ExplicitNonCompletionCases: the old thin `ItemDetailPanel` remains the visible body; copied/unpacked/runtime-created hierarchy; fake/sample/BuildSandbox data; black plate or offscreen content; dead close/scroll; closed popup blocks Item input; compile, Prefab existence or serialization alone.
- RequiredRealPath: `WorldMap -> 1-1 -> Unified -> click real Item -> inspect/scroll -> close -> click another Item -> return/re-enter`.
- UserAcceptedAt: latest direct dependency handoff plus the user's prior explicit exact-popup request.

## 6. Capability Coverage

| Capability | State owner | Carrier | Intake status | Required package proof |
|---|---|---|---|---|
| Exact BattleSandbox visible popup body | Animation/VFX source dependency | `C1ExactBattleSandboxItemDetailPopupStandalone.prefab` | TECHNICAL PASS | preserve exact nested source link/hash; authored inspection |
| Real selected Item identity/artwork/combat facts | Item | released formal detail projection | TECHNICAL PASS | consume only existing projection; no inferred/hard-coded facts |
| Formal click/open/close lifecycle | Mainline + Item presenter event | `C1ExactBattleSandboxItemDetailPresenter` | PRESENT, old body only | bind exact descendant view/button; idempotent reset/unbind |
| Unified authored mount | Mainline composition | existing `ItemDetailPopupSlot` / `ItemDetailPresentationFrame` | MISSING exact body | one nested exact standalone Prefab; old nested body retired |
| Closed input safety | Mainline presenter | parent `CanvasGroup` | code present, real composition unproven | alpha 0, interactable false, blocksRaycasts false; input restored |
| Normal player route | Mainline | WorldMap -> Unified | UNPROVEN for new carrier | normal-path Fresh Play when safely operable, then user handtest |

Milestone verdict at dispatch: `FAITHFUL / MILESTONE_FIDELITY_BLOCKED` until mounting and real-path proof.

## 7. Technical Director Decision / Carrier Preview

- Player-visible result: the accepted BattleSandbox popup appears in Unified, not a redraw.
- Authoritative state owner: Item projection remains the sole Item fact owner.
- Correct carrier: existing Unified Scene + existing Shell Prefab + released standalone popup Prefab + narrow Mainline Presenter adapter + one Editor authoring tool.
- Prefab visibly contains: exact popup Canvas/SafeArea/PopupLayer/body, images, text hierarchy, masks, ScrollRects, tabs/sections and local close/input surfaces already serialized upstream.
- Prefab never owns: Item roster, damage/cooldown truth, placement legality, Battle state, reward, inventory or save.
- Runtime feeder: `C1ExactBattleSandboxItemArrangementPresenter` formal detail event -> `C1ExactBattleSandboxItemDetailPresenter` -> nested `ItemDetailPanelView`.
- Reuse: existing `ItemDetailPopupSlot`, `ItemDetailPresentationFrame`, catalog provider, Host admission and source standalone Prefab.
- Create: one narrow Editor mount/validator and focused tests only; no new product Prefab or Scene.
- Serialization: only `UnifiedBattlePageShell.prefab`; source standalone Prefab and Unified Scene remain byte-protected.
- Unity lease: one owner for the bounded Shell authoring pass; code/compile may proceed offline. Never control user/unknown Unity.
- QA class: scoped Runtime/Editor compile, focused deterministic/static tests, one Prefab authoring/validation pass, presentation inspection, then normal-path Fresh Play if observable; otherwise exact user handtest gate.
- Mobile/APK risks: nested Canvas/CanvasScaler/GraphicRaycaster ordering, SafeArea scaling, masks/stencils, full-screen raycasts, closed lifecycle, duplicate EventSystem/listeners and overdraw. Validate no runtime/editor-only dependency and no per-frame find or runtime final hierarchy.
- Stop condition: stop on need to change Item truth/projection, source standalone Prefab, Scene, Sandbox controller, Host admission contract, or to unpack/copy/rebuild the Shell.

## 8. Allowed Writes

1. `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/Presentation/ExactItemDetail/C1ExactBattleSandboxItemDetailPresenter.cs`
2. `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1UnifiedExactBattleSandboxItemDetailPopupMountAuthoring.cs` and `.meta` (new)
3. `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1UnifiedExactBattleSandboxItemDetailPopupMountTests.cs` and `.meta` (new)
4. `Assets/_Game/Prefabs/TalismanBag/UnifiedBattle/UnifiedBattlePageShell.prefab`
5. This Task Contract, Guard-owned and read-only to Development after dispatch.

Patch budget: up to 6 product/code files plus the contract. Exceeding it requires stopping with the first exact dependency; do not broaden.

## 9. Protected / Forbidden Writes

- `Assets/_Game/Prefabs/TalismanBag/Items/C1ExactBattleSandboxItemDetailPopupStandalone.prefab` and `.meta`
- `Assets/_Game/Prefabs/TalismanBag/Items/ItemDetailPanel.prefab` and `.meta`
- `Assets/_Game/Scenes/Scene_TalismanBag_V04_UnifiedBattlePageShell.unity`
- BattleSandbox Scene and every Sandbox runtime/controller
- Item Authority, formal Item projection/contracts/catalog/baselines and exact Board/Tray/Card Prefabs/scripts
- Unified Host, shell slot contracts, WorldMap, navigation, Prepare, background, battle presentation, Enemy, Battle, Reward, Save, VFX and Project/Build settings
- No runtime-created final UI, `GameObject.Find`, Resources path guessing, Prefab unpack/apply-to-source, broad rebuild, Revert All, Apply All, Git/worktree or unrelated cleanup.

Protected intake hashes:
- Shell: `69E34855B9146DA50F941CF19F389FEEB005ED35861D1011C30293D6BC147090`
- Exact standalone popup: `992313B2958E4AA1E70D1455106B5FA64C267A18695CE0361513937C87E5FE61`
- Unified Scene: `D432B6E492EDF0CB0EEA8B62E66E27A6EE3F7773C8BBCC8AD886A86A1A0BDD63`
- Old ItemDetail source asset: `2A6924DA0823B74E2639B593926D8C2F8422101B01EDBE4073AF33CD424E027C`

## 10. Required Behavior

1. Replace only the old nested `ItemDetailPanel.prefab` instance under the authored frame with exactly one nested instance of the released standalone popup Prefab; preserve the source Prefab link and internal hierarchy without unpacking, copying or rescaling its internal visible body.
2. The existing Presenter may gain one narrow serialized carrier-root reference so it can validate the exact descendant `ItemDetailPanelView` and close button inside the standalone carrier. It must not create hierarchy at runtime or become Item truth.
3. Keep `ItemDetailPopupSlot` as the one lifecycle/input gate. Closed means alpha=0, interactable=false, blocksRaycasts=false and the exact carrier cannot intercept Tray/Board input.
4. Explicit Item click opens only the real current formal projection. Movement drag must not open detail. Close, reset, unbind and re-entry clear current visuals/listeners/state.
5. Preserve the exact internal Canvas/SafeArea/PopupLayer/body, ScrollRects, masks, labels, images and authored control order. Adapt only the outer mount/reference frame required by Unified.
6. Authoring must classify current exact intake, final mounted and invalid-conflict states; be idempotent and fail before save on drift or duplicate/partial carriers.
7. Final Shell contains one popup slot, one presentation frame, one standalone Prefab instance, one Presenter, one exact `ItemDetailPanelView`, no old nested panel Prefab instance, no missing script, no forbidden Sandbox owner and no duplicate raycast/listener owner.

## 11. Failure / Edge Cases

- Missing/invalid formal projection: remain closed; no sample text or fallback.
- Legacy font/content bind failure: remain closed with exact diagnostic; never show a dark backing alone.
- Duplicate event/result: no duplicate open/listener; stable selection is idempotent.
- Zero/invalid viewport: fail closed; never expose a full-screen blocking plate.
- Reset/unbind/teardown/re-entry: close and release input synchronously.
- Existing unexpected Shell override or source-link drift: stop before save with exact diagnostic; no broad repair.

## 12. Verification

- [ ] Intake hashes and source/meta GUIDs exact before writes.
- [ ] Scoped Assembly-CSharp and Assembly-CSharp-Editor compile, zero errors.
- [ ] Focused deterministic/static tests for descendant carrier validation, open/close/idempotence, font/layout failure, reset/unbind and closed raycast state.
- [ ] One bounded owned Shell Prefab authoring pass after an atomic Unity lease check.
- [ ] Reloaded serialized validation: unique carrier/ref chain, exact source link, old nested source GUID count zero, missing scripts zero, forbidden owners zero, source Prefab and Scene hashes unchanged.
- [ ] Presentation inspection in normal Editor/Prefab context: content frame is onscreen, scroll/mask/close are interactive, dark backing is behind readable content.
- [ ] One normal-path Fresh Play from WorldMap when input/observation is reliable; otherwise return `USER_FRESH_PLAY_REQUIRED` with the shortest exact 3-5 steps.

Compile/static/authoring does not prove player-visible completion.

## 13. Process Ownership

- Before Unity: verify no user/unknown Unity/Tuanjie/ShaderCompiler/bee process, `Temp/UnityLockfile`, or `Library/EditorInstance.json` owner.
- If occupied, continue disjoint code/compile and stop at `AUTHORING_READY / EXTERNAL_UNITY_OWNER` with exact owner evidence; never stop/kill it.
- If clear, one bounded owned authoring lifecycle. Record PID, command, log, timeout and cleanup. Stop on first objective failure; one harness-only correction maximum.
- Do not use Git/worktree.

## 14. Completion / Return Contract

Development returns exactly one terminal result to Mainline Guard:
- `ITEM_DETAIL_EXACT_POPUP_INTERNAL_QA_READY`, or
- first exact blocker/failure layer with all safe changes preserved.

Mainline Guard performs one close review. Same-package corrections stay in the same visible task. Strongest internal status is `USER_HANDTEST_READY / FAITHFUL_PARTIAL`; `COMPLETE` requires user PASS on the real path.

Required user handtest after internal close:
1. `WorldMap -> 1-1 -> Unified`, click a real Tray Item and verify exact popup + correct artwork/facts + scrolling.
2. Close it, verify Tray/Board input resumes, then click a Board Item and verify the selected data changes.
3. Return/re-enter and verify exactly one initially closed popup with no stale content/input block.

Visual mismatch feedback returns to Mainline as the locked integration Owner; Mainline consults Animation/VFX without transferring user responsibility.
