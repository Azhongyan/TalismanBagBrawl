# V0.4-C1UnifiedRealtimeBattleAssembly01 Assignment

Status: `COMPLEX_GUARDED_ONCE / BOUNDARY_FROZEN / READY_FOR_DEV`

## 1. Identity

- Package: `V0.4-C1UnifiedRealtimeBattleAssembly01`
- Primary Guard: Mainline Flow Guard
- Product context: `CAMPAIGN_NORMAL_LV1`
- User Review Gate: `YES / USER_AUTHORIZED_FINAL_P0_REALTIME_ASSEMBLY_ROUTE`
- PlayerVisibleDelivery: `FULL only after the exact real path exists`
- MilestoneCompletionEvidence: `YES only after user real-path acceptance`
- Git/worktree: `FORBIDDEN`
- Development owner: exactly one new visible, unarchived, direct-local task

This package is the final player-visible assembly. It may not inherit the failed
`USER_HANDTEST_READY` claim from `V0.4-C1FormalObtainRebuildBattleLoop01`.

## 2. Immutable Player Promise Card

```text
MilestoneId: C1_P0_REALTIME_OBTAIN_REBUILD_VERIFY
ProductContext: CAMPAIGN_NORMAL_LV1
PlayerPromise: From WorldMap, the player enters 1-1 and sees a real BattleSandbox-derived realtime battle, receives guaranteed meaningful I002, rearranges it, then sees realtime 1-2 produce the accepted materially different timing/survival result before returning to WorldMap.
PlayerMustDo: select WorldMap 1-1, observe the full live battle, claim I002, rearrange I001/I002 around I031, start and observe 1-2, return WorldMap.
PlayerMustSee: correct player/enemies, live HP, authoritative target, exact Item source triggers, enemy attacks, Hit, semantic floats, Death, restrained VFX/audio, terminal result, visible I002 claim and availability, rebuild and visibly different 1-2.
MustChangeLive: Battle time, player/enemy HP, target/death, Item/enemy applications, presentation cues and audio advance over time; 1-2 target arrangement is 25% faster and avoids three enemy applications versus weak layout.
PersistenceOrResetExpectation: I002 persists through this memory-only 1-1 -> rebuild -> 1-2 session; durable Inventory/Save persistence is outside this P0; exit/reset leaves no stale Battle, reward, presentation or audio state.
ExplicitNonCompletionCases: new generic/redrawn UI, summary-only or precomputed Battle, replay/preview substitute, missing live enemies/HP/target/cues/VFX/audio, missing visible I002 claim/rebuild, dev panels, runtime fallback hierarchy, copied BattleSandbox graph, or a handtest that omits any promise row.
RequiredRealPath: WorldMap -> 1-1 realtime fight -> terminal -> fixed I002 claim -> player rearranges -> 1-2 realtime fight with visible accepted consequence -> WorldMap.
UserAcceptedAt: USER_AUTHORIZED_FINAL_P0_REALTIME_ASSEMBLY_ROUTE + PLAYER_PROMISE_CLARIFICATION
```

The promise is invariant. Shared words such as `formal`, `Prefab`, `real`,
`PASS` or `integrated` do not prove it.

## 3. Milestone Fidelity Coverage

| Capability | State owner | Concrete carrier | Status at intake | Required C evidence |
|---|---|---|---|---|
| WorldMap 1-1 launch and Unified admission | Mainline | `FormalBattleLaunchTransit`, `BattleLaunchContext`, `UnifiedBattleFormalSceneHost` | present | user real-path |
| Live clock, actors, HP, target, actions, terminal | Battle/Bridge | `C1FormalRealtimeBattleSession` + Adapter + immutable start/tick/cues | A terminal | runtime integration |
| BattleSandbox-derived player/enemy/HUD/feedback | Visual | `FormalBattlePresentationRoot.prefab` and nested presentation prefabs/profile | B terminal, player behavior unproven | normal-Editor presentation inspection + user real-path |
| Initial authoritative target | Battle -> Mainline transport -> Visual | `CurrentRealtimeStart.initialCues` | released | target outline visible before first tick and refreshed for 1-2 |
| Formal Board/Tray/Card and arrangement truth | Item | `C1FormalItemSessionAuthority`, Board/Tray/Card prefabs | present | real drag/rebuild and snapshot-driven 1-2 |
| Fixed meaningful I002 first-clear reward | Reward/Item/Mainline | released fixed RewardResult/claim/dedupe + entitlement + existing product action surface + Item Tray | data/session present | I002 identified, claimed once, visibly appears in Tray and is usable |
| Result only from realtime terminal | Battle/Mainline | realtime terminal projected through `C1FormalObtainRebuildBattleLoop` | present in code, unproven real path | no immediate summary; action unlocks only after terminal |
| 1-2 visible consequence | Item + Battle + Visual | current arrangement snapshot -> realtime 1-2 -> presentation | unproven | target layout 36s/7 attacks/HP44; weak 48s/10/HP20 remains supported |
| Return and cleanup | Mainline | `TalismanSceneNavigationOwner`, loop/presentation reset | route owner present | return WorldMap; no stale actors/cues/audio/reward |
| Clean product page | Mainline + Visual | Unified authored slots/prefabs | unproven | no dev panels, fallback hierarchy, coarse card projectile/fire substitute |

At intake no required structural carrier is `MISSING`, `UNOWNED` or
`PROXY_ONLY`. Every player-visible row is nevertheless
`UNPROVEN_DOWNSTREAM_C`; C exists to assemble and prove them.

## 4. Technical Director Decision

- Existing Scene: use only `Scene_TalismanBag_V04_UnifiedBattlePageShell`.
- Prefab composition: use the current `UnifiedBattlePageShell.prefab` with the
  already promoted Formal Battle Presentation and Formal Item prefabs.
- No new Scene and no simplified/redrawn substitute UI.
- Scene owns slots/composition/transitions only.
- Battle Session A remains sole clock/HP/target/action/result owner.
- Formal Item authority remains sole roster/placement/lighting owner.
- Reward contracts and Item entitlement remain sole I002 truth.
- Presentation B consumes immutable start/tick cues and owns visuals/audio only.
- Mainline host owns phase orchestration, binding, input lock, terminal-to-reward,
  rebuild-to-1-2 and return navigation.
- Existing B presentation components/prefabs/profile and A session contracts are
  read-only dependencies. Owner defects return to their original task; C does
  not rewrite them.

### Concrete carrier inventory

Present and reusable:

- `Scene_TalismanBag_V04_UnifiedBattlePageShell.unity`
- `UnifiedBattlePageShell.prefab`
- `FormalBattlePresentationRoot.prefab`
- `FormalBattlePlayerPresentation.prefab`
- `FormalBattleEnemySlot.prefab`
- `FormalBattleDamageFloatPool.prefab`
- `FormalBattleCueFxAudioRoot.prefab`
- `FormalBattlePresentationProfile.asset`
- `C1FormalItemBoard.prefab`, `C1FormalItemTray.prefab`, `C1FormalItemCard.prefab`
- `C1FormalRealtimeBattleSession`, Adapter, contracts and initial-cue seam
- fixed I002 Reward/entitlement/session chain

Created by this package:

- at most one narrow Editor integration verifier plus `.meta`.

Still missing:

- no structural carrier is known missing at freeze time;
- the complete target-context runtime/presentation/user evidence is missing and
  is the sole reason the milestone remains blocked.

## 5. Exact Allowed Writes

Modify only when required by a reproduced final-integration defect:

```text
Assets/_Game/Scripts/TalismanBag/UnifiedBattle/UnifiedBattleFormalSceneHost.cs
Assets/_Game/Scripts/TalismanBag/UnifiedBattle/C1FormalObtainRebuildBattleLoop.cs
Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1FormalObtainRebuildBattleLoopTests.cs
Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1FormalObtainRebuildBattleLoopSceneAuthoring.cs
Assets/_Game/Prefabs/TalismanBag/UnifiedBattle/UnifiedBattlePageShell.prefab
```

Add only:

```text
Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1UnifiedRealtimeBattleAssemblyVerifier.cs
Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1UnifiedRealtimeBattleAssemblyVerifier.cs.meta
```

The Unified Scene is a protected read-only carrier and must remain byte-for-byte
unchanged. The existing Prefab instance must receive shared-prefab changes.

## 6. Intake Baselines

```text
UnifiedBattleFormalSceneHost.cs|C0B6B68E15E0803896AE3D0248611C8F944428BA7DAAF554A345D401CFC5D898
C1FormalObtainRebuildBattleLoop.cs|DB529AC71239E1D3C94EAF650CF9170A767D7BD83F6839C8A44B96A05BE7623B
C1FormalObtainRebuildBattleLoopTests.cs|9C505DC101306D9D0354C9B2248811ABA2C1D6ABA340DC2E04E3AC5F2DCC8D0E
C1FormalObtainRebuildBattleLoopSceneAuthoring.cs|CCD9BD6958C81B7A8BA077C741E197D270F135CA85792ABAA5A8196F70A3A2A0
UnifiedBattlePageShell.prefab|5E64146954350BD4F16877C9328DE371B0A16A2C3F2FFD7855727E71ABFBFF34
UnifiedBattle Scene|FC5E64037CACB2EA00ACC7BA614D404E3AB26D5FFB3CC091DF0BF15B4458F1D9
Realtime contracts|B82C0B87A2FCAF214E88113732A3477B03430AA9A5C618D1010F3DB37758C065
Realtime session|4AA29D486EC8E7054D0FE5B106EEF79DFDA2B28A85232AD3399CAE0F38D95D47
Realtime adapter|4304CB3D6E267EC1AF53B00061501A88DF754B8025D1DB6049046071D99AFB1F
FormalBattlePresentationRoot.cs|D5B0D71CA18D79D6A6F2C4266B38E64BA52FB9E1739EDCE582687F741F6EC7C4
FormalBattlePresentationProfile.asset|4A73C98F4E0C97106BC19FF5E0C9FE95CAC23D9F885F08DE92B7E9C6BEC772D2
FormalBattlePresentationRoot.prefab|F614F510B0E2E305E1C021063F3F3A6DF2B9E4783003E0338215DFCFDB0B8D49
C1FormalItemBoard.prefab|984EEA4E7A33D86B29F16A1C5B59D26C08889D79E5A8F6E9D1C9B969F41DEFDD
C1FormalItemTray.prefab|416E534214B429A21A2EC7EC431EDDB324DF3A4EDAADE87155E2817CF35A5605
C1FormalItemCard.prefab|9D97B2B612F65FD0D1574FE13C690416523D7FE19A740F3854C4FC1AAAD06868
WorldMap Scene|9F946361BA208E129A349BB9F382F40A18D33DC49D6EA4DC68D0E8605B108B3E
EditorBuildSettings.asset|CF244226FFD286638DEC957BBE5A9ABCA1DE52E9D529D71D406B5D03DE805932
```

Preserve unrelated dirty/untracked work. If an allowed/protected baseline drifts
after intake, stop with the exact conflict; do not reset or restore it.

## 7. Required Behavior

1. WorldMap 1-1 publishes one accepted `CAMPAIGN_NORMAL_LV1` context and enters
   the existing Unified Scene.
2. Unified starts one live non-terminal 1-1 session. It must not call the old
   one-shot application as the player-visible Battle path.
3. Presentation begins from `CurrentRealtimeStart.initialCues` before the first
   tick, then consumes each tick's emitted cues monotonically and once.
4. 1-1 displays the correct player plus two Shattered Host actors with live HP,
   one authoritative target outline, I001 source pulse, enemy Attack, Hit,
   semantic damage floats, restrained impact VFX/audio and Death.
5. Board/Tray remain the existing formal Item prefabs. During live Battle,
   arrangement input is locked without hiding the current Build; it unlocks only
   in the rebuild phase.
6. Stage result and the I002 claim action become available only after the
   realtime session reaches accepted terminal state.
7. Fixed `I002@white` is identified to the player, claimed exactly once through
   the existing entitlement chain, visibly appears in the formal Tray, and is
   movable/rotatable through Item authority. A duplicate click is a no-op.
8. Starting 1-2 takes a fresh immutable Item Battle snapshot from the player's
   arrangement, starts a fresh live session and resets presentation dedupe,
   targets, transient VFX and audio.
9. 1-2 displays two Shattered Hosts plus one Porcelain Hound and refreshed target.
   I001/I002 source resolution must match the accepted roster and placement.
10. The accepted target arrangement terminals at 36s / 7 enemy applications /
    player HP 44. Weak arrangement support remains 48s / 10 / HP 20. No balance
    data may be retuned by this package.
11. After terminal 1-2, the existing action returns to WorldMap and clears all
    memory-only Battle, reward, Item presenter, target, cue, VFX and audio state.
12. Normal Play contains no dev/test panel, OnGUI, selector, fallback hierarchy,
    card projectile/travel or coarse fire-explosion substitute.

## 8. Explicit Non-Completion Cases

The package cannot return `USER_HANDTEST_READY` if any of these is true:

- Battle is immediate, summary-only, replay-only or precomputed;
- the page is a new generic/redrawn substitute rather than the promoted
  BattleSandbox-derived Prefab composition;
- any player/enemy/HP/target/Item trigger/enemy attack/Hit/float/Death/VFX/audio
  row is absent or only proven by compile/Prefab existence;
- I002 is text-only and never visibly enters the Tray for rearrangement;
- 1-2 does not start from the player's accepted Item snapshot or does not show
  the accepted live consequence;
- a dev panel/fallback hierarchy/dev context is required;
- the proposed user handtest omits any promise row.

## 9. Forbidden Writes And Ownership

- Do not modify A Battle contracts/session/adapter/oracle or retune timing/damage.
- Do not modify B Presentation scripts/prefabs/profile/audio; return a proven
  B-owned defect to the same B task instead.
- Do not modify Item authority/presenter/Board/Tray/Card, Reward contracts/data,
  Enemy balance/config, WorldMap Scene/route, navigation owner, Save, Inventory,
  Drop probability, Progression, BuildSettings, ProjectSettings, BattleSandbox,
  CoreLoopLab, V02/V03, AGENTS/LOCKED/Queue or Git.
- Do not create a second clock, HP, target, Item, reward or presentation truth.
- Do not create runtime fallback UI or use name/Scene/display-text inference.
- Do not expand into random drops, permanent Save/Inventory, 1-3..1-10, Boss,
  story, tutorial, offline patrol or full reward economy.

## 10. Patch Budget And Unity Ownership

- Existing files modified: target 0-3, hard maximum 5 from the whitelist.
- New files: maximum 2, verifier plus `.meta`.
- No new Scene or presentation Prefab.
- One Unity serialization owner. The current shared Prefab composition should be
  reused; one narrow Prefab authoring correction is allowed only if an exact
  missing serialized reference is reproduced. Unified Scene save remains zero.
- Record owned Unity PID/command/log/timeout and leave no owned child process or
  lock. Never control an external/user Unity process.

## 11. Verification And Evidence Classes

Required before `USER_HANDTEST_READY`:

1. Scoped Runtime and Editor compile.
2. Deterministic loop/runtime integration tests proving initial cues, monotonic
   tick consumption, terminal-only result/reward, 1-2 fresh start, dedupe/reset,
   and unchanged 1-1/1-2 oracle traces.
3. Authored Prefab/slot validation: exactly one Formal Battle root, player view,
   three enemy slots, float pool, cue/VFX/audio root, Board, Tray, Item presenter
   and formal host; no missing scripts or fallback hierarchy.
4. One normal-Editor presentation inspection on the actual WorldMap -> Unified
   path covering both 1-1 and 1-2. Do not substitute compile, static Prefab
   inspection, a verifier-only fixture or a summary string.
5. Milestone Fidelity close review must enumerate every coverage row and its
   evidence class. `USER_HANDTEST_READY` is allowed only when the exact continuous
   handtest below exercises all rows.

No repeated Unity window/batch/harness loop. One failed automation attempt may
fall back to the shortest normal Editor inspection. User acceptance remains the
strongest evidence and is not inherited.

## 12. One Continuous User Handtest

```text
WorldMap challenge 1-1
-> confirm correct player and two 1-1 enemies appear
-> watch live HP, target, I001 source pulse, enemy Attack, Hit/floats/Death,
   restrained VFX and all three semantic audio cues over time
-> confirm result appears only after terminal
-> claim fixed I002 once and see it appear in Tray
-> rearrange I002 into the accepted lit target layout
-> start realtime 1-2
-> confirm two Shattered Hosts + Porcelain Hound, refreshed target and no stale
   1-1 visuals/audio
-> observe the accepted 36s / 7 enemy applications / player HP44 consequence
-> return WorldMap
-> confirm no dev panel, immediate summary substitute, duplicate reward, stale
   cue/audio, card projectile or fallback hierarchy
```

## 13. Completion And Stop

Allowed terminal before user action:

```text
MILESTONE_INTEGRATED / USER_HANDTEST_READY
```

Final milestone `COMPLETE` requires the user's acceptance of the exact continuous
real path. Technical/compile/Prefab PASS cannot promote it.

Stop and return to the named owner if:

- `BATTLE_OWNER_DEFECT`: A session/cue truth must change;
- `PRESENTATION_OWNER_DEFECT`: B component/prefab/profile/audio must change;
- `ITEM_OR_REWARD_OWNER_DEFECT`: entitlement, arrangement or Item carrier must change;
- `FORMAL_ROUTE_DEFECT`: WorldMap/navigation truth must change outside this whitelist;
- `MILESTONE_FIDELITY_BLOCKED`: any promise row is missing, proxy-only or the
  handtest would be weakened;
- `SERIALIZATION_CONFLICT`: another active owner touches the Unified prefab/scene;
- any solution requires new Scene, redrawn UI, runtime fallback or formal
  Save/Drop/Inventory expansion.

Do not dispatch follow-on work from the development task.
