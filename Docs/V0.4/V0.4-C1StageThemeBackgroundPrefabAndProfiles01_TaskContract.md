# V0.4-C1StageThemeBackgroundPrefabAndProfiles01 — Task Contract V2

Status: `FROZEN_DISPATCH_READY`

Dispatch gate: `V0.4-ItemLivingGradientOutlineExactCarrierPromotion01` is terminal and its carrier has been released. The existing popup task is occupied by a genuine `USER_MENU_REQUIRED` gate, so this genuinely new package uses one dedicated visible direct-local development task under the standing user authorization. Disjoint code/Profile preparation may proceed immediately. Prefab/import serialization remains `UNITY_HOLD` until Mainline directly releases the single Unity lease and the development task rechecks process/lock state atomically.

## 1. Outcome

The Unified formal battle page retains one authored, reusable background body. A directly supplied Stage Theme background Profile selects exactly one of three appearances: static interior, static exterior, or the authored night sprite sequence. A new stage/session/reset starts cleanly without stale frames, duplicate roots, input interception, Scene-per-theme composition, or runtime hierarchy fallback.

Carrier phase delivery is a technical prerequisite only. It does not claim that formal `StageThemeProfileId` binding or the real-path visual handtest is complete.

## 2. Product Context

`CAMPAIGN_NORMAL_LV1 / Unified`

## 3. Task Class

`COMPLEX_GUARDED_ONCE / NEW_SEQUENTIAL_STAGE_THEME_BACKGROUND_PRESENTATION_PACKAGE`

Reason: the package authors a reusable Prefab/Profile carrier and changes full-screen sprite import settings, while authoritative stage-context resolution remains downstream Mainline ownership.

## 3A. User Review Gate

- Required: `YES`
- Fixed Guard: Animation/VFX/Shared Presentation Guard
- `USER_ACCEPTED_SCOPE`: `USER_AUTHORIZED_BACKGROUND_PREFAB_WORK / TECHNICAL_DIRECTOR_DECISION / ADDITIVE_REQUEST`
- No additional user decision is required for carrier creation. Which future stage IDs use exterior or animated night remains a later content-mapping choice and does not block this package.

## 4. Owners

- Primary Guard: Animation/VFX/Shared Presentation Guard `019fa19d-1609-7f03-ad5b-5f12cba0fdc4`
- Development Owner: one dedicated visible direct-local task titled `制作C1场景主题背景Prefab与三套Profile`; no hidden task, child implementation task, worktree or Git
- Authoritative stage/theme selection owner: Mainline/StageConfig through immutable `StageThemeProfileId`
- Presentation owner: this Prefab/Profile package
- Downstream binding owner: Mainline Guard `019f45d7-a33b-7630-a049-976481bb7b6d`
- User Decision Point: `NONE_NOW`

## 5. Allowed Writes

Exact existing carrier:

- `Assets/_Game/Prefabs/TalismanBag/UnifiedBattle/C1ExactBattleSandboxGameBackground.prefab`

New runtime carrier files and Unity metas:

- `Assets/_Game/Scripts/TalismanBag/Presentation/StageThemes.meta`
- `Assets/_Game/Scripts/TalismanBag/Presentation/StageThemes/C1StageThemeBackgroundProfile.cs`
- `Assets/_Game/Scripts/TalismanBag/Presentation/StageThemes/C1StageThemeBackgroundProfile.cs.meta`
- `Assets/_Game/Scripts/TalismanBag/Presentation/StageThemes/C1StageThemeBackgroundPresenter.cs`
- `Assets/_Game/Scripts/TalismanBag/Presentation/StageThemes/C1StageThemeBackgroundPresenter.cs.meta`

New one-shot authoring/validation files and Unity metas:

- `Assets/_Game/Scripts/TalismanBag/Editor/Presentation/StageThemes.meta`
- `Assets/_Game/Scripts/TalismanBag/Editor/Presentation/StageThemes/C1StageThemeBackgroundPrefabAuthoring.cs`
- `Assets/_Game/Scripts/TalismanBag/Editor/Presentation/StageThemes/C1StageThemeBackgroundPrefabAuthoring.cs.meta`
- `Assets/_Game/Scripts/TalismanBag/Editor/Presentation/StageThemes/C1StageThemeBackgroundCarrierTests.cs`
- `Assets/_Game/Scripts/TalismanBag/Editor/Presentation/StageThemes/C1StageThemeBackgroundCarrierTests.cs.meta`

New Profile directory, folder meta, three Profile assets and their metas:

- `Assets/_Game/Resources/V04/StageThemeBackgrounds/**`

Import-setting changes only; PNG bytes are forbidden:

- `Assets/_Game/Resources/场景地图/场景动画序列帧/frame_00001.png.meta`
- `Assets/_Game/Resources/场景地图/场景动画序列帧/frame_00002.png.meta`
- `Assets/_Game/Resources/场景地图/场景动画序列帧/frame_00003.png.meta`
- `Assets/_Game/Resources/场景地图/场景动画序列帧/frame_00004.png.meta`
- `Assets/_Game/Resources/场景地图/场景动画序列帧/frame_00005.png.meta`
- `Assets/_Game/Resources/场景地图/场景动画序列帧/frame_00006.png.meta`
- `Assets/_Game/Resources/场景地图/场景动画序列帧/frame_00007.png.meta`
- `Assets/_Game/Resources/场景地图/场景动画序列帧/frame_00008.png.meta`
- `Assets/_Game/Resources/场景地图/场景动画序列帧/frame_00009.png.meta`
- `Assets/_Game/Resources/场景地图/场景动画序列帧/frame_00010.png.meta`
- `Assets/_Game/Resources/场景地图/场景动画序列帧/frame_00011.png.meta`

Do not add any other file without returning an exact boundary blocker to the Primary Guard.

## 6. Forbidden Writes

- All Scenes, including Unified and BattleSandbox Scenes
- `UnifiedBattleFormalSceneHost.cs`, Unified shell Prefabs and Mainline navigation/host binding
- all StageConfig and Battle launch-context files
- all Item/Card/Board/Tray, Enemy, Battle, Save, Reward and RunFlow files
- the two static PNG bytes/metas, all eleven sequence PNG bytes, and `worldmap.png`
- the active ItemLiving seven-layer VFX package files and its Prefab/Profile/material/shader carrier
- BuildSettings, ProjectSettings, AGENTS, LOCKED, Queue, Notion and Git

## 7. Required Sources / Protected Baselines

- `Assets/_Game/Prefabs/TalismanBag/UnifiedBattle/C1ExactBattleSandboxGameBackground.prefab`
  - frozen intake SHA-256: `B79F90501227A4FA5CB35BA1F3F85DED7B8C83397428FBF56F39481669CB5906`
- `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/UnifiedBattleFormalSceneHost.cs`
  - current read-only intake SHA-256: `36DDA986BE2EE4262D038B1D102FE00D0E86A6790E4C04F4A83B2708DB4D8B54`
  - the earlier frozen hash belonged to an upstream revision; this package did not write the Host and must keep the current intake hash unchanged
- `Assets/_Game/Resources/场景地图/ChatGPT Image 2026年7月24日 17_40_43.png`
- `Assets/_Game/Resources/场景地图/ChatGPT Image 2026年7月24日 17_40_50.png`
- `Assets/_Game/Resources/场景地图/场景动画序列帧/frame_00001.png` through `frame_00011.png`
- current immutable `BattleLaunchContext.StageThemeProfileId` and C1 StageConfig values, read-only
- `BattleSandboxAnimatedBackgroundPrototypeController.cs` may be read only as lifecycle/timing reference; its bootstrap, finder and Resources-loading graph must not be promoted.

## 8. Patch Budget

- Suggested project files: at most 29 including the two required Unity folder metas, all file metas and the eleven import metas
- New files: runtime Profile/presenter, Editor authoring/test, three assets and necessary folder/metas
- Modified files: one existing Prefab plus exactly eleven sequence-frame metas
- Over budget or wider ownership: stop with one exact boundary blocker

## 9. Required Behavior

1. Keep `C1ExactBattleSandboxGameBackground.prefab` as one authored background frame/body containing exactly one non-raycast UGUI background `Image`; add the authored local presenter reference without creating three active visual children.
2. Preserve the Prefab's authored RectTransform, dimensions, parent/sibling contract, color/material and manual layout adjustability. Runtime may only change the selected Sprite and local playback state.
3. Create three direct-reference Profiles:
   - interior static, Profile id `campaign.normal.lv1.theme.bone_aspect.c1`;
   - exterior static, candidate id `campaign.normal.lv1.theme.bone_aspect.c1.exterior`;
   - animated night, candidate id `campaign.normal.lv1.theme.bone_aspect.c1.night_animated`.
4. Profile mode is either static Sprite or ordered Sprite sequence. The presenter accepts a supplied Profile reference; it must not guess a Resources path, infer stage state, or choose a fallback theme by itself.
5. The animated night Profile preserves the accepted eleven timing positions, loops indefinitely at 10 FPS, and holds frame 1 for 5 seconds between loops. Pixel-identical source frames may be represented by repeated references to one imported Sprite while preserving all timing positions; source files remain untouched.
6. The presenter owns presentation-only `Apply`, `Clear`, `ResetPresentation` and authored-reference validation semantics. Disable/clear/reset cancels playback and clears stale sequence state idempotently; no per-loop allocation, material cloning, Instantiate/Destroy or second background owner.
7. Missing/invalid Profile, Sprite, sequence or mode fails closed with one exact diagnostic and no arbitrary art substitution. Unknown theme-id resolution is downstream Mainline responsibility.
8. All sequence frames become UI Sprite-compatible imports, preserve original 1672x941 PNGs, disable mipmaps, retain a mobile-appropriate max size/compression, and avoid an uncompressed RGBA32 requirement. Do not preload or activate all three variants through one always-live object graph.
9. Static interior remains the current safe mapping. Exterior/night are complete replaceable carriers but are not assigned to a campaign stage in this package.
10. The presenter never owns or mutates Stage progress, Battle, navigation, Save, Item or Enemy truth and never rewrites Scene layout.

## 10. Failure / Edge Cases

- Missing Profile or Sprite: fail closed, exact one-time diagnostic, no guessed fallback
- Empty/invalid sequence: do not start playback; retain/restore the authored safe background state
- Duplicate image bytes: repeated Profile reference is allowed; do not delete source files
- Reset/re-entry/disable/domain reload: no stale frame, duplicate presenter, ticking after disable, residual allocation or exception
- Mobile import: no mipmap-enabled full-screen UI sequence and no destructive resize
- Unity serialization collision: do not launch or author; wait for the exact current owner to release

## 11. Delivery Shape

- Profile: three authored replaceable Stage Theme background Profile assets
- Runtime: one presentation-only Profile type and one local background presenter
- Editor: one one-shot Prefab/Profile authoring tool plus focused serialized/import validation
- Scene: none
- Prefab: upgrade the existing single background Prefab only
- Adapter/Bridge: none in this package; Mainline binding is downstream
- Report: none; terminal receipt lists exact assets, API, Profile IDs, import changes and hashes

## 12. Verification

- [x] Read-only carrier/import/mobile survey before dispatch
- [ ] Scoped Runtime compile
- [ ] Scoped Editor compile
- [ ] One bounded Unity Prefab/Profile/import authoring pass after exclusive serialization release
- [ ] Serialized reference, missing-script, one-Image, raycast, RectTransform and no-Scene-dependency validation
- [ ] Profile ids/modes/direct Sprite refs/order/timing and duplicate-reference validation
- [ ] Import validation for all eleven frame metas and protected PNG-byte hashes
- [ ] Owned process/lock cleanup

Carrier PASS requires all authored carriers and validations above, protected boundaries unchanged, and no owned Unity residue. A Fresh Play and user visual PASS are deliberately downstream after Mainline context binding; do not claim them here.

## 13. Process Ownership

- Current state: `CODE_OFFLINE_DEV_ALLOWED / UNITY_HOLD`; the background package owns no Unity process or lock until Mainline explicitly releases the serialized Unity lease.
- Before authoring: recheck that the prior VFX package is terminal, the visible task is eligible, Unity/Tuanjie/ShaderCompiler/bee owners are absent, and Lockfile/EditorInstance are absent.
- The development task records its one authoring command, owned PID, log and bounded timeout.
- It must close/clean only its exact owned lifecycle and cannot declare terminal with owned residue.

## 14. Completion / Dependency Release

Maximum carrier terminal:

`STAGE_THEME_BACKGROUND_CARRIER_COMPLETE / TECHNICAL_PREREQUISITE_COMPLETE / RELEASED_TO_MAINLINE`

- PlayerVisibleDelivery: `NONE`
- MilestoneCompletionEvidence: `NO`

On carrier acceptance, release to Mainline Guard:

- exact Prefab path/hash;
- presenter API and component reference;
- three Profile paths/ids/hashes;
- exact eleven-frame import manifest and ordered timing references;
- fail-closed/reset semantics;
- proof of no Scene/host/StageConfig writes and clean Unity lifecycle.

Mainline then owns the minimal immutable `StageThemeProfileId` to Profile resolution and host/session/reset binding. Only after that binding and one normal-Editor real-path inspection may the chain claim:

`STAGE_THEME_BACKGROUND_USER_HANDTEST_READY / FAITHFUL_PARTIAL`

This package does not complete the wider P0 milestone.
