# V0.4-C1CompletionOnlyStageMemory01 Assignment

Status: `TECHNICAL_COMPLETE / PENDING_MAINLINE_GUARD_CLOSE`

## 1. Outcome

After a legitimate successful clear is integrated by a later Mainline package, that stage can remain cleared and replayable after restart. This package delivers only the completion-memory authority; it does not wire victory or WorldMap runtime consumers.

## 2. Product Context

- `CAMPAIGN_NORMAL_LV1`
- Campaign: `bone_aspect_chapter_1`
- Stages: `1-1` through `1-5`

## 3. Task Class / Owners

- Class: `CONTAINED_ONE_GUARD / PURE CODE-DATA / PARALLEL DISJOINT`
- Primary Guard / user-facing owner: Mainline Guard task `019f45d7-a33b-7630-a049-976481bb7b6d`
- Development Owner: this visible executor task
- State / Data Owner: `V04CampaignStageCompletionRepository`
- User Review Gate: `NO / NOT_REQUIRED` (behavior and schema are frozen; no visual or product choice remains)
- User Decision Point: `NONE`

## 4. Allowed Writes

1. `Docs/V0.4/V0.4-C1CompletionOnlyStageMemory01_Assignment.md`
2. `Assets/_Game/Scripts/TalismanBag/V04/WorldMap/V04CampaignStageCompletionRepository.cs`
3. `Assets/_Game/Scripts/TalismanBag/V04/WorldMap/V04CampaignStageCompletionRepository.cs.meta`
4. `Assets/_Game/Scripts/TalismanBag/Editor/V04/WorldMap/V04CampaignStageCompletionRepositoryTests.cs`
5. `Assets/_Game/Scripts/TalismanBag/Editor/V04/WorldMap/V04CampaignStageCompletionRepositoryTests.cs.meta`

## 5. Forbidden Writes / Scope

- No modification of existing product files.
- No Git, worktree, Unity launch, Scene, Prefab, material, import, ProjectSettings, or serialization.
- No runtime callback, StageClear listener, GameObject, Resources/Scene lookup, `DontDestroyOnLoad`, UI, or automatic write.
- Do not use V02 SaveData/MainTrial progression as V04 authority.
- No Item, material, reward roll, inventory, formation/loadout, battle, or mid-battle persistence.
- Runtime wiring from accepted victory to `MarkCleared`, and repository projection into `V04WorldMapProgressSnapshot`, belong to a later Mainline integration package.
- If an existing-file change or new helper becomes necessary, stop and return the exact need to the Primary Guard.

## 6. Required Sources

- `AGENTS.md`
- `Docs/LOCKED/ENGINEERING_PROCESS_V2_LOCK.md`
- `Docs/CURRENT/PROJECT_ENGINEERING_STATE_V2.md`
- This Assignment
- Existing V04 WorldMap contracts and directly relevant Editor test conventions, read-only

## 7. Patch Budget / Delivery Shape

- Five-file exact whitelist: one Assignment, one new Runtime C# file and meta, one new Editor test file and meta.
- Runtime: plain C# repository with PlayerPrefs production backend and the smallest injected storage seam.
- Editor: deterministic focused tests; no lifecycle or scene dependencies.
- Scene / Prefab / Adapter / Bridge / Report: `NONE`.

## 8. Required Behavior

1. New/default storage loads an empty completed-stage snapshot.
2. The only persisted gameplay state is a versioned payload containing normalized `completedStageIds` for `bone_aspect_chapter_1`, limited to stages `1-1` through `1-5`.
3. A stored set is valid only when it is a duplicate-free, known, contiguous prefix. Duplicate, unknown, skipped/gapped, corrupt JSON, or unsupported version loads safely as empty.
4. `MarkCleared` accepts only the next sequential stage. A duplicate already-cleared stage is an idempotent success and performs no write. A skipped or unknown stage is rejected and performs no write.
5. `Load` and `IsCleared` never write. Only a newly accepted `MarkCleared` persists.
6. `Reset` deletes only this package's storage key.
7. First-entry availability remains outside this repository.

## 9. Deterministic Acceptance

- [x] New/default: zero completed stages.
- [x] Mark `1-1` through `1-4`, reconstruct and load: exactly those four are cleared.
- [x] `1-5` remains false until accepted `MarkCleared("1-5")`.
- [x] Duplicate `MarkCleared` is idempotent; storage write count does not increase.
- [x] There is no defeat/quit or automatic callback API path that writes a clear.
- [x] Corrupt, unsupported-version, duplicate, unknown, and gapped payloads fail safely to empty.
- [x] Reset removes only this package key.
- [x] Static scan finds no prohibited persistence domain or runtime wiring.
- [x] Scoped Runtime and Editor compile and deterministic focused tests pass without launching Unity.
- [x] Diff and whitelist checks pass.

## 10. Process Ownership / Completion

- `UNITY_NOT_REQUIRED`; no process lease or Fresh Play.
- Expected terminal: `COMPLETION_ONLY_STAGE_MEMORY_TECHNICAL_COMPLETE`
- `PlayerVisibleDelivery=NONE`
- `MilestoneCompletionEvidence=NO`
- `FreshPlay=NOT_APPLICABLE`
- Return files, behavior, QA, and any exact blocker directly to the Mainline Guard.
