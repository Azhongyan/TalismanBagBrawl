# V0.4-C1ExactBattleSandboxItemDetailPopupStandalonePrefab01 Task Contract

## 0. Contract Identity

- Package: `V0.4-C1ExactBattleSandboxItemDetailPopupStandalonePrefab01`
- Product Context: `CAMPAIGN_NORMAL_LV1`
- Task Class: `CONTAINED_ONE_GUARD / VISUAL PREFAB TECHNICAL PREREQUISITE`
- Primary Guard: Animation / VFX / Shared Presentation Guard `019fa19d-1609-7f03-ad5b-5f12cba0fdc4`
- Development Owner: existing visible direct-local task `019fc1c2-e796-7790-92e1-c89cc921ff5c`
- User Review Gate: `YES`
- USER_ACCEPTED_SCOPE: `继续先复刻battlesandbox中道具弹窗详情的样式，先不接`
- PlayerVisibleDelivery: `NONE`
- MilestoneCompletionEvidence: `NO`
- Git / worktree / Notion: `FORBIDDEN`

## 1. Immutable Player Promise Card

### Player Promise

建立一个可独立打开检查、可复用的 authored Prefab；它在独立检查时一比一复现 BattleSandbox `PopupLayer/ItemDetailPanel` 已接受的视觉构成。本包只交付表现载体，不接入 Unified、真实 Item 选择、Reward、Battle 或 Mainline。

### Player Must See In Standalone Inspection

1. 与源一致的 PopupLayer/ItemDetailPanel 层级、兄弟顺序和 1080×1920 竖屏构图关系。
2. 与源一致的背景图、边框、颜色、透明度、材质、字体和文本层级。
3. 与源一致的 Header、Artwork、固定基础属性、详情滚动区、调试/辅助分区视觉表面、关闭按钮和滚动条构成。
4. 与源一致的 RectTransform、比例、ScrollRect、Viewport、Mask、Scrollbar、raycast 与必要 Canvas 行为。
5. 独立 Prefab 中没有 BattleSandbox 运行控制器、选中 Item 真相、样例数据投影或外部 Scene 引用。

### Must Change Live

`NONE`。本包刻意不做真实数据绑定、打开/关闭流程或正式运行接入。

### Persistence / Reset Expectation

Prefab 本身必须是确定、无 Scene 依赖的 authored carrier；重复执行 authoring 不产生重复子节点、重复组件、GUID 重建或外部引用残留。

### Required Path

Unity Project 中独立打开新 Prefab 进行 Prefab Stage 检查。不是 WorldMap→Unified 正式玩家路径。

### Explicit Non-Completion Cases

- 只修改或继续使用当前正式 `ItemDetailPanel.prefab`；
- 修改 BattleSandbox 源 Scene；
- 仅复制黑色底板或近似重画 UI；
- 丢失 Canvas/PopupLayer 父级关系后凭目测调整；
- 携带 `ItemSandboxDetailPanelView`、BuildSandbox bootstrap、样例/调试数据 feeder；
- 运行时创建最终层级、`GameObject.Find` 或 fallback；
- Prefab 已存在、编译通过就声称已接入 Unified；
- 点击真实 Item 能打开、真实数据绑定、正式关闭/Reset/Input 恢复或玩家里程碑完成。

## 2. Milestone Fidelity Coverage

| Capability | Authoritative source / owner | Concrete carrier owned now | Status before package | Required evidence |
|---|---|---|---|---|
| Exact popup carrier context | BattleSandbox authored Scene is visual source; Presentation owns the copy | new standalone root Canvas path ending at `PopupLayer` | MISSING | serialized parent/Canvas/RectTransform parity |
| Exact ItemDetailPanel hierarchy and sibling order | BattleSandbox authored Scene | nested `ItemDetailPanel` subtree | MISSING | canonical hierarchy comparison |
| Images/material/color/alpha/font/text hierarchy | BattleSandbox authored Scene and referenced assets | copied internal Graphics/TMP components and project-asset refs | MISSING | serialized property parity |
| Scroll/mask/viewport/scrollbar structure | BattleSandbox authored Scene | copied internal UI components | MISSING | serialized component/config parity |
| Close/tabs/sections visual/input surfaces | BattleSandbox authored Scene | copied local UI surfaces; no external runtime listener | MISSING | local-reference and listener audit |
| Real Item projection | Item System / future Item-owned presenter | NONE in this package | DOWNSTREAM | explicitly not claimed |
| Unified mounting and lifecycle | Mainline / future integration | NONE in this package | DOWNSTREAM | explicitly not claimed |

Any missing or proxy-only row owned by this package is `MILESTONE_FIDELITY_BLOCKED`. Downstream rows do not become complete through this package.

## 3. Technical Director Decision

### Chosen Unity Carrier

Create one new isolated Prefab:

`Assets/_Game/Prefabs/TalismanBag/Items/C1ExactBattleSandboxItemDetailPopupStandalone.prefab`

The Prefab uses an authored Canvas carrier copied from the source `BuildSandboxPreviewCanvas` settings needed for a 1080×1920 standalone inspection. It retains only the minimum parent chain required to preserve the source relationship:

`[standalone Canvas root] / MobileSafeAreaRoot / SafeAreaRoot / PopupLayer / ItemDetailPanel`

- The standalone Canvas root may be renamed to the new asset identity; this name-only normalization must be listed in the parity report.
- Canvas, CanvasScaler, GraphicRaycaster and the parent RectTransforms must preserve the source configuration required for the source layout.
- If the source safe-area helper has no external reference and affects only this retained subtree, it may remain as a presentation-layout component. If it has an external/Scene dependency, remove only that helper and preserve the source 1080×1920 computed carrier geometry; record the intentional difference.
- Under `PopupLayer`, retain only `ItemDetailPanel`.
- Exclude the unrelated source siblings `EnemyCombatFeedbackFloatingRoot` and `BuildSandboxItemInfoPanel_Runtime`.
- Preserve the exact internal child order of `ItemDetailPanel`.
- Preserve the local presentation component `ItemDetailPanelView` and its internal/project-asset references.
- Remove `ItemSandboxDetailPanelView` and every BattleSandbox/runtime adapter or external persistent UnityEvent listener.
- Keep purely local built-in UI behavior (ScrollRect, Scrollbar, Mask, Button/Selectable surfaces) when all references remain inside the Prefab.
- Close and tab surfaces remain authored visual/input controls, but do not integrate navigation or Item truth.
- Preserve static chrome headings/labels. Clear selected-item-specific artwork/value/status strings and all sample/debug projected values. Debug/auxiliary visual sections may remain only as inert source-faithful surfaces, initially in the same active/inactive state as the source and with no feeder/state owner.
- Do not add a new runtime script. Future data enters through the existing Item-owned detail Presenter seam, outside this package.

### Reuse / New Work

- Reuse existing sprites, materials, TMP fonts and `ItemDetailVisualTheme.asset` by reference.
- Reuse `ItemDetailPanelView.cs` without modification.
- Create one new Editor-only one-shot authoring/validation script.
- Do not copy source PNGs/materials/fonts or create duplicate runtime systems.

### Scene / Prefab Serialization Impact

- New isolated Prefab only.
- Source BattleSandbox Scene save count must be zero and its hash must remain exact.
- Existing formal `ItemDetailPanel.prefab`, Unified Shell/Scene and Item carriers remain byte-identical.

## 4. Read-Only Source Attribution

Authoritative source Scene:

`Assets/_Game/Scenes/Scene_TalismanBag_V04_BattleSandboxPreview.unity`

Unique source path:

`BuildSandboxPreviewCanvas/MobileSafeAreaRoot/SafeAreaRoot/PopupLayer/ItemDetailPanel`

Confirmed source facts:

- `PopupLayer` and `ItemDetailPanel` are Scene-authored (`m_PrefabAsset: 0`), not instances of the existing formal prefab.
- `PopupLayer` is a stretched child of `SafeAreaRoot`, offset `{x:0,y:348}`, and carries transparent non-raycast Image plus nested Canvas.
- `ItemDetailPanel` is centered at `{x:0,y:-192}`, size `{1080,1920}`, scale approximately `0.7999976`, with five direct children in source order.
- `ItemDetailPanelView` is the reusable presentation view.
- `ItemSandboxDetailPanelView` is a BattleSandbox adapter and must not be promoted.
- Existing `Assets/_Game/Prefabs/TalismanBag/Items/ItemDetailPanel.prefab` is already mounted by Unified and is protected evidence, not this package's output.

## 5. Exact Allowed Writes

1. `Docs/V0.4/V0.4-C1ExactBattleSandboxItemDetailPopupStandalonePrefab01_TaskContract.md` — Guard-owned contract, already frozen at dispatch.
2. `Assets/_Game/Prefabs/TalismanBag/Items/C1ExactBattleSandboxItemDetailPopupStandalone.prefab` — new.
3. `Assets/_Game/Prefabs/TalismanBag/Items/C1ExactBattleSandboxItemDetailPopupStandalone.prefab.meta` — new, Unity-owned GUID.
4. `Assets/_Game/Scripts/TalismanBag/Editor/Presentation/Items/C1ExactBattleSandboxItemDetailPopupStandalonePrefabAuthoring.cs` — new Editor-only authoring/validation.
5. `Assets/_Game/Scripts/TalismanBag/Editor/Presentation/Items/C1ExactBattleSandboxItemDetailPopupStandalonePrefabAuthoring.cs.meta` — new.
6. `Docs/V0.4/Reports/C1ExactBattleSandboxItemDetailPopupStandalonePrefabParity.md` — new concise parity report.

No other write is allowed. If any new folder `.meta` is required, stop and report because both target folders exist at dispatch.

## 6. Forbidden Writes / Protected Baselines

Do not modify or save:

| Protected carrier | Dispatch SHA-256 |
|---|---|
| `Assets/_Game/Scenes/Scene_TalismanBag_V04_BattleSandboxPreview.unity` | `1D7A367B803F9A7477E261A90E05D58FBAB2F9FAA1F3464A86806B8A9FAF9FCA` |
| `Assets/_Game/Prefabs/TalismanBag/Items/ItemDetailPanel.prefab` | `2A6924DA0823B74E2639B593926D8C2F8422101B01EDBE4073AF33CD424E027C` |
| `Assets/_Game/Prefabs/TalismanBag/UnifiedBattle/UnifiedBattlePageShell.prefab` | `BA8A421C1CE0260F076106BCE3E540C4E0475BC365A9153F3E5E7F90B079A586` |
| `Assets/_Game/Scenes/Scene_TalismanBag_V04_UnifiedBattlePageShell.unity` | `D432B6E492EDF0CB0EEA8B62E66E27A6EE3F7773C8BBCC8AD886A86A1A0BDD63` |
| `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/Presentation/ExactItemDetail/C1ExactBattleSandboxItemDetailPresenter.cs` | `0C7DE4683BB97DB555509C0C55C48901C960DFB718784F84B026EF7E44AAEA64` |
| `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1ExactBattleSandboxItemDetailPhaseBAuthoring.cs` | `EE637E23526A0F1E492536199568C3AFBBE4326C9DD7D9C72E4E6DB4860F3431` |
| `Assets/_Game/Prefabs/TalismanBag/Items/C1ExactBattleSandboxItemCard.prefab` | `C4D449A1C298BF5B2A91072A4BEA2A67AFD5E1B1D713A4D769CB843D059DDD8C` |
| `Assets/_Game/Prefabs/TalismanBag/Items/C1ExactBattleSandboxItemBoard.prefab` | `46949A5FECAAF94960D2F2A31C6715F0C1297CDAEAD1C71FF749959762FBB7A8` |
| `Assets/_Game/Prefabs/TalismanBag/Items/C1ExactBattleSandboxItemTray.prefab` | `6FA5F995433B4433B201613CA7FBA939779F6664E44E19CC02ACA060E4F4D552` |

Also forbidden:

- every Scene save or new Scene;
- existing Prefab rewrites;
- `ItemDetailPanelView.cs`, visual theme, fonts, sprites, materials and import settings;
- Host/Presenter/Mainline/Item/Battle/Reward/Save/Navigation data or code;
- runtime bootstrap, fallback hierarchy, `RuntimeInitializeOnLoad`, runtime `GameObject.Find`;
- sample/debug projection, fake Item data or selected Item owner;
- ProjectSettings, BuildSettings, AGENTS, LOCKED, Queue, Notion, Git/worktree.

## 7. Authoring Requirements

The Editor-only tool must:

1. Open/read only the exact source Scene and fail on zero/duplicate source path matches.
2. Duplicate only the minimum Canvas→Popup→Panel chain defined in section 3.
3. Delete excluded siblings and components only from the in-memory duplicate.
4. Strip Scene/object references outside the retained Prefab subtree; project asset references are allowed.
5. Clear runtime/sample data without changing the UI styling components.
6. Save only the new Prefab path; never call `SaveScene` on the source.
7. Be idempotent: rerun updates the same new Prefab without duplicate roots/components and without changing its GUID.
8. Validate no missing scripts, no external Scene refs, no runtime owner, no Sandbox adapter, and no forbidden child roots.
9. Emit one concise PASS/FAIL marker and write the parity report only after all required comparisons pass.

## 8. Source-to-Prefab Parity Report

The report must compare source and output for:

- retained hierarchy paths and exact sibling order;
- RectTransform anchors/pivot/anchoredPosition/sizeDelta/localScale;
- component type/order and enabled/active state;
- Canvas/CanvasScaler/GraphicRaycaster and PopupLayer Canvas/Image behavior;
- Image sprite/material/type/color/alpha/raycast/maskable;
- TMP font/material/font size/alignment/color/gradient/overflow;
- ScrollRect axes, viewport/content/scrollbar refs, movement/inertia/sensitivity;
- Mask/RectMask2D and Scrollbar/Selectable configuration;
- close button and section/tab visual/input surfaces;
- all intentional differences: new asset/root identity, excluded unrelated siblings, removed Sandbox adapter/runtime listeners, cleared selected-item/sample/debug values, and any safe-area-helper exclusion.

Aggregate “looks similar” is not evidence. Every intentional difference must be explicit and limited to the frozen exclusions.

## 9. QA / Evidence

Required:

1. Current-source scoped Editor compile; Runtime compile only if the Editor tool introduces a shared dependency (otherwise state `RUNTIME_NOT_CHANGED`).
2. One bounded owned Unity authoring/import pass after an atomic CLOSED_CLEAN check.
3. Serialized validation of the new Prefab and parity report.
4. One standalone Prefab Stage visual inspection in the same owned Editor lifecycle when technically accessible.
5. Recheck every protected hash in section 6.
6. Final owned Unity/Tuanjie/ShaderCompiler/bee children = 0; `UnityLockfile` and `EditorInstance.json` absent.

Not required and prohibited as acceptance inflation:

- Fresh Play;
- Unified route handtest;
- APK build;
- runtime sample fixture;
- screenshot harness, repeated Unity retry, bulk CSV or multi-Guard acceptance.

If the single normal-Editor window cannot be interacted with, do not start a second Unity run. Return the exact observation gap separately; do not claim visual inspection PASS.

## 10. Process Ownership

- Record exact command, owned PID, log path and bounded timeout before Unity launch.
- Never start while another Unity serialization owner/process/lock exists.
- Never stop or clear an external process/lock.
- A package cannot return terminal while its owned process or lifecycle artifacts remain.

## 11. Stop Conditions

Stop with one exact blocker instead of expanding scope if:

- preserving the source appearance requires modifying the source Scene, existing formal Prefab, Unified or Item presenter;
- the retained clone contains an unavoidable external Scene reference or runtime truth owner;
- the source path is missing/duplicated;
- another Unity serialization owner appears before authoring;
- any protected baseline drifts for reasons not owned by this package;
- more than the six allowed files are required.

## 12. Completion Cap

Strongest allowed terminal:

`TECHNICAL_PREREQUISITE_COMPLETE / STANDALONE_ITEM_DETAIL_POPUP_PREFAB_COMPLETE`

- PlayerVisibleDelivery: `NONE`
- MilestoneCompletionEvidence: `NO`
- User visual acceptance: `NOT_APPLICABLE_YET`

Completion requires exact allowed writes only, authored Prefab and parity validation, protected baselines unchanged, no owned process residue, and no unresolved package-owned capability row. It does not release or imply formal integration.
