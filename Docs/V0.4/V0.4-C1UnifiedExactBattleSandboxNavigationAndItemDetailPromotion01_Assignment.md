# V0.4-C1UnifiedExactBattleSandboxNavigationAndItemDetailPromotion01

Status: REV11G / GUARDED_OWNED_AUTHORING_AND_REAL_FRESH_PLAY / DISPATCH_READY / USER_HANDTEST_FAIL  
Classification: COMPLEX_GUARDED_ONCE / PLAYER_VISIBLE EXACT PREFAB PROMOTION  
Primary Guard: Mainline Flow Guard  
Product Context: CAMPAIGN_NORMAL_LV1  
User Review Gate: YES / USER_ACCEPTED_SCOPE  
PlayerVisibleDelivery: PARTIAL  
MilestoneCompletionEvidence: NO  
Git / worktree: FORBIDDEN

## REV11G Guard QA reauthorization after REV11F close

Guard close verdict for REV11F:
`ACCEPTED / INTERNAL_CORRECTION_READY`.

Accepted evidence:

- authoring and tests match the frozen two-file whitelist;
- detached Prefab creation and final validation no longer consume
  `viewport.rect.size` or call runtime layout refresh;
- authored frame geometry is centered `(1080,1920)` with unit scale;
- runtime Presenter hash remains
  `0C7DE4683BB97DB555509C0C55C48901C960DFB718784F84B026EF7E44AAEA64`
  and still rejects zero/NaN live viewports;
- Runtime/Editor compile and focused checks passed;
- Shell remains exact repair intake
  `AEA7310160E3F080EE973C6DBB3CDC32B99C112AD1D55F12324D0B0100A8E1D8`;
- no product serialization occurred and the project process/lock gate is clear.

This section is the explicit new Guard QA decision required by REV11F. It is based
on the user's still-current instruction `自己fresh` and authorizes exactly one new,
task-owned normal Unity Editor lifecycle. It is not an automatic retry of the
failed REV11E lifecycle.

Atomic QA sequence:

1. Recheck immediately before launch that Unity/Tuanjie/ShaderCompiler/bee project
   owners are zero and both project lock files are absent. Otherwise return
   `EXTERNAL_UNITY_OWNER` without polling, killing or retrying.
2. Start one normal owned Editor lifecycle. Use the existing public REV11 authoring
   menu/`ApplyFromMenu` entry in that Editor. Do not select the command-line entry
   that necessarily exits the Editor before the real-path attempt.
3. Require `SINGLE_FORMAL_PATH_AUTHORING_AND_VALIDATION_PASS`, a changed Shell hash,
   one authored `ItemDetailPresentationFrame`, one serialized Presenter frame
   reference, zero Missing Script and zero forbidden owner before Play.
4. Continue in the same owned Editor lifecycle. Attempt only the existing real
   `WorldMap -> 1-1 -> Unified` route through visible authored controls and formal
   launch/navigation owners. External UI input automation is allowed only to drive
   those real visible controls; no fake/direct launch context, dev injection, new
   harness, hidden control or runtime fallback may be added.
5. If available input/window automation cannot reliably reach or operate the real
   controls, return `FORMAL_FRESH_PLAY_INPUT_BLOCKER` with the exact missing window,
   control or input capability. Do not infer a Fresh Play PASS from logs.
6. If reachable, require no Item-detail partial/invalid, Shell-slot, Item-mount,
   Missing Script or forbidden-owner error. Click the real I001, directly inspect
   readable identity/artwork/combat/placement-lighting content, close with the
   authored close control and verify Item input responds again.
7. Stop at the first objective failure. No second launch, menu rerun or Fresh Play
   repeat. Exit the owned Editor and prove process/lock cleanup.

No source edit is expected or authorized in REV11G. The only product write remains
the exact Shell serialization already frozen by REV11E/F. Unified Scene and all
protected source/carrier Prefabs remain byte-protected. No Git/worktree.

Allowed terminal statuses:

- `REV11G_ITEM_DETAIL_REAL_PATH_OBSERVED / FAITHFUL_PARTIAL`;
- `REV11G_AUTHORING_PASS / FORMAL_FRESH_PLAY_INPUT_BLOCKER / FAITHFUL_PARTIAL`;
- `EXTERNAL_UNITY_OWNER`;
- exact first objective failure with `USER_HANDTEST_FAIL`.

Even a successful real Item-detail observation is not full P0 completion;
`MilestoneCompletionEvidence=NO` and all unrelated promise rows remain pending.

## REV11F detached-Prefab viewport authoring correction

Latest objective failure:

`REV11E_ITEM_DETAIL_VIEWPORT_INVALID (0.00, 0.00)` occurred during the single
owned REV11E authoring lifecycle before any product serialization or Fresh Play.
`PrefabUtility.LoadPrefabContents` correctly loaded a detached Shell Prefab whose
full-stretch `ItemDetailPopupSlot.rect.size` is zero until it is instantiated in a
real Canvas/layout context.

Milestone state remains:
`USER_HANDTEST_FAIL / ITEM_DETAIL_BLACK_SCREEN / MILESTONE_FIDELITY_BLOCKED`.

First missing layer and ownership:

- Mainline Editor authoring/validation conflates authored reference geometry with
  runtime-resolved viewport scale.
- `CreateItemDetailPresentationFrame` incorrectly requires a positive live viewport
  while authoring detached Prefab contents.
- `IsReferenceFrameGeometryValid` repeats the same invalid assumption during
  post-save detached-Prefab validation, so removing only the first check is not a
  complete correction.
- Runtime Presenter ownership is already correct: it resolves the uniform scale
  from a real positive viewport and fails closed on zero/NaN/Infinity. That runtime
  contract must not be weakened.

Exact write whitelist:

- `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1UnifiedSingleFormalPathAuthoring.cs`
- `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1FormalObtainRebuildBattleLoopTests.cs`
  only for the smallest focused deterministic/static coverage.

`C1ExactBattleSandboxItemDetailPresenter.cs`, the Shell/Scene and every Prefab are
read-only during this correction.

Frozen correction:

1. Separate authored reference-frame validation from runtime viewport resolution.
2. Detached-Prefab authoring creates the unique centered `(1080,1920)` frame with
   anchors/pivot centered, zero anchored position, identity rotation and authored
   local scale `Vector3.one`. It must not call `TryCalculateUniformReferenceFit`
   using `viewport.rect.size`.
3. Detached-Prefab final validation checks only the authored frame geometry,
   hierarchy, source Panel transform, source link, non-Graphic/non-raycast state and
   serialized Presenter reference. It must not require a resolved live scale.
4. Remove the authoring-time `presenter.RefreshPresentationLayout()` requirement.
   The saved Prefab remains an unresolved authored reference frame until a real
   viewport exists.
5. Runtime `TryCalculateUniformReferenceFit` and `RefreshPresentationLayout` remain
   unchanged: real positive viewports resolve a finite uniform scale; zero/invalid
   runtime viewport keeps the popup closed and non-raycast.
6. Preserve the exact `AEA731...E1D8` repairable-state gate, the three-state
   admission model, source Panel geometry `(1080,1920)/(0,-192)/0.8`, PopupSlot-last
   rule, font fail-closed behavior, zero Missing Script and strict forbidden-owner
   validation.
7. Focused coverage must prove:
   - a detached zero-size viewport no longer blocks authored frame creation or
     serialized-geometry validation;
   - authored frame scale is exactly one;
   - runtime fit still rejects zero/NaN and resolves the existing 1920x1080,
     1080x1920 and 720x1280 cases;
   - no runtime fallback hierarchy or source Prefab/Scene mutation path is added.

QA boundary:

- scoped Runtime/Editor compile plus focused deterministic/static checks only;
- do not launch or control Unity, invoke the authoring menu, serialize any asset,
  run Fresh Play or perform Git/worktree operations;
- preserve all current product/protected hashes and return exact changed hashes.

Expected development terminal:
`REV11F_DETACHED_PREFAB_VIEWPORT_CORRECTION_COMPILE_READY`.

The REV11E owned Unity lifecycle is consumed. A further authoring/Fresh Play
lifecycle is not authorized by this correction and must return to Mainline Guard
for an explicit new QA decision after code close. No readiness claim is allowed.

## REV11E ItemDetail black-screen presentation/mount correction

Latest authoritative user evidence:
`桥接页面点道具详情是黑屏的`.

Milestone state:
`USER_HANDTEST_FAIL / ITEM_DETAIL_BLACK_SCREEN / MILESTONE_FIDELITY_BLOCKED`.

Producer classification:
`READABILITY / P0`. Opening an opaque plate without readable authoritative Item
content is equivalent to the detail capability being absent.

Technical Director read-only attribution is frozen:

1. The click/selection path reaches the modal: the Presenter enables the
   `ItemDetailPopupSlot` CanvasGroup, while the nested `ItemDetailPanel` root Image
   is an almost opaque dark plate (`rgba 0.115,0.079,0.052,0.992`).
2. The accepted BattleSandbox source carrier is direct-authored under `PopupLayer`:
   - source Scene SHA-256
     `1D7A367B803F9A7477E261A90E05D58FBAB2F9FAA1F3464A86806B8A9FAF9FCA`;
   - source Panel anchors `(0.5,0.5)..(0.5,0.5)`;
   - anchored position `(0,-192)`;
   - size `(1080,1920)`;
   - local scale approximately `(0.8,0.8,0.8)`;
   - source parent `PopupLayer` owns its own Canvas and source coordinate/sorting
     context.
3. REV11D mounted the exact body directly under a full-screen formal slot. Current
   Shell SHA-256 is
   `AEA7310160E3F080EE973C6DBB3CDC32B99C112AD1D55F12324D0B0100A8E1D8`;
   the slot stretches `(0,0)..(1,1)` and the nested Panel inherits the Prefab root
   `(720,1280)`, scale `1`. This does not preserve the accepted source presentation
   frame and exposes the dark backing while content can lie outside the readable
   viewport/order.
4. REV11D validated object identity and serialization only. It did not validate an
   onscreen content frame, aspect mapping, popup sibling order or readable runtime
   body.
5. The secondary black-screen path is real even if the user log does not prove it
   fired: `ItemDetailPanelView.Bind()` hides and returns when legacy-font resolution
   fails, but the Presenter then unconditionally calls `SetVisible(true)` and opens
   the parent CanvasGroup. That contradiction can expose only the dark backing.

Authoritative ownership and carrier:

- Item/detail projection remains the sole selected-instance data truth.
- Existing `C1ExactBattleSandboxItemDetailPresenter` owns formal open/close and the
  deterministic presentation-frame mapping only; it must not infer Item values.
- Existing exact `ItemDetailPanel.prefab` remains the visible body and source link.
- `UnifiedBattlePageShell.prefab` owns one authored
  `ItemDetailPresentationFrame` under the existing full-screen PopupSlot. The frame
  is serialized by the existing REV11 authoring entry; no runtime-created final UI.
- Unified Scene remains composition-only and byte-protected.

REV11E exact write whitelist:

- `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/Presentation/ExactItemDetail/C1ExactBattleSandboxItemDetailPresenter.cs`
- `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1UnifiedSingleFormalPathAuthoring.cs`
- `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1FormalObtainRebuildBattleLoopTests.cs` only for the smallest focused deterministic/static coverage.

Source BattleSandbox Scene, source `ItemDetailPanel.prefab`, Unified Scene and all
other runtime/Prefab/Item/Battle/VFX files are read-only.

Frozen correction:

1. Add exactly one authored `ItemDetailPresentationFrame` RectTransform beneath
   the existing `ItemDetailPopupSlot`, and place the unique exact nested
   `ItemDetailPanel` beneath that frame. Do not create a second Panel or copy the
   BattleSandbox controller/runtime graph.
2. The frame is a deterministic aspect/layout adapter, not state truth:
   - centered reference frame `(1080,1920)`;
   - at open and on parent-size change, uniformly fit that reference frame wholly
     inside the current PopupSlot viewport without cropping or stretching;
   - finite positive scale only; zero/invalid viewport fails closed;
   - no raycast Graphic or opaque Image on the frame.
3. Preserve the accepted source Panel transform inside the reference frame:
   anchors and pivot centered, anchored position `(0,-192)`, size `(1080,1920)`,
   local scale `(0.8,0.8,0.8)`, identity rotation. Keep the exact Prefab source
   body, images, content hierarchy, ScrollRects and close button unchanged.
4. Keep the PopupSlot full-screen only as the CanvasGroup/input boundary. It must be
   the last formal Shell sibling while open, the frame must be its sole visible
   body carrier, and the unique Panel must be under the frame. Do not add a dark
   fallback plate, second Canvas truth or runtime hierarchy fallback.
5. Extend the existing Presenter with serialized frame/panel references and a
   deterministic layout refresh. It may run on bind/open and
   `OnRectTransformDimensionsChange`, but it owns no selected Item data and creates
   no GameObject.
6. Close the font contradiction using existing public ItemDetail facts only:
   after `panelView.Bind(...)`, require successful legacy-font resolution (the
   existing non-null `CachedLegacyTextFont`/resolution state) before calling
   `panelView.SetVisible(true)` or opening the CanvasGroup. On failure, keep the
   Panel closed, keep alpha/interactable/blocksRaycasts disabled, clear the rendered
   signature so a later valid click can retry, and retain the existing precise font
   diagnostic. Do not fabricate a font or show the dark plate.
7. Current Shell admission is explicit and fail closed:
   - `EXACT_BLACK_SCREEN_REPAIRABLE`: exact current Shell hash above, one PopupSlot,
     one Presenter, one exact Panel source, no frame and exact current bad mount;
   - `FINAL_VISIBLE_MOUNT`: one exact authored frame and all final geometry/order/
     font-fail-closed invariants;
   - `INVALID_CONFLICT`: any duplicate, unexpected frame, mismatched source,
     unrelated geometry drift or partial state; reject before save.
8. Keep REV11B `guajiann` omission, REV11D zero-Missing-Script state, strict
   forbidden-owner validation, current Host bindings and every unrelated Shell
   carrier/override unchanged.

Validation and acceptance before another user run:

- exact source Scene hash and raw serialized source Panel/PopupLayer values above
  are checked read-only before authoring;
- current repairable Shell hash and bad-mount identity are exact;
- final serialized Shell contains exactly one PopupSlot/frame/Panel/Presenter,
  source Prefab link unchanged, PopupSlot last, frame non-Graphic/non-raycast,
  Panel source geometry exact and closed CanvasGroup non-intercepting;
- deterministic reference viewports at minimum `(1920,1080)`, `(1080,1920)` and
  `(720,1280)` produce a finite positive fit and keep the full source frame within
  the viewport;
- Presenter font failure leaves both Panel and CanvasGroup closed and retryable;
  success path shows authoritative projection and enables input only after bind;
- Runtime and Editor scoped compile plus focused deterministic/static tests;
- one bounded authoring/import pass only after Mainline Guard close; no user rerun
  before that close;
- no Scene save, source Prefab write, Apply/Unpack, fallback UI, Git or unrelated
  carrier change.

Expected development terminal:
`REV11E_ITEM_DETAIL_VISIBLE_MOUNT_USER_MENU_READY`.

Strongest result before normal-Editor user observation remains
`FAITHFUL_PARTIAL / MilestoneCompletionEvidence=NO`. Compile, component counts or a
saved Prefab cannot prove the popup is readable.

## REV11E QA continuation addendum — one new owned Unity lifecycle

Latest authoritative user evidence:
`UNIFIED_FORMAL_ITEM_DETAIL_PARTIAL_OR_INVALID` after REV11E runtime code was
imported while the Shell remained the exact pre-REV11E hash
`AEA7310160E3F080EE973C6DBB3CDC32B99C112AD1D55F12324D0B0100A8E1D8`.
The user explicitly instructed Codex to perform the Fresh Play attempt: `自己fresh`.

This is a new, separate REV11E QA authorization. It does not reuse or reinterpret
the already-consumed REV11D lifecycle below. It authorizes exactly one atomic,
task-owned Unity Editor lifecycle after the immediate lease gate passes.

Atomic lease and ownership gate:

- immediately before launch, Unity/Tuanjie/ShaderCompiler/bee project owners must
  be zero, `Temp/UnityLockfile` absent and `Library/EditorInstance.json` absent;
- do not start, stop, attach to or automate an external/user-owned Editor;
- if any external owner, lock or unsaved user lifecycle is present, return
  `EXTERNAL_UNITY_OWNER` without polling, killing or retrying;
- the task owns every process it starts and must close only those owned processes,
  then prove the process family and project locks are clean.

Authoring phase inside that one lifecycle:

1. Run only the existing REV11 single-path authoring entry equivalent to menu
   `TalismanBag/V0.4/Unified Battle/Apply REV11 Single Formal Path Once` against
   the exact repairable Shell state above.
2. Require marker `SINGLE_FORMAL_PATH_AUTHORING_AND_VALIDATION_PASS`, a new Shell
   hash, exactly one authored `ItemDetailPresentationFrame`, one serialized
   Presenter `presentationFrame` reference, zero Missing Script and zero forbidden
   Sandbox runtime owner.
3. Preserve the Unified Scene, source BattleSandbox Scene, source
   `ItemDetailPanel.prefab`, PrepareSurface, Board/Tray/Card, Navigation,
   GameBackground, FormalBattlePresentation and every other protected hash.
4. The only product serialization allowed is the already-authorized exact Shell
   save. No Scene save, source-Prefab Apply/Unpack, broad cleanup or second pass.

Real Fresh Play attempt, only after authoring acceptance:

1. Use only the existing real `WorldMap -> 1-1 -> Unified` route and current formal
   navigation/launch owners. Do not use BattleSandbox/dev injection, a fake or
   directly constructed `BattleLaunchContext`, a new harness, hidden controls or a
   summary substitute.
2. If available automation cannot reliably drive that real route, return exact
   terminal `FORMAL_FRESH_PLAY_INPUT_BLOCKER` with the missing tooling/input seam.
   The successful authoring evidence remains valid, but no runtime admission or
   visual claim may be made.
3. If the real route is reachable, stop on the first occurrence of any
   `UNIFIED_FORMAL_ITEM_DETAIL_PARTIAL_OR_INVALID`, Shell-slot, Item-mount,
   forbidden-owner, Missing Script or other objective admission error.
4. On successful admission, click the real I001 carrier, observe readable
   authoritative identity/artwork/combat and placement-lighting content, close via
   the authored close control and verify closed alpha/interactable/blocksRaycasts
   state restores Item input. Do not infer visibility from logs or references.
5. No automatic repeat. One failure or inability to drive the route ends the
   lifecycle and is reported exactly.

Allowed evidence/status:

- authoring success plus unreachable real-route automation:
  `REV11E_AUTHORING_PASS / FORMAL_FRESH_PLAY_INPUT_BLOCKER / FAITHFUL_PARTIAL`;
- real-route admission and visible detail/close/input observation:
  `REV11E_ITEM_DETAIL_REAL_PATH_OBSERVED / FAITHFUL_PARTIAL`;
- any objective failure: exact first failure fingerprint and
  `USER_HANDTEST_FAIL / MILESTONE_FIDELITY_BLOCKED`.

None of these statuses proves the full P0, ItemLiving quality or the complete
0/1/2 battle/reward loop. `MilestoneCompletionEvidence` remains `NO`.

## REV11D QA authorization addendum — one owned Unity lifecycle

Latest explicit user authorization:
`你放开 unity的 frsh play 让codex帮我验收吧`.

This supersedes the earlier REV11D development ban on Unity only for one bounded,
task-owned Unity lifecycle after the code correction and offline compile/static gate
are ready. It does not authorize a second lifecycle, retry loop, external Editor
control, new runtime harness, dev injection or broader product serialization.

Read-only QA-carrier survey is frozen:

- `C1UnifiedSingleFormalPathAuthoring` provides the existing exact authoring and
  serialized validation entry plus marker
  `SINGLE_FORMAL_PATH_AUTHORING_AND_VALIDATION_PASS`.
- Existing automated Fresh Play tools
  (`C1EncounterOwnershipFreshPlayVerifier`,
  `V04Chapter1ContinuousHandtestMenu`,
  `C1ThreeSceneJourneyHandtestMenu`) target B-Line/BattleSandbox dev runtime and/or
  inject DontSave dev owners. They are forbidden for this formal Unified proof.
- No existing reliable Unified-specific automated entry was found that publishes a
  valid WorldMap 1-1 `BattleLaunchContext` and enters the formal page without a new
  harness, fake context or dev injection.

Atomic lease gate:

1. Immediately before launch require Unity/Tuanjie/ShaderCompiler/bee project owner
   count zero, `Temp/UnityLockfile` absent and `Library/EditorInstance.json` absent.
2. If any external Editor/serialization owner, lock, unsaved user lifecycle or
   ambiguous ownership is present, return `EXTERNAL_UNITY_OWNER` immediately. Do
   not poll, kill, steal, retry or touch the external process.

One owned lifecycle may perform, in order:

1. Start the configured project in one normal owned Unity Editor process; no second
   process or batch/Fresh-Play loop.
2. Run the existing REV11 authoring entry exactly once against the admitted REV11D
   state and require
   `SINGLE_FORMAL_PATH_AUTHORING_AND_VALIDATION_PASS`.
3. Allow normal import/compile to settle and require zero compile errors, zero
   Missing Script, zero REV11* failure, zero forbidden Sandbox owner and exact
   protected Scene/source-Prefab hashes.
4. Only if the owned visible Editor can drive the existing real WorldMap 1-1
   Challenge path without adding code, using a dev menu, injecting objects,
   publishing a fake context or creating a hidden harness, perform one Fresh Play.
   The valid context must originate from the existing WorldMap formal path.
5. For that one Fresh Play, evidence is limited to runtime admission: Unified Host
   accepts the valid context and the repaired Shell loads without `REV11*`, Missing
   Script, `UNIFIED_FORMAL_HOST_SHELL_SLOT_MISSING`,
   `UNIFIED_FORMAL_ITEM_MOUNT_BINDING_INVALID` or forbidden Sandbox-owner errors.
6. Stop at the first objective failure and return the complete first exception/log
   fingerprint. Do not retry. Exit the owned Editor cleanly and verify owned
   Unity/process/lock residue is zero.

Exact Fresh Play limitation:

- If the existing real WorldMap entry cannot be driven reliably inside that owned
  lifecycle without new automation/harness work, do not fabricate the evidence.
  Complete only the authoring/import/runtime checks genuinely available and return
  `FORMAL_FRESH_PLAY_ENTRY_MISSING` with the exact limitation.
- Do not use the B-Line/BattleSandbox Fresh Play verifier as a substitute and do not
  add an Editor/runtime QA orchestrator under REV11D.

Serialization boundary:

- The only product serialization permitted is the already-authorized exact Shell
  save performed by the existing REV11 authoring entry.
- No Unified Scene save, source Prefab apply/unpack, broad cleanup, VFX/Item/Battle/
  Enemy/Reward/Save change or user-layout reset.

Evidence cap:

- Authoring/import success plus valid-context Fresh Play admission may reach only
  `REV11D_UNIFIED_RUNTIME_ADMISSION_VALIDATED / FAITHFUL_PARTIAL` with
  `PlayerVisibleDelivery=PARTIAL` and `MilestoneCompletionEvidence=NO`.
- If only authoring/import can be executed, report that narrower evidence and the
  exact Fresh Play limitation; do not promote it to runtime admission.
- Neither result proves visual ItemDetail readability, click/drag feel, exact
  Board/Tray parity, ItemLiving VFX quality, 0/1/2 outcome branches or full
  `USER_HANDTEST_READY`.

## REV11D ItemDetail partial-state repair — supersedes REV11C AddedComponent API assumptions

Latest user one-shot menu evidence:
`REV11C_PARTIAL_ITEM_DETAIL_ADDED_OVERRIDE_INVALID count=0`.

Technical Director fidelity verdict:
`PARTIALLY_FOLLOWED / IMPLEMENTATION_METHOD_DRIFT`.

The architecture remains correct: retain the exact `ItemDetailPanel`, use the
Presenter-owned authored close button, forbid root-wide cleanup and target only the
one missing relay placeholder. REV11C drifted by assuming a missing-script nested
Prefab override is enumerable through
`PrefabUtility.GetAddedComponents(panel.gameObject)`. Current Unity returns zero for
that stripped missing component even though the raw Prefab YAML contains the exact
added-component override.

Current read-only state is frozen:

- Shell SHA-256 remains
  `941B5C5E06F57247CDF07971D0B9B1FF131E84A82AA0905FE1FC40FEF2DB1109`;
  the failed pass saved no mutation.
- Raw Shell YAML contains exactly one `m_Script: {fileID: 0}`, exactly one
  `addedObject: {fileID: 7099084332157108925}`, exactly one matching nested
  `ItemDetailPanel` `m_AddedComponents` row and the exact frozen
  `presenter` / `panelView` payload.
- Loaded hierarchy already proves exactly one missing script overall and exactly
  one on the unique exact `ItemDetailPanel`.
- The exception occurs before
  `RemoveMonoBehavioursWithMissingScript` and before `SaveAsPrefabAsset`.

REV11D exact write whitelist:

- `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1UnifiedSingleFormalPathAuthoring.cs`

Frozen correction:

1. Remove only the invalid `PrefabUtility.GetAddedComponents(panel.gameObject)`
   precondition and matching postcondition for this missing-script placeholder.
   Do not replace them with another Unity API assumption about a stripped
   `MonoBehaviour`.
2. Keep the exact frozen Shell hash and exact raw-YAML identity/payload proof from
   REV11C unchanged as the serialized-override authority for the repairable partial.
3. Keep loaded-object proof before mutation:
   - exactly one final-composition ItemDetail Presenter and one exact
     `ItemDetailPanelView`;
   - Presenter/panel/authored close-button identity exact;
   - exactly one missing script in the whole Shell;
   - exactly one missing script on that Panel GameObject and zero elsewhere.
4. Invoke `RemoveMonoBehavioursWithMissingScript` only on that exact Panel
   GameObject and require `removed == 1`. Before save require Panel and full Shell
   missing-script counts both zero.
5. After save and Prefab reload, require all final composition validation plus raw
   YAML proof of:
   - zero `m_Script: {fileID: 0}`;
   - zero exact `addedObject: {fileID: 7099084332157108925}`;
   - zero matching `m_AddedComponents` override block;
   - no relay token or unexpected ItemDetail component residue.
6. Replace the ItemDetail carrier's binary `alreadyFinal` shortcut with an explicit,
   fail-closed classification (enum or equivalent):
   - `FINAL_CLEAN`: final composition shape, valid Presenter close contract, zero
     loaded missing scripts and zero forbidden raw-YAML residue; validate only and
     perform no repair mutation;
   - `EXACT_REPAIRABLE_PARTIAL`: exact frozen partial hash plus every raw-YAML and
     loaded-object proof above; perform exactly the one Panel-local repair;
   - `INVALID_CONFLICT`: every partial, mismatched, duplicated or ambiguous state;
     reject before mutation.
   The preexisting old-baseline creation path may remain separately admitted only
   by exact `IntakeShellHash`; after in-memory authoring it must satisfy the same
   `FINAL_CLEAN` invariants before save.
7. Preserve REV11B whole-`guajiann` omission, strict forbidden-owner validation,
   exact source Prefabs, Presenter/runtime, Unified Scene bytes and every unrelated
   Shell carrier/override.
8. No broad missing-script cleanup, source ItemDetail modification, runtime change,
   Scene/Prepare/VFX/Item/Battle write, unpack/apply, user-layout reset or fallback
   carrier.

Development acceptance before another user menu:

- scoped Runtime and Editor compile PASS;
- static proof shows no `GetAddedComponents` assumption remains in the repair path,
  the explicit state classification is fail closed, the exact raw-YAML and loaded
  object gates precede Panel-local removal, and post-save/reload raw YAML is clean;
- old-baseline, clean-final, exact-partial and invalid-conflict branches are covered
  without product serialization;
- protected source Prefab and Unified Scene hashes remain exact;
- no Unity launch/control, Prefab/Scene serialization or Git.

Expected development terminal:
`REV11D_ITEMDETAIL_PARTIAL_STATE_REPAIR_USER_MENU_READY`.

Only after Mainline Guard close may the existing non-Play menu be released once:
`TalismanBag/V0.4/Unified Battle/Apply REV11 Single Formal Path Once`.

Success marker remains:
`SINGLE_FORMAL_PATH_AUTHORING_AND_VALIDATION_PASS`.

Evidence cap remains compile/static before that marker. Milestone remains
`USER_HANDTEST_FAIL / MILESTONE_FIDELITY_BLOCKED`; no popup-visible or P0 readiness
claim is permitted.

## REV11C ItemDetail unserializable close-relay removal — supersedes the relay requirement

Latest one-shot user menu evidence:
`REV11_COMPONENT_COUNT_INVALID TalismanBag.UnifiedBattle.Presentation.ExactItemDetail.C1ExactBattleSandboxItemDetailCloseRelay actual=0`.

Read-only attribution is frozen:

- `C1ExactBattleSandboxItemDetailCloseRelay` is a second public `MonoBehaviour`
  declared in `C1ExactBattleSandboxItemDetailPresenter.cs`; its added override cannot
  be serialized as a valid independent `MonoScript` carrier in the Shell.
- Current partial Shell SHA-256 is
  `941B5C5E06F57247CDF07971D0B9B1FF131E84A82AA0905FE1FC40FEF2DB1109`.
- The Shell contains exactly one `m_Script: {fileID: 0}` row. It is added component
  fileID `7099084332157108925` on the unique nested exact `ItemDetailPanel` root,
  referenced by exactly one matching `m_AddedComponents` override and retaining
  only the expected `presenter` / `panelView` serialized fields.
- Existing `C1ExactBattleSandboxItemDetailPresenter` already owns the accepted
  explicit close behavior through its non-null authored `closeButton`, with
  idempotent `RemoveListener` / `AddListener(HandleCloseRequested)` during bind
  and listener removal during unbind. Outside-content pointer-down close is not a
  Player Promise requirement.

REV11C exact write whitelist:

- `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1UnifiedSingleFormalPathAuthoring.cs`

All runtime ItemDetail files, source ItemDetail Prefab, PrepareSurface, Shell,
Unified Scene and every other carrier are read-only during development. Product
serialization remains user-owned and is allowed only through a later Guard-closed
one-shot menu.

Frozen correction:

1. `CreateItemDetailCarrier` must no longer add, configure or require
   `C1ExactBattleSandboxItemDetailCloseRelay`. Newly authored baseline composition
   uses the existing Presenter plus its exact authored close button only.
2. `ValidateFinalComposition` must stop counting/validating the relay. It must
   instead require exactly one Presenter, exactly one exact `ItemDetailPanelView`,
   the Presenter's serialized `CloseButton` non-null, under that Panel, identical
   to the Panel's authored close-button reference, and the existing Presenter
   authored-reference contract valid.
3. Before the already-final partial branch is validated, support exactly one
   narrow repair intake:
   - Shell hash equals the frozen partial hash above;
   - final composition shape is otherwise present;
   - exactly one missing `MonoBehaviour` exists in the entire loaded Shell;
   - it is on the unique exact nested `ItemDetailPanel` root;
   - exactly one added-component override points to that missing component;
   - the missing component has exactly the expected `presenter` and `panelView`
     serialized references and no other unexpected serialized payload;
   - no other missing script, partial ItemDetail carrier or competing popup exists.
4. Only after that full preflight, remove exactly that one missing added component
   and its matching override, then save/reload. A Unity helper that removes missing
   scripts is allowed only on that exact GameObject after count=1 and exact override
   identity have been proven; no root-wide or broad cleanup is allowed.
5. Old-baseline creation and current-partial repair must converge to the same final
   invariant: missing-script count zero, relay count zero, Presenter close-button
   binding exact, one popup only, closed-state non-raycast contract unchanged.
6. Preserve REV11B whole-object `guajiann` omission and strict
   `RequireNoForbiddenRuntimeOwner`; do not weaken either validator.
7. Do not modify the Presenter/runtime, create a replacement relay, move close
   ownership, infer Item data, touch Scene bytes, or perform a broad rebuild,
   unpack/apply, override cleanup or second Unity pass.

Development acceptance before another user menu:

- scoped `Assembly-CSharp` and `Assembly-CSharp-Editor` compile PASS;
- static proof covers old-baseline no-relay creation, frozen partial-hash repair,
  exact one-object/one-override preflight, zero post-reload missing scripts, exact
  Presenter close-button ownership and unchanged strict forbidden-owner checks;
- protected source Prefabs and Unified Scene hashes remain exact;
- no Unity launch/control, Prefab/Scene serialization or Git.

Expected development terminal:
`REV11C_ITEMDETAIL_RELAY_REMOVAL_USER_MENU_READY`.

After Mainline Guard close, the same user-owned non-Play menu may be released once:
`TalismanBag/V0.4/Unified Battle/Apply REV11 Single Formal Path Once`.

Success marker remains:
`SINGLE_FORMAL_PATH_AUTHORING_AND_VALIDATION_PASS`.

Evidence cap remains compile/static until that marker is returned. Milestone remains
`USER_HANDTEST_FAIL / MILESTONE_FIDELITY_BLOCKED`; no popup, route or P0 user-ready
claim is permitted from REV11C alone.

## REV11B formal `guajiann` omission — supersedes REV11A preservation wording

Latest explicit user scope decision: `guajian可以先不要`.

This is an accepted removal of one non-core decoration/control. It does not weaken
the P0 Player Promise Card: `guajiann` is not a required Board, Tray, Card,
navigation, background, ItemDetail, realtime-Battle, ItemLiving, reward or 0/1/2
arrangement capability.

### Frozen replacement boundary

1. In the loaded `UnifiedBattlePageShell.prefab` composition, locate the unique
   nested exact PrepareSurface instance and the exact relative GameObject path
   `C1ExactBattleSandboxPrepareSurface/guajiann`.
2. Preflight must prove the object is the same inactive historical source object
   that carries `BuildSandboxSpriteSequencePlayer` and its Button persistent
   `PlayFromStart` callback. Any duplicate, different path, active object or
   mismatched source identity fails closed before save.
3. Remove the entire `guajiann` GameObject from the formal Shell composition as one
   authored nested-Prefab removed-GameObject override. Its Image/Sprite/layout and
   local Button/sequence behavior are intentionally omitted from this milestone.
4. Do not modify, unpack, apply-to-source or save
   `C1ExactBattleSandboxPrepareSurface.prefab`. Its source hash remains protected
   at `95207E6F0123938B9F532AE911575867795EC0A6040AB8776F4E9F487ED1FC32`.
5. Preserve the real Prepare presenter/view, exact Board/Tray/Card, navigation,
   background, ItemDetail, FormalBattlePresentation, Shell layout outside this one
   removed object, Unified Scene bytes and unrelated user overrides.
6. Keep `RequireNoForbiddenRuntimeOwner` unchanged. Final loaded Shell validation
   must prove `guajiann` count zero, zero BuildSandbox runtime owner and zero stale
   persistent callback/missing-script residue.
7. Do not add a replacement adapter, compatibility component or formal animation.
   StageTheme and ItemLiving P2 remain separate downstream rows.
8. The exact code/test whitelist, user-owned one-shot Shell-only menu, success
   marker, evidence cap and stop conditions from REV11A remain unchanged except
   that whole-object removal replaces component/callback stripping.

Expected development terminal:
`REV11B_FORMAL_GUAJIAN_OMISSION_USER_MENU_READY`.

Menu after Mainline close:
`TalismanBag/V0.4/Unified Battle/Apply REV11 Single Formal Path Once`.

Success marker:
`SINGLE_FORMAL_PATH_AUTHORING_AND_VALIDATION_PASS`.

No new task/worktree/Git/user decision. The milestone remains
`USER_HANDTEST_FAIL / MILESTONE_FIDELITY_BLOCKED` until later real-path proof.

## REV11A forbidden Prepare sequence-owner strip

Latest user-owned one-shot menu evidence:

`REV11_FORBIDDEN_RUNTIME_OWNER TalismanBag.BuildSandbox.BuildSandboxSpriteSequencePlayer`

This is a same-package authoring correction. Milestone status remains
`USER_HANDTEST_FAIL / MILESTONE_FIDELITY_BLOCKED`.

### Exact attribution

- The forbidden component is not carried by `C1ExactBattleSandboxGameBackground.prefab`.
- It is serialized in the protected source
  `C1ExactBattleSandboxPrepareSurface.prefab` at exact hierarchy path
  `C1ExactBattleSandboxPrepareSurface/guajiann`.
- `guajiann` is inactive and retains the accepted Image/Sprite/material/layout.
  Its `Button.onClick` contains one persistent call to the same
  `BuildSandboxSpriteSequencePlayer.PlayFromStart` component.
- The component has no formal consumer. Formal Prepare open/close motion and input
  lifecycle are already owned by `C1ExactBattleSandboxPrepareSurfacePresenter` and
  `C1ExactBattleSandboxPrepareSurfaceView`; no Sandbox sprite player is required.
- The correct formal carrier is therefore a precise removed-component/property
  override on the nested PrepareSurface instance inside the Shell Prefab. Do not
  modify the source PrepareSurface Prefab, whitelist the type, or replace it with a
  new visual system.

### Exact write whitelist

Code/offline only:

1. `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1UnifiedSingleFormalPathAuthoring.cs`
2. `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1FormalObtainRebuildBattleLoopTests.cs` only if the smallest existing static regression is required.

Serialization remains the existing user-owned one-shot menu and may modify only:

3. `Assets/_Game/Prefabs/TalismanBag/UnifiedBattle/UnifiedBattlePageShell.prefab`.

Protected and byte-read-only:

- `Assets/_Game/Prefabs/TalismanBag/UnifiedBattle/C1ExactBattleSandboxPrepareSurface.prefab` SHA-256 `95207E6F0123938B9F532AE911575867795EC0A6040AB8776F4E9F487ED1FC32`.
- `Assets/_Game/Prefabs/TalismanBag/UnifiedBattle/C1ExactBattleSandboxGameBackground.prefab` SHA-256 `B79F90501227A4FA5CB35BA1F3F85DED7B8C83397428FBF56F39481669CB5906`.
- Unified Scene SHA-256 `D432B6E492EDF0CB0EEA8B62E66E27A6EE3F7773C8BBCC8AD886A86A1A0BDD63`.
- exact Board/Tray/Card, ItemDetail, FormalBattlePresentation, Item/Battle/Reward/Save/VFX and all unrelated files.

### Frozen correction

1. During `AuthorSingleFormalPath`, before `ValidateFinalComposition`, locate the
   exact nested Prefab source and exact relative path
   `C1ExactBattleSandboxPrepareSurface/guajiann`.
2. Preflight must require exactly one
   `TalismanBag.BuildSandbox.BuildSandboxSpriteSequencePlayer` on that object; its
   target Image must be the same GameObject; the object must be inactive; and its
   Button must contain exactly one persistent call targeting that component and
   method `PlayFromStart`.
3. Clear only that exact persistent call and remove only that exact component from
   the nested formal instance, producing explicit Shell-level Prefab overrides.
   Preserve the `guajiann` GameObject, RectTransform, Image, Sprite, material,
   CanvasRenderer, active state, sibling order and every unrelated component/field.
4. Do not modify, unpack, apply-to-source or save the nested PrepareSurface Prefab.
   Do not introduce a formal replacement component because the current formal
   Prepare presenter already owns the actual transition lifecycle.
5. Keep `RequireNoForbiddenRuntimeOwner` unchanged. Post-authoring validation must
   prove zero BuildSandbox/ItemSandbox/legacy bridge runtime owner in the loaded
   final Shell composition and no missing persistent target/script.
6. Re-running against a correctly overridden Shell is idempotent: validate the
   exact removed-component/cleared-callback state and do not create another
   override or mutate visible data.
7. The same menu path and success marker remain authoritative:
   - `TalismanBag/V0.4/Unified Battle/Apply REV11 Single Formal Path Once`
   - `SINGLE_FORMAL_PATH_AUTHORING_AND_VALIDATION_PASS`

### Acceptance and return

- Runtime/Editor scoped compile and focused static proof pass.
- Source PrepareSurface, GameBackground and Unified Scene hashes remain exact.
- No Unity launch/control/serialization by the development task while the user
  owns the Editor. Developer returns
  `REV11A_FORBIDDEN_PREPARE_SEQUENCE_OWNER_STRIP_USER_MENU_READY`.
- Mainline performs one close review before releasing one new user menu pass.
- On the new menu pass, any first failure stops without retry and returns to this
  same visible task.
- Strongest eventual package state remains
  `SINGLE_FORMAL_PATH_INTERNAL_ASSEMBLY_READY / FAITHFUL_PARTIAL`; no user-ready
  or milestone-complete claim is allowed.

Stop if the component exists on another carrier/path, a needed formal behavior
actually calls it, the source PrepareSurface must be modified, or Unity cannot
represent the removal as a Shell nested-Prefab override without unpack/apply-to-source.

## REV11 C1 Unified single formal-path clean consolidation

### Immutable Player Promise Card

- MilestoneId: `C1_UNIFIED_SINGLE_FORMAL_PATH_CLEAN_CONSOLIDATION`.
- ProductContext: `CAMPAIGN_NORMAL_LV1`.
- PlayerPromise: WorldMap -> one clean Unified formal page -> visible realtime 1-1 -> guaranteed I002 -> optional rearrangement -> visible realtime 1-2 reflecting the chosen 0/1/2 active Items -> WorldMap, using the exact BattleSandbox-derived Board/Tray/Card/navigation/background/detail composition and accepted formal Battle presentation.
- PlayerMustSee: exactly one Board/Tray/Card composition, one Battle presentation, one Item-detail popup, one navigation carrier, one stage/background carrier, and no sample/dev/duplicate/historical UI.
- MustChangeLive: current authoritative Item layout and reward entitlement feed the live Battle Session and the next stage. No precomputed summary, hidden compatibility owner or duplicate state owner may decide the result.
- PersistenceOrResetExpectation: Prepare pauses/resumes the same Session; terminal, reset and re-entry clear stale input, popup, cues and presentation.
- ExplicitNonCompletionCases: compile/Prefab existence, restoring inert ghost BoardArea/ItemTrayArea, leaving duplicate mounts hidden, runtime fallback hierarchy, sample/dev panels, copied BattleSandbox runtime graph, deleting validators without validating the real chain, or blocking valid 0/1/2 active-item layouts.
- RequiredRealPath: WorldMap -> Unified -> live 1-1 -> I002 -> optional 0/1/2 arrangement -> live 1-2 -> WorldMap.
- UserAcceptedAt: `USER_AUTHORIZED_UNIFIED_SINGLE_PATH_CLEAN_CONSOLIDATION` plus the accepted Battle 0/1/2 and formal-presentation dependency releases.

This Assignment does not inherit any prior `USER_HANDTEST_READY`. Latest user evidence remains `UNIFIED_FORMAL_HOST_SHELL_SLOT_MISSING BoardArea,ItemTrayArea`; the milestone remains `USER_HANDTEST_FAIL / MILESTONE_FIDELITY_BLOCKED` until the continuous real path is later user-tested.

### Dependency gate

- `V0.4-FormalBattlePresentationPrefabPromotion01 REV01`: `FORMAL_BATTLE_PRESENTATION_SOURCE_CARRIER_RELEASED_TO_MAINLINE / TECHNICAL_PREREQUISITE_COMPLETE / MilestoneCompletionEvidence=NO`.
- `V0.4-C1FormalActiveItemAdmissionAndRealDefeat01`: `BATTLE_0_1_2_ACTIVE_ITEM_ADMISSION_RELEASED / TECHNICAL_PREREQUISITE_COMPLETE / MilestoneCompletionEvidence=NO`.
- Item combat-detail projection: released; Mainline must consume it without hard-coded Item numbers.
- The existing Reward/I002, formal Item Authority, live Battle Session and navigation owners remain authoritative and read-only except for the exact Mainline consumption files below.

### Player-visible capability coverage

| Capability | State owner | Concrete carrier | REV11 contribution | Status after internal success |
|---|---|---|---|---|
| WorldMap route and return | Mainline/Navigation | existing launch transit + Unified Host | preserve and validate | runtime-integration evidence only |
| Exact Board/Tray/Card | Item Authority | `C1ExactBattleSandboxPrepareSurface` -> exact Presenter/Board/Tray/Card | validate unique chain; remove obsolete shell contract | runtime-integration evidence only |
| 0/1/2 active Item admission and real defeat | Battle/Bridge | released realtime adapters/session | consume unchanged | runtime-integration evidence only |
| Formal player/enemy/HP/target/cues | Battle presentation | released `FormalBattlePresentationRoot` source carrier | bind external DamageFloatPool, CueFxAudioRoot and exactly three distinct exact CardViews | presentation inspection still required |
| Navigation + real combat log | Mainline presentation | exact BottomNavigation + new exact BattleSandbox-derived combat-log carrier | replace V03 sample status/log slot | presentation inspection still required |
| Item detail | Item projection + Mainline presenter | exact `ItemDetailPanel.prefab` + `C1ExactBattleSandboxItemDetailPresenter` | mount exactly once and bind real selected instance | presentation inspection still required |
| Background | StageTheme/presentation | current exact BattleSandbox background carrier | preserve one non-raycast carrier | StageTheme identity/user proof remains downstream |
| ItemLivingGradient on exact Cards | Item + Visual | released seven-layer carrier + future exact Card P2 binding | no Card write in REV11 | `OUT_OF_SCOPE_BUT_REQUIRED / UNPROVEN_DOWNSTREAM` |
| Continuous full route | all existing owners | one Unified Shell composition | assemble only | user real-path proof remains downstream |

### Technical carrier decision

- Scene: keep the existing `Scene_TalismanBag_V04_UnifiedBattlePageShell`; Scene bytes must remain unchanged.
- Prefab: keep one authored `UnifiedBattlePageShell.prefab` as the composition body. It owns visible hierarchy, slots and serialized references only.
- System/Authority: Item, Battle, Reward and Navigation remain sole truth owners.
- Bridge/Presenter: existing Host/Presenters translate immutable truth into the Prefabs; no `GameObject.Find`, runtime-created final UI or second owner.
- The obsolete `BoardArea` / `ItemTrayArea` compatibility requirement is removed. Do not recreate empty ghost objects. The required Item carrier is the real PrepareSurface -> exact Presenter/Board/Tray/Card chain.
- The released formal Battle source carrier is nested once and receives external DamageFloatPool, CueFxAudioRoot and exactly three distinct real exact CardView references.
- The exact Item-detail panel is mounted once under its authored popup slot and remains non-intercepting while closed.

### KEEP inventory

- WorldMap launch transit/navigation and the existing Unified Scene.
- `UnifiedBattlePageShell.prefab` as the single authored formal composition.
- exact PrepareSurface, exact Board/Tray/Card, exact BottomNavigation and exact GameBackground Prefabs.
- formal Item Authority/Presenter, Reward/I002 flow, live Battle Session/adapters and formal navigation owner.
- released FormalBattlePresentationRoot, Player, Enemy slots, DamageFloatPool, CueFxAudioRoot and Profile.
- `ItemDetailPanel.prefab`, released Item detail projection and `C1ExactBattleSandboxItemDetailPresenter`.
- `EnemyInfoArea` and `BattleFeedbackLayer` only as pure composition anchors.

### MIGRATE / REPLACE inventory

1. Remove `BoardArea` and `ItemTrayArea` from the Shell required-slot contract and replace their validation with the exact PrepareSurface-owned Item-chain validation.
2. Replace `V03FlowAdapterSlot` sample summary/log responsibility with one exact BattleSandbox-derived authored combat-log/status Prefab bound to accepted realtime cues. Navigation's visible battle-log button must open only this carrier.
3. Mount the exact Item-detail carrier and real projection under exactly one authored popup slot.
4. Bind the accepted formal Battle source carrier to the two external feedback roots and exactly three distinct real exact CardViews.
5. Replace the historical authoring/menu chain with one narrow idempotent `C1UnifiedSingleFormalPathAuthoring` entrypoint. It may update the Shell Prefab only; it must not author or save the Scene.

### DELETE inventory owned by REV11

Delete only after the new path validates and an all-project reference scan proves zero remaining consumer:

- `Assets/_Game/Prefabs/TalismanBag/UnifiedBattle/BattleLikePreviewAreaBridge.prefab` plus `.meta`.
- `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/UnifiedBattleBattleLikePreviewAreaBridgeHost.cs` plus `.meta`.
- `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/UnifiedBattleBattleLikePreviewAreaBridgeBuilder.cs` plus `.meta`.
- `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/UnifiedBattlePageShellSampleData.cs` plus `.meta`.
- `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/UnifiedBattlePageShellSceneBuilder.cs` plus `.meta`.
- `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/UnifiedBattlePageShellVerifier.cs` plus `.meta`.
- `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/UnifiedBattlePageShellReportWriter.cs` plus `.meta`.
- `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1FormalObtainRebuildBattleLoopSceneAuthoring.cs` plus `.meta`.
- `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1ExactBattleSandboxNavigationPhaseAAuthoring.cs` plus `.meta`.
- `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1ExactBattleSandboxItemDetailPhaseBAuthoring.cs` plus `.meta`.
- obsolete menu wrappers/references to those tools in `TalismanBagEditorMenu.cs` and `Chapter1FormalLaunchAuthoring.cs`.
- Shell-local `BossCastBarSlot`, `StoryGuidePopupLayer`, `ResultRewardPlaceholder`, `V03FlowAdapterSlot`, `V04SandboxAdapterSlot`, `DevOnlyDiagnosticsSlot` and their sample text children, but only after any required responsibility has the explicit replacement above.

The old approximate Item Prefabs (`C1FormalItemBoard/Tray/Card` and `ItemTrayArea`) are not in the visible formal path and must have zero Shell/Scene references after REV11, but their asset deletion remains Item-owned and is not authorized here. Record them as `QUARANTINED_FOR_ITEM_OWNER_DELETE`, not as a retained visible fallback.

### Exact write whitelist

Modify existing runtime:

1. `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/UnifiedBattlePageShellSlotNames.cs`
2. `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/UnifiedBattlePageShell.cs`
3. `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/UnifiedBattleFormalSceneHost.cs`
4. `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/Presentation/ExactItemDetail/C1ExactBattleSandboxItemDetailPresenter.cs` only if required to consume the already-released projection; no Item-value inference.

Add runtime/presentation only if the exact source survey proves no reusable formal carrier exists:

5. `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/Presentation/ExactNavigation/C1ExactBattleSandboxCombatLogView.cs` plus `.meta`.
6. `Assets/_Game/Prefabs/TalismanBag/UnifiedBattle/C1ExactBattleSandboxCombatLogPanel.prefab` plus `.meta`.

Modify/add Editor and focused tests:

7. `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1UnifiedSingleFormalPathAuthoring.cs` plus `.meta`.
8. `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1FormalObtainRebuildBattleLoopTests.cs`.
9. `Assets/_Game/Scripts/TalismanBag/Editor/TalismanBagEditorMenu.cs`.
10. `Assets/_Game/Scripts/TalismanBag/Editor/V04/Campaign/Chapter1/Chapter1FormalLaunchAuthoring.cs`.
11. The exact DELETE paths listed above.

Serialization:

12. `Assets/_Game/Prefabs/TalismanBag/UnifiedBattle/UnifiedBattlePageShell.prefab`.

All matching `.meta` writes are limited to explicitly added/deleted assets above. The Unified Scene, PrepareSurface, exact Board/Tray/Card, BottomNavigation, GameBackground, released Battle presentation source Prefabs/Profile, ItemDetailPanel source Prefab, Item Authority, Battle/Enemy/Reward/Save, BuildSettings and ProjectSettings are read-only.

### Intake baselines

- Shell slots `22416AF3C3F7B2553E4D1E074A9AA82A0737F234313A5CF7D27CEC9C2680D906`.
- Shell runtime `536140B5B052A4D41A01FF08A41E6A9C85BDF2DC99783D1EE37ED270AC30C2DF`.
- Formal Host `DEA4811A27D981223148F0A90336BB71E693D3E87331520580E9FFDA8F39B4A6`.
- Item-detail presenter `6A2D78CDE8680F72DABB4574B84F215CC3405CE8CD15D76AD8B3A83E5D039490`.
- Shell Prefab `0D6CB76DB22F6E6F575DF9827D82423282B6FF28D1B299BA70B239224DA1389A`.
- Unified Scene protected `D432B6E492EDF0CB0EEA8B62E66E27A6EE3F7773C8BBCC8AD886A86A1A0BDD63`.
- PrepareSurface protected `95207E6F0123938B9F532AE911575867795EC0A6040AB8776F4E9F487ED1FC32`.
- FormalBattlePresentationRoot protected input `0553DF1F08584D49F1723F9539475A101F86F3FED2B143FE355D29CBC10A7C3B`.
- DamageFloatPool protected `E7B7906DD684B213490EA8B1C92B88D1B42299608EB583FF9203AB4B08588A73`.
- CueFxAudioRoot protected `7443D7B84A82F58A007BD01506A223182420F4A6C6BC80EAFE4A6BF340AAF005`.
- GameBackground protected `B79F90501227A4FA5CB35BA1F3F85DED7B8C83397428FBF56F39481669CB5906`.
- ItemDetailPanel protected `2A6924DA0823B74E2639B593926D8C2F8422101B01EDBE4073AF33CD424E027C`.

### Bounded serialization strategy

1. Recheck task visibility, Unity/Tuanjie/ShaderCompiler/bee processes, `Temp/UnityLockfile` and `Library/EditorInstance.json` immediately before authoring.
2. Implement code/offline tests first. Use one owned, bounded Unity Prefab authoring/validation pass only when no external serialization owner exists.
3. Load only `UnifiedBattlePageShell.prefab` contents. Never open/save/rewrite the Unified Scene, run a broad generator, instantiate a second Shell, use Revert All/Apply All/Unpack, or clear unrelated overrides.
4. Modify only the named historical objects/serialized references. Preserve all unrelated user hierarchy, layout, anchors, positions, images and Prefab links.
5. Validate post-save by reloading the Shell Prefab, then compare the Unified Scene hash byte-for-byte to the protected baseline.
6. One automatic authoring correction maximum. Any new serialization/harness failure after that returns one exact blocker rather than another broad pass.

### Minimum acceptance

1. `CollectMissingRequiredSlots` and Host admission no longer require `BoardArea` or `ItemTrayArea`; neither ghost object exists.
2. Exactly one real PrepareSurface -> formal Item Presenter -> exact Board/Tray/Card chain exists; no approximate or legacy bridge mount is serialized in Shell/Scene.
3. Exactly one accepted FormalBattlePresentationRoot is bound to external DamageFloatPool, external CueFxAudioRoot and three distinct exact CardViews; source/internal and downstream/external validation both pass.
4. Exactly one Item-detail popup/presenter is authored and bound to the released real projection; closed state is alpha=0/interactable=false/blocksRaycasts=false.
5. Navigation retains five real behaviors. Battle log uses the migrated exact authored carrier, not V03 sample text or a dead button.
6. Background, Prepare input lifecycle, paused live-Item refresh, optional 0/1/2 stage-two admission, reward dedupe and return route remain intact.
7. Legacy bridge/sample/dev objects and generation/menu paths in the DELETE inventory are gone and cannot regenerate themselves. All-project GUID/type/text reachability scan passes.
8. Runtime and Editor scoped compile, focused deterministic Mainline regressions, missing-script/reference validation and serialized-carrier inspection pass.
9. Unified Scene hash remains exact; no Scene/BuildSettings/ProjectSettings/Item/Battle/Reward/Save/VFX-source write.
10. The developer terminal is `SINGLE_FORMAL_PATH_INTERNAL_ASSEMBLY_READY / FAITHFUL_PARTIAL`, not `USER_HANDTEST_READY`.

### OutOfScopeButRequiredForMilestone

- Item-owned P2 binding of the accepted ItemLivingGradient carrier into exact Card/CardView/Presenter remains `UNPROVEN_DOWNSTREAM`.
- StageTheme identity/background profile delivery and user-visible proof remain `UNPROVEN_DOWNSTREAM`.
- Normal-Editor presentation inspection and the one continuous full player-path handtest remain downstream.

### Return path and stop conditions

Developer returns one terminal result to Mainline Flow Guard with changed/deleted/created paths, final hashes, compile/tests, serialized inventory, Scene hash and process/lock cleanup. Mainline performs one close review; `talisman-version-pm` collects the result. Same visible task is reused for one in-scope correction.

Stop with one exact blocker if the task finds a live consumer for a DELETE row, requires deleting Item-owned approximate carriers, needs a Scene write, cannot bind the released source carrier to exactly three exact CardViews, needs a second runtime owner/fallback hierarchy, or encounters an external Shell serialization owner.

## REV10 Paused live-Item refresh correction

Latest user real-path evidence is authoritative:

`Prepare 中把当前 Board 道具退回 Tray，关闭整备并恢复同一场战斗后，敌人仍继续掉血。`

Milestone status remains `USER_HANDTEST_FAIL / MILESTONE_FIDELITY_BLOCKED`.

### Immutable player promise row

While the authoritative realtime Battle is paused in Prepare, accepted formal Item
rearrangement must change that same Battle after resume. Returning the only active
damage Item to Tray cancels its future attacks. Re-placing an accepted damage Item
restores only future attacks from the accepted paused tick. Battle time, current
player/enemy HP, target, encounter/session identity, enemy cadence and accepted
history remain unchanged. No hidden time advance, restart, precomputed substitute,
fake UI or second Item/Battle owner is allowed.

This correction owns the live Item-input refresh row only. Exact page parity, Item
Detail Phase B, VFX P2 and the full P0 milestone remain downstream and blocked.
PlayerVisibleDelivery stays `PARTIAL`; MilestoneCompletionEvidence stays `NO`.

### Frozen first missing layer and ownership

- Item Authority already owns ReturnToTray/placement truth and publishes the latest
  immutable `C1FormalItemBattleInputSnapshot`.
- `C1FormalRealtimeBattleSession` currently captures `request.itemFacts` only at
  `TryStart` and reschedules the original ItemTrigger forever.
- Mainline currently locks Item and resumes the same Session without submitting the
  latest Item snapshot.
- Battle/Bridge therefore owns one new paused-session refresh transaction. Mainline
  only submits the current authoritative snapshot and consumes the accepted result.
- `C1Lv1FormalBattleApplicationAdapter` remains the strict start/oracle translator.
  Do not weaken its placed/lit winning-start rules. Mid-session refresh uses a
  separate narrow translator in the realtime adapter.

Battle/Bridge one-time consultation status:
`BATTLE_BRIDGE_LIVE_ITEM_REFRESH_BOUNDARY_FROZEN / SAME_TASK_AUTHORIZED`.

### Exact write whitelist and intake baselines

Only these existing files may change:

1. `Assets/_Game/Scripts/TalismanBag/Contracts/Battle/C1FormalRealtimeBattleSessionContracts.cs`
   - intake SHA-256 `B82C0B87A2FCAF214E88113732A3477B03430AA9A5C618D1010F3DB37758C065`.
2. `Assets/_Game/Scripts/TalismanBag/BattleBridge/Formal/C1FormalRealtimeBattleSession.cs`
   - intake SHA-256 `4AA29D486EC8E7054D0FE5B106EEF79DFDA2B28A85232AD3399CAE0F38D95D47`.
3. `Assets/_Game/Scripts/TalismanBag/BattleBridge/Formal/C1FormalRealtimeBattleSessionAdapter.cs`
   - intake SHA-256 `4304CB3D6E267EC1AF53B00061501A88DF754B8025D1DB6049046071D99AFB1F`.
4. `Assets/_Game/Scripts/TalismanBag/Editor/BattleBridge/C1FormalRealtimeBattleSessionVerifier.cs`
   - intake SHA-256 `089567F06672302E66EC3286A377509B8E9D7E096E69E50D3E5466E183113659`.
5. `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/C1FormalObtainRebuildBattleLoop.cs`
   - intake SHA-256 `261F9F64C7F7FFCA2018746A2D8413EC2066A62644469E53455E8B36234196AB`.
6. `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/UnifiedBattleFormalSceneHost.cs`
   - intake SHA-256 `1156C6333C5EDBFE6EF0297C4131F2050FFE25D953AAA036BEAF88C1963532E9`.
7. `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1FormalObtainRebuildBattleLoopTests.cs`
   - intake SHA-256 `6154B24F0CA72C2B750649EA0E1636DB3F1AFAADCD765B18DFED9312786371FD`.

No `.meta`, Item Authority/Presenter/View, Enemy, Reward, Save, Scene, Prefab,
VFX, BuildSettings, ProjectSettings, old start translator, Item Detail Phase B or
other file may change. Patch budget: seven existing files; no new file.

### Frozen Battle/Bridge contract

1. Add exact immutable
   `C1FormalRealtimeBattleItemRefreshRequest.v1` and
   `C1FormalRealtimeBattleItemRefreshResult.v1` contracts in the existing realtime
   Battle contract file. The request carries: schema id, session id/token, expected
   session/reset generations, refresh command id, expected paused Battle time,
   expected prior Session-state canonical signature, complete Item battle-input
   canonical, expected Item-session and Item-catalog canonicals, and the translated
   active Item facts. The result carries: accepted/error, session/reset identity,
   Battle time, input canonical, accepted command id, canceled/retained/scheduled
   future Item-action counts, active-fact count, state snapshot, emitted cues and
   canonical signature.
2. The realtime adapter accepts the latest authoritative
   `C1FormalItemBattleInputSnapshot`, validates schema/context/session/catalog/item
   instance/projection/canonical lineage, and translates only currently placed and
   authoritatively lit damage rows into active facts. A valid snapshot with zero
   active facts is accepted. It must not call or relax the start oracle translator.
3. Refresh is accepted only while the exact live Session is active, nonterminal and
   authoritatively paused. Running, terminal, wrong context/session/token,
   stale/reset mismatch, malformed snapshot, unknown Item or invalid projection
   rejects without mutation.
4. An accepted refresh atomically handles only unexecuted future ItemTrigger and
   Item-unlit diagnostic actions, retains all executed/history rows and all pending
   enemy actions, and replaces the current active Item facts. A future ItemTrigger
   whose source id and fact canonical are still unchanged stays at its existing due
   time. A removed fact is canceled in-place with a stable diagnostic. A newly
   active or changed fact is scheduled strictly at `acceptedBattleTime + cooldown`.
   No past-due burst, retroactive damage, duplicated event/application id or
   action-index alias is allowed.
5. Identical duplicate refresh command id and canonical against the same state
   returns the same accepted no-op result without re-cancelling, re-scheduling or
   emitting duplicate cues. The same command id with different canonical rejects
   `REFRESH_COMMAND_CONFLICT`; stale prior-state canonical, generation, reset or
   session rejects unchanged. Reset/unbind clears refresh state and invalidates stale
   requests.
6. Expose exact APIs:
   `C1FormalRealtimeBattleSession.TryRefreshItemsWhilePaused(request, out result)`
   and
   `C1FormalRealtimeBattleSessionAdapter.TryRefreshItemsWhilePaused(itemSnapshot, expectedItemSessionCanonicalSignature, refreshCommandId, out result)`.
   Battle remains the only clock, damage, HP, target, action and result owner.
7. No-refresh sessions retain the existing exact deterministic-oracle parity gate.
   After at least one accepted Item refresh, terminal result is derived from the
   live Session state and must not fault only because it legitimately differs from
   the start oracle. Do not rerun or manufacture a new oracle. The resulting
   `BattleResultSnapshot` remains `shouldWriteSave=false` and
   `shouldGrantReward=false`; Reward/Mainline continue owning downstream policy.

### Frozen Mainline integration order

For live Prepare close, preserve the same session and execute exactly:

`Item BATTLE_LOCKED -> create latest authoritative Item battle snapshot -> accepted Battle paused refresh -> finish hide/lower -> authoritative TryResume`.

- Mainline may expose one read-only loop method that submits the current Item
  Authority snapshot to the realtime adapter and returns the accepted refresh
  envelope/tick plus diagnostic.
- Host must consume refresh cues/result before resume. If refresh rejects, remain
  paused and Item-locked, do not resume stale attacks, and emit one concise existing
  formal diagnostic without fallback or repeated per-frame spam.
- Duplicate close callbacks/transition completion must be idempotent. Reset,
  disable, stage change and re-entry cannot reuse a prior refresh command/state.
- Existing pause/unlock/show and lock/hide/resume ordering, zero hidden advancement,
  navigation, exact Prepare carriers and Phase B safe code remain protected.

### Deterministic acceptance and evidence

1. Start 1-1, advance to a non-zero accepted tick, pause, return I001 to Tray,
   refresh and resume: Battle time/HP/target/enemy schedule/history are unchanged at
   refresh; no later Item damage occurs; enemy actions continue normally.
2. Pause again, legally re-place/light I001, refresh and resume: its first new attack
   is due at accepted tick plus cooldown, then repeats normally; no catch-up burst.
3. Stage 1-2 refresh covers I001/I002 placed/unplaced/lit/unlit combinations and
   future damage uses the latest accepted facts only.
4. Duplicate identical refresh is a no-op success; mismatched duplicate, stale
   generation/reset/session, running refresh, terminal refresh and malformed
   canonical all reject with zero mutation.
5. Refresh preserves current player/enemy HP, target, battle time, enemy cadence,
   prior cue/application ledger and monotonic ids. Reset/re-entry has no stale action.
6. No-refresh 1-1/1-2 oracle fixtures stay exact. Refresh-divergent win/lose terminal
   truth is accepted live truth, not `REALTIME_ORACLE_MISMATCH`, and has no direct
   Save/Reward/Inventory/Progress write.
7. Actual scoped `Assembly-CSharp` and `Assembly-CSharp-Editor` compile pass plus
   focused deterministic Battle and Mainline tests. No Unity, Scene/Prefab
   serialization, Fresh Play harness or Git.

Expected development terminal:

`REV10_PAUSED_LIVE_ITEM_REFRESH_COMPILE_READY`

Return exact changed files/hashes, compile/test counts, preserved baselines and the
shortest single normal-Editor real-path handtest. This terminal does not itself
claim user PASS, package completion, full P0 readiness, Item Detail Phase B release
or VFX P2 release.

Stop with the first exact blocker if the implementation would restart/replace the
encounter, mutate Item Authority, rerun a one-shot result, add a second Battle owner,
change Enemy/Reward/Save/Scene/Prefab/VFX, or cannot preserve the current live state.

## REV09 Item Board-drag bind-reset correction

Latest user real-path evidence is authoritative:

`棋盘栏的道具在整备状态下还是没办法移动。`

Milestone status remains `USER_HANDTEST_FAIL / MILESTONE_FIDELITY_BLOCKED`.

### Frozen first failing layer

The current Unified Prepare orchestration already pauses the authoritative realtime Battle, accepts `PREPARE_ENABLED`, enables the authored Prepare CanvasGroup input gate, and mounts the exact Board with the local raycast override. The missing behavior is Item-owned:

1. `C1ExactBattleSandboxItemBoardView.OnPointerDown` stores `pressedItemInstanceId`.
2. `OnBeginDrag` forwards that identity to `C1ExactBattleSandboxItemArrangementPresenter.BeginBoardDrag`.
3. `BeginBoardDrag -> BeginInteraction -> PublishCurrent` immediately calls `boardView.Bind(...)`.
4. `Bind` unconditionally clears `pressedItemInstanceId`.
5. Later `OnDrag` and `OnEndDrag` therefore have no stable identity and never reach preview/update/commit.

The released Item Authority already owns `MoveOnBoard`, candidate validation, commit, invalid restore and stale-command rejection. No Mainline, Battle, Scene or Prefab seam is missing.

### Item Guard authorization and exact write whitelist

Item Guard returned `SAME_MAINLINE_TASK_ITEM_WRITE_AUTHORIZED` for the existing visible task `019fc0d0-be0b-7280-bebf-5600250eec24`.

Only these two files may change:

1. `Assets/_Game/Scripts/TalismanBag/Items/CampaignBaseline/C1ExactBattleSandboxItemBoardView.cs`
   - intake SHA-256 `A176C16F21545BC00B0D91D3B6F8812C565E1F4CFB9489943F5D701C27C19E65`.
2. `Assets/_Game/Scripts/TalismanBag/Editor/ItemCampaignBaseline/C1FormalItemInteractionAuthorizationTests.cs`
   - intake SHA-256 `ED2A27EECE948EFC433004AD85327CE23881FEEF34A8143DA91582263497DBB7`.

No `.meta`, Presenter, Authority, Mainline, Battle, Scene, Prefab, Reward, Save, VFX or other Item file may change.

### Frozen correction

1. Separate transient pointer-down identity from the stable active Board-drag identity.
2. `OnBeginDrag` captures exactly the pressed authoritative item identity before `Presenter.BeginBoardDrag` can trigger `PublishCurrent/Bind`.
3. A benign `BoardView.Bind` may clear transient press state and repaint, but must preserve the stable active Board-drag identity for `OnDrag/OnEndDrag`.
4. `OnDrag/OnEndDrag` route only that stable identity. Stale or mismatched Presenter state remains a no-op.
5. `OnEndDrag` clears both local identities after forwarding.
6. `CancelInteractionState`, `BATTLE_LOCKED`, unbind, reset/session identity change and failed authorization synchronously clear both identities and all feedback.
7. Do not add item-id/shape branches, a second placement owner or a public gameplay API used only by tests.

### Acceptance and return

- In `PREPARE_ENABLED`, an existing Board item survives the `BeginInteraction -> PublishCurrent -> Bind` repaint and reaches preview/update/one authoritative `MoveOnBoard` commit.
- `BATTLE_LOCKED` remains zero-mutation; stale `Drag/EndDrag` are no-ops.
- Invalid/collision/bounds/cancel restores the exact prior placement.
- Tray-to-Board, Board-to-Tray, four rotations, click/detail and Tray scrolling remain unchanged.
- Actual scoped `Assembly-CSharp` and `Assembly-CSharp-Editor` compile pass.
- Focused deterministic bind-during-drag, cancel, lock and stale-event tests pass.
- Unity is not required; no Scene/Prefab serialization or Git.

Expected development terminal:

`REV09_ITEM_BOARD_DRAG_BIND_RESET_USER_HANDTEST_READY`

This terminal is compile/deterministic evidence only. The package remains `FAITHFUL_PARTIAL`; the user must perform the shortest integrated normal-Editor Prepare interaction handtest before any VFX P2 or Item Detail Phase B release.

Stop with the first exact blocker if Presenter/Authority contracts or serialized carriers would need to change.

## REV08 required Host removed-carrier correction — latest menu failure

Latest user evidence is authoritative:

`[UnifiedFormalHost] UNIFIED_FORMAL_HOST_AUTHORED_REFERENCE_MISSING`

The REV07 menu reached `AuthorUnifiedScene`, reverted the three stale Item Host properties in memory, then stopped before Scene save at `host.ValidateAuthoredBindings`. The accepted base `UnifiedBattlePageShell.prefab` has non-null references for every required Host carrier. The Scene still contains exactly one non-empty `m_RemovedGameObjects` override on the Unified shell Prefab instance. Therefore the remaining first-layer missing reference is a required base carrier suppressed by that Scene removed-GameObject override, not another Item reference assignment defect.

### Frozen correction

1. Keep runtime Host/Shell and every presentation/Item/Battle Prefab read-only. Modify only the existing authoring tool.
2. Before final Host validation, inspect required Host serialized reference fields individually:
   - `shell`, `marker`, `v03FlowAdapterSummaryText`, `navigationPresenter`, `prepareSurfacePresenter`, `gameBackgroundImage`, `itemPresenter`, `itemBoardView`, `itemTrayView`, `formalPresentationRoot`.
3. If a Scene property is null because it is itself a null property override while the accepted base Prefab property is non-null, revert only that exact null property override.
4. If a required property has no property override but resolves null because its referenced base GameObject is removed from the Scene Prefab instance, use Unity 2022.3 `PrefabUtility.GetRemovedGameObjects(...)` / removed-override data to identify it. Revert only when all conditions are exact:
   - exactly one missing required Host field maps to exactly one removed base GameObject;
   - the removed override belongs to the current Unified shell Prefab instance;
   - the removed asset GameObject is the GameObject carrying the accepted base component referenced by that missing Host field;
   - no unrelated removed object is restored.
5. The current Scene contains one non-empty removed-GameObject row. If it does not map uniquely to the missing required Host carrier, stop with a field-specific exact blocker. Never clear all removed overrides or rebuild/reinstantiate the shell.
6. Preserve every unrelated Scene property, transform, active-state, sibling and added-object override. Preserve the already-frozen REV07 cleanup of only `itemPresenter`, `itemBoardView`, `itemTrayView` and the old exact Item mounts.
7. Replace the generic authoring failure with field-specific diagnostics showing:
   - missing property path;
   - whether it was a null property override or removed-GameObject override;
   - matched asset GameObject/source component identity;
   - post-revert inherited reference and `prefabOverride=false`.
8. Final validation must prove all required Host references non-null, `ValidateAuthoredBindings` passes, the single accepted PrepareSurface/Board/Tray chain remains, and no unrelated Scene override changed.

### REV08 baselines, writes and terminal

Accepted read-only inputs:

- authoring-tool intake: `F1AE1913135E3CA01B78D3B43ABB3313FE4F4F9CBC56D43B6A433FD3106B4554`;
- Unified shell: `44A897ACECF2F871A7BCD45A3390FFF5ED72EB894FB0A815E751BC6BA4663590`;
- PrepareSurface: `95207E6F0123938B9F532AE911575867795EC0A6040AB8776F4E9F487ED1FC32`;
- pre-correction Unified Scene: `C512A8B16C2E537204B472493EB8CEAAB41F81CF1D88629F9B8A576B356648B7`.

Allowed code write now:

- `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1ExactBattleSandboxNavigationPhaseAAuthoring.cs`.

Allowed serialization write only through one user-executed non-Play menu pass after compilation:

- `Assets/_Game/Scenes/Scene_TalismanBag_V04_UnifiedBattlePageShell.unity`.

The development task performs scoped offline compile/static proof only and returns `REV08_REQUIRED_HOST_CARRIER_USER_EDITOR_MENU_READY`; it must not start, terminate or control Unity or invoke the menu. The user then exits Play, waits for compilation and invokes `TalismanBag/V0.4/Unified Battle/Apply REV05 Narrow Mount Once` once.

Any ambiguous mapping, more than one missing required field/removal, unsupported Unity API or new validator failure returns one exact blocker. No blanket override clearing, Scene replacement, automated Unity/Fresh Play loop, repeated menu cycle, Phase B resume, VFX P2 dispatch or readiness claim.

## REV07 Scene Item-mount override correction — latest Fresh Play failure

Latest user Fresh Play evidence is authoritative:

`[UnifiedFormalHost] UNIFIED_FORMAL_ITEM_MOUNT_BINDING_INVALID`

REV06 successfully serialized the corrected PrepareSurface, Unified shell and Unified Scene. The base `UnifiedBattlePageShell.prefab` now contains the correct integrated PrepareSurface-owned `itemPresenter`, `itemBoardView` and `itemTrayView`. The Scene instance still carries stale overrides pointing those three Host fields to the superseded scene-added Presenter/Board/Tray mounts. Runtime therefore combines the new PrepareSurface with old Item references and correctly fails closed.

### Frozen correction

1. Modify only the existing REV05-REV07 authoring tool. Do not change runtime Host, Shell, Item or Battle code.
2. In the Unified Scene authoring step, retire only superseded formal Item mounts outside the accepted PrepareSurface:
   - the old scene-added `C1ExactBattleSandboxItemArrangementPresenter` component, only when it is an added component override and is not the PrepareSurface-owned Presenter;
   - the old exact Board and Tray prefab instances, only when they are outside the PrepareSurface, resolve to the exact Board/Tray source Prefab paths and are added under the legacy `BoardArea` / `ItemTrayArea` shell slots.
3. Do not delete by display name alone. Any unexpected source path, parent slot, non-added component/object or additional competing owner is an exact blocker.
4. Remove/revert exactly these three stale serialized Host Scene property overrides:
   - `itemPresenter`;
   - `itemBoardView`;
   - `itemTrayView`.
5. Use serialized-property/Prefab override APIs so those fields inherit the already-correct references from `UnifiedBattlePageShell.prefab`. Do not reassign them as new Scene overrides and do not rebuild the Scene hierarchy.
6. Preserve every unrelated Scene override, including user-authored layout/RectTransform/style/active/sibling changes and unrelated Host fields. Navigation, PrepareSurface, background and formal presentation references are not blanket-reverted.
7. After cleanup, validation must prove:
   - the three Host Scene property paths have no property modifications;
   - resolved Host values equal the PrepareSurface-owned Presenter/Board/Tray and satisfy `ValidateAuthoredBindings`;
   - exactly one Item Presenter, one Board and one Tray remain, all inside the accepted PrepareSurface;
   - Board and Tray still resolve to the exact source Prefabs and retain the REV06 local Board raycast override through the PrepareSurface;
   - no stale scene-added exact Item mount object/component, duplicate owner, missing script, BuildSandbox runtime dependency or runtime fallback remains.

### REV07 write and execution boundary

Allowed code write now:

- `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1ExactBattleSandboxNavigationPhaseAAuthoring.cs`.

Allowed serialization write only through one user-executed in-Editor menu pass after compilation:

- `Assets/_Game/Scenes/Scene_TalismanBag_V04_UnifiedBattlePageShell.unity`.

`UnifiedBattlePageShell.prefab`, PrepareSurface, Navigation, gameBackground and exact Item Board/Tray/Card Prefabs are read-only accepted inputs for REV07. All runtime Host/Shell/Presenter files, Item/Battle truth, BattleSandbox source Scene, BuildSettings/ProjectSettings, Phase B Item Detail and Item VFX P2 remain protected.

The user-open Unity Editor remains the only serialization owner. The development task performs code/offline compile/static proof only and returns `REV07_SCENE_OVERRIDE_CLEANUP_USER_EDITOR_MENU_READY`; it must not start, terminate or control Unity or invoke the menu.

The user then exits Play, waits for compilation and invokes `TalismanBag/V0.4/Unified Battle/Apply REV05 Narrow Mount Once` once. A successful menu result returns to Mainline Guard for the close review; any new failure returns one exact blocker. No automated Fresh Play loop, repeated menu cycle, manual Inspector deletion, Scene rebuild, Phase B resume, VFX P2 dispatch or milestone claim.

## REV06 Prepare input-boundary correction — user Editor menu failure

Latest user evidence is authoritative. Running `TalismanBag/V0.4/Unified Battle/Apply REV05 Narrow Mount Once` stopped before Shell/Scene mounting with `EXACT_PREPARE_INPUT_OR_RUNTIME_BOUNDARY_INVALID`.

Read-only attribution accepts the first missing fact:

- the PrepareSurface contains the exact nested Board and Tray Prefab instances and three exact Card views;
- no `BuildGridInteractionPreviewController` or `BuildGridInteractionPreviewRuntime` is serialized into the PrepareSurface/Board/Tray chain;
- the exact source Board Prefab root `Graphic/Image.raycastTarget` is intentionally `false` and remains protected;
- the PrepareSurface currently has no local nested-property override, so the formal prepare input ancestry cannot reach the Board surface even when the existing InputGate and Item authorization are open.

This is a formal composition/input-boundary defect, not an Item shape, placement, Battle, Scene redesign or source-Prefab defect.

### Frozen correction

1. Keep `C1ExactBattleSandboxItemBoard.prefab`, `C1ExactBattleSandboxItemTray.prefab` and `C1ExactBattleSandboxItemCard.prefab` byte-identical and source-linked.
2. Update only the REV05/REV06 authoring tool so the existing PrepareSurface receives one explicit local nested Board root `Graphic.raycastTarget=true` property override. Do not unpack, clone or edit the source Board Prefab.
3. The override is presentation/input routing only. It remains gated by the existing PrepareSurface `CanvasGroup` InputGate and `C1FormalItemInteractionAuthorization` lifecycle:
   - battle/transition/closed: InputGate non-interactable/non-raycast and Item `BATTLE_LOCKED`;
   - fully open after authoritative pause: InputGate raycast/interactable and Item `PREPARE_ENABLED`;
   - close/reset/failure: Item locks and clears residue before InputGate closes.
4. Validation must prove all of the following before mounting:
   - nested Board and Tray still resolve to the exact source Prefab paths;
   - source Board `Graphic.raycastTarget` remains `false` and source Board hash remains exact;
   - the PrepareSurface nested Board root resolves `raycastTarget=true` through an explicit local Prefab property modification for `m_RaycastTarget`, not through source mutation or runtime code;
   - the PrepareSurface InputGate is authored closed by default;
   - Cards are present and no BuildSandbox controller/runtime dependency exists.
5. The existing in-Editor menu remains the only serialization path. It may apply this one controlled PrepareSurface override, validate the corrected carrier, then mount/update only Unified Shell and Unified Scene. It must not regenerate Navigation, gameBackground, Board, Tray or Card.
6. Navigation and gameBackground hashes remain fixed. The corrected PrepareSurface final hash is reported and becomes the next Guard baseline only after successful menu execution; do not weaken validation to arbitrary carrier drift.

### REV06 baselines and whitelist

Protected source baselines:

- exact Board: `46949A5FECAAF94960D2F2A31C6715F0C1297CDAEAD1C71FF749959762FBB7A8`;
- exact Tray: `AE444A9BB96E84DB5409A444DADF13787575E377EE93956FA0B31E608F1751E1`;
- exact Card: `C4D449A1C298BF5B2A91072A4BEA2A67AFD5E1B1D713A4D769CB843D059DDD8C`;
- Navigation: `E8F694911F95A0EA68A164E487B7501DEFAAA09EB2F74453C7678BAABDF4CD80`;
- gameBackground: `B79F90501227A4FA5CB35BA1F3F85DED7B8C83397428FBF56F39481669CB5906`;
- pre-correction PrepareSurface: `5F829419ECE5F14A678BD7BA84DC77AE9D2C09693C04CC852B6022D1939DB31A`;
- pre-mount Unified Shell: `A767D306FFE7787630E184F7DDE156DCD3E63639C4F67EED6A0F91C1DDB7B675`;
- pre-mount Unified Scene: `9FED79FE312A34A6B802F464F928DFD032AF4615EBEE4715A7BF73DEC3BE3058`.

Allowed code write now:

- `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1ExactBattleSandboxNavigationPhaseAAuthoring.cs`.

Allowed serialization writes only through the user-open Editor menu after compile:

- `Assets/_Game/Prefabs/TalismanBag/UnifiedBattle/C1ExactBattleSandboxPrepareSurface.prefab`;
- `Assets/_Game/Prefabs/TalismanBag/UnifiedBattle/UnifiedBattlePageShell.prefab`;
- `Assets/_Game/Scenes/Scene_TalismanBag_V04_UnifiedBattlePageShell.unity`.

Everything else remains read-only, including exact Item source Prefabs, Navigation/gameBackground carriers, Host/Presenter runtime code, Battle/Item truth, BattleSandbox source Scene, BuildSettings/ProjectSettings, Phase B Item Detail and Item VFX P2.

### Execution and terminal

- The user currently owns an open Unity Editor. The development task must not start, terminate or control Unity and must not serialize concurrently.
- Development performs the authoring-tool correction plus scoped offline compile/static proof only, then returns `REV06_PREPARE_INPUT_OVERRIDE_USER_EDITOR_MENU_READY` with the exact same menu instruction. This is not player-visible readiness.
- The user exits Play, waits for Unity compilation, then invokes `TalismanBag/V0.4/Unified Battle/Apply REV05 Narrow Mount Once` once. The menu must apply the single PrepareSurface override and continue through Shell/Scene mount and validation.
- A successful menu result returns to Mainline Guard for one close review capped at `CORRECTED_BASE_INTERACTION_USER_HANDTEST_READY / FAITHFUL_PARTIAL`.
- Any new validator/serialization failure returns the first exact blocker. No automated Unity launch, repeated menu loop, carrier regeneration, Phase B resume, VFX P2 dispatch or milestone claim.

## REV05 narrow mount continuation — Guard-authorized after QA_HARNESS_FAIL

The previous authoring attempt ended in `REV05_AUTHORING_VALIDATION_RETRY_EXHAUSTED / QA_HARNESS_FAIL`. Its generated presentation carriers are accepted as preservable inputs for this continuation. This is not authorization for another full generation pass.

Preserve these exact current Prefab baselines:

- `C1ExactBattleSandboxBottomNavigation.prefab`: `E8F694911F95A0EA68A164E487B7501DEFAAA09EB2F74453C7678BAABDF4CD80`;
- `C1ExactBattleSandboxPrepareSurface.prefab`: `5F829419ECE5F14A678BD7BA84DC77AE9D2C09693C04CC852B6022D1939DB31A`;
- `C1ExactBattleSandboxGameBackground.prefab`: `B79F90501227A4FA5CB35BA1F3F85DED7B8C83397428FBF56F39481669CB5906`.

### Exact correction boundary

1. Modify only `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1ExactBattleSandboxNavigationPhaseAAuthoring.cs` before authoring.
2. The formal functional controls `V04BattlePreparejiasu` and `V04BattlePreparezhandourizhi` are the only allowed `Button.enabled` semantic exceptions when their read-only Sandbox source buttons are disabled. Their promoted formal buttons must be enabled because the accepted formal package requires working Speed and Battle Log controls.
3. The exception applies only to `Button.enabled`. RectTransform, Image/Sprite, transition, ColorBlock, SpriteState, Navigation, interactable state, target-graphic presence and persistent-listener count remain exact validation facts. Decorative controls remain source-equivalent.
4. Add or use one narrow continuation entrypoint that validates the three already-generated Prefabs and then authors/mounts only `UnifiedBattlePageShell.prefab` and `Scene_TalismanBag_V04_UnifiedBattlePageShell.unity`.
5. The narrow entrypoint must not call or re-enter the full carrier generators for Navigation, PrepareSurface or gameBackground. Their bytes remain the preserved inputs above.
6. Before Unity authoring, recheck Unity/Tuanjie/ShaderCompiler/bee processes, `Temp/UnityLockfile` and `Library/EditorInstance.json`. Proceed only with a clear single serialization lease. Record the owned Unity PID/command/log and clean all owned children/locks before terminal.
7. Validate after mounting: exactly one Navigation, one PrepareSurface and one gameBackground instance; live serialized Host/Shell references are non-null and point to those authored carriers; exact Item Board/Tray Prefab links remain nested and source-linked; no approximate/fallback/dev runtime carrier; no missing scripts; source BattleSandbox and protected baselines unchanged.

### Narrow continuation write whitelist

Allowed writes only:

- `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1ExactBattleSandboxNavigationPhaseAAuthoring.cs`;
- `Assets/_Game/Prefabs/TalismanBag/UnifiedBattle/UnifiedBattlePageShell.prefab`;
- `Assets/_Game/Scenes/Scene_TalismanBag_V04_UnifiedBattlePageShell.unity`.

The three generated carrier Prefabs above are read-only inputs for this continuation. All Phase B Item-detail files, Item VFX P2, exact Item Board/Tray/Card carriers, Item/Battle truth, source BattleSandbox Scene, BuildSettings/ProjectSettings and unrelated dirty work remain protected.

### Terminal and stop rule

- Run scoped compile/static validation, then exactly one narrow Unity mount/validation pass.
- Success returns `NARROW_MOUNT_PASS_READY` with modified files, compile/authoring evidence, mounted carrier counts/references, protected hashes and owned-process cleanup.
- Any new narrow-pass failure returns one `EXACT_BLOCKER` with the first failing diagnostic and leaves existing safe carriers preserved. No second correction, full regeneration, extra Unity retry, user handtest claim or downstream dispatch is allowed from the development task.
- Mainline Guard performs the only close review. The maximum later release remains `CORRECTED_BASE_INTERACTION_USER_HANDTEST_READY / FAITHFUL_PARTIAL`; this development terminal alone is not that release.

## REV05 player-promise correction — latest real-path evidence is authoritative

This section supersedes every earlier `READY` or prepare-surface assumption in this Assignment. The package remains `MILESTONE_FIDELITY_BLOCKED` until the user passes the corrected normal-Editor path.

### Immutable Player Promise Card

- The visible `战斗日志` control opens only the accepted realtime cue/ledger log. The visible `加速` control changes only host-local 1x/2x pacing. Their labels, serialized Button/Text references and callbacks are distinct.
- Clicking `整备` pauses the same authoritative realtime Battle, pulls the accepted BattleSandbox-derived authored arrangement region upward, and only then enables formal Item input. It is not a popup and is not a bare `SetActive` swap of separate Board/Tray roots.
- While prepare is fully open, Tray -> Board, Board -> Board, Board -> Tray and all four rotations use the released formal Item authority. Invalid/bounds/collision placement restores safely. During battle and both motion transitions, all Item mutation is locked and stale drag/preview state is cleared.
- Closing prepare locks Item input first, lowers the same authored surface, and resumes the same paused Battle only after the closed state is reached. Battle time cannot advance while open or during either transition.
- The accepted BattleSandbox `gameBackground` image, 1300x900 authored geometry and layer meaning appear in Unified as an independent background Prefab below battle/prepare presentation. It never blocks raycasts.
- Non-completion: two visible `加速` labels; dead or cross-wired controls; direct BoardArea/ItemTrayArea visibility toggling; a new generic popup/panel; missing source images; Board drag unavailable in prepare; Item movement during battle; hidden battle-time advance; runtime-created final UI; copied BuildSandbox state/runtime; or a second Battle/Item owner.

### Capability coverage at dispatch

| Player-visible row | Truth owner | Required carrier | Current evidence/status |
|---|---|---|---|
| speed vs battle-log semantics | Mainline Host; Battle cues read-only | exact Navigation Prefab/View/Presenter | `FAIL`: callbacks are separate in code, but both authored labels are `加速` and no semantic validator exists |
| authoritative pause/resume | Battle/Bridge | existing Mainline pass-through | `PRESENT`: REV03 compile/tests; real corrected carrier path still unproven |
| Item interaction authorization | Item | released exact Item Presenter API | `PRESENT`: API and pointer handlers exist; current Unified composition failed user Board -> Board |
| pull-up prepare presentation | Mainline presentation | new exact source-derived PrepareSurface Prefab + View/Presenter | `MISSING` |
| exact Board/Tray/Card | Item | existing exact nested Prefabs | `PRESENT`, protected and unchanged |
| independent game background | Mainline/Stage presentation | new exact gameBackground Prefab | `MISSING` |
| corrected real path | Mainline composition | existing Unified Scene/Shell | `UNPROVEN`; user real-path is current authority |

PlayerVisibleDelivery remains `PARTIAL`. MilestoneCompletionEvidence remains `NO`.

## REV05 Technical Director decision

- Product context: `CAMPAIGN_NORMAL_LV1`.
- Existing Scene: keep `Scene_TalismanBag_V04_UnifiedBattlePageShell`; no new Scene.
- State owners remain unchanged: Battle owns pause/time/result; Item owns roster, placement, rotation, legality and interaction authorization; Mainline owns ordering, authored mounting and presentation state translation.
- Unity carriers:
  1. Update the existing exact Navigation Prefab. Preserve every source image/RectTransform/style fact, but correct the `V04BattlePreparezhandourizhi/Label` semantic text to exactly `战斗日志`. The source's duplicate `加速` text is a confirmed authored-source defect, not a parity target.
  2. Add one reusable `C1ExactBattleSandboxPrepareSurface.prefab`. Its presentation root is derived from the source `BattleLikePreviewArea` open geometry (`anchoredPosition=(0,-54.5)`, `sizeDelta=(80,-191)`, scale `1.02`) and source battle offset `-750`, with the accepted source decorative/image children. It contains the existing exact Board/Tray Prefabs as nested Prefab instances at their accepted authored geometry. It excludes `PlacementFeedback_Runtime`, BuildGrid controller/runtime components and all dev diagnostics.
  3. A narrow prepare View/Presenter owns only authored `RectTransform`/CanvasGroup/mask/raycast presentation. Open/closed motion uses unscaled presentation time and source motion factor `9`; it never changes Battle or Item truth.
  4. Add one independent `C1ExactBattleSandboxGameBackground.prefab` from the source `gameBackground` visual carrier: same sprite, color, anchors, position, size, pivot and scale; formal `Image.raycastTarget=false` is mandatory. Mount below battle/prepare content as a sibling, never under Tray/Card.
  5. Update the existing Unified shell Prefab/Scene composition in one bounded authoring pass. The exact Board/Tray source Prefabs remain linked, are not unpacked, and no approximate/fallback Item carrier is made visible.
- Existing `BattleLikePreviewAreaBridge.prefab` is read-only and not reusable here: it is `devOnly`, contains `BuildGridInteractionPreviewRuntime` and diagnostics, and would copy the Sandbox runtime graph.
- Scene/Prefab serialization impact: one Mainline-owned pass after process/lock recheck. Current intake found Unity/Tuanjie/ShaderCompiler/bee = 0 and no UnityLockfile/EditorInstance, but the development task must recheck immediately before authoring.
- Player/APK: authored serialized Prefab references only; no Editor bootstrap, `GameObject.Find`, per-frame hierarchy search, runtime fallback UI or BuildSandbox dependency.

### Required orchestration

1. Battle state/default: prepare carrier is in the authored closed position; Item is `BATTLE_LOCKED`; prepare raycasts are disabled. Do not toggle BoardArea/ItemTrayArea directly.
2. Open request: authoritative Battle pause accepted -> begin pull-up while Battle remains paused and Item remains locked -> at exact open completion apply matching `PREPARE_ENABLED` -> enable prepare raycasts. Failure compensates/fails closed with the surface closed and Item locked.
3. Close request: apply `BATTLE_LOCKED` and synchronously clear drag/preview residue -> disable prepare raycasts -> lower the authored surface while Battle stays paused -> after exact closed completion authoritative resume accepted.
4. Reset, disable, re-entry, stage transition and terminal Battle snap closed, lock Item, clear motion/pointer residue and cannot carry stale generation state.
5. Existing Phase B Item-detail code remains safe-stopped and read-only. This correction does not resume Item-detail authoring.

## REV05 write whitelist

Allowed new paths:

- `Assets/_Game/Prefabs/TalismanBag/UnifiedBattle/C1ExactBattleSandboxPrepareSurface.prefab` and `.meta`;
- `Assets/_Game/Prefabs/TalismanBag/UnifiedBattle/C1ExactBattleSandboxGameBackground.prefab` and `.meta`;
- `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/Presentation/ExactPrepare/**` and required `.meta` files;
- one focused REV05 authoring/validation file under `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/` and its `.meta`.

Allowed existing modifications only when required by the above carrier/binding:

- `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/UnifiedBattleFormalSceneHost.cs`;
- `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/UnifiedBattlePageShell.cs`;
- `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/Presentation/ExactNavigation/C1ExactBattleSandboxNavigationBarView.cs`;
- `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/Presentation/ExactNavigation/C1ExactBattleSandboxNavigationPresenter.cs`;
- `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1ExactBattleSandboxNavigationPhaseAAuthoring.cs`;
- `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1FormalObtainRebuildBattleLoopTests.cs`;
- `Assets/_Game/Prefabs/TalismanBag/UnifiedBattle/C1ExactBattleSandboxBottomNavigation.prefab`;
- `Assets/_Game/Prefabs/TalismanBag/UnifiedBattle/UnifiedBattlePageShell.prefab`;
- `Assets/_Game/Scenes/Scene_TalismanBag_V04_UnifiedBattlePageShell.unity`.

Read-only/protected:

- source `Scene_TalismanBag_V04_BattleSandboxPreview.unity` and `BuildGridInteractionPreviewController.cs`;
- exact Item Board/Tray/Card Prefabs and all Item authority/Presenter/View files;
- Battle/Bridge contracts and adapters, Reward/Save/Drop/Progress, BuildSettings/ProjectSettings;
- `BattleLikePreviewAreaBridge.prefab` and its dev builder/host;
- all Phase B Item-detail files and `ItemDetailPanel.prefab`;
- unrelated dirty work. No Git/worktree/cleanup.

## REV05 acceptance and terminal

- Scoped Runtime/Editor compile and focused deterministic tests for distinct speed/log callbacks, pause/open/lock/close/resume ordering, no hidden advance, duplicate toggle/reset/stale generation and failure compensation.
- One bounded Unity authoring/validation pass: source-to-Prefab image/RectTransform/sibling parity with explicit battle-log-label and background-raycast exceptions; exact Board/Tray nested Prefab source links; one PrepareSurface, one Background and one Navigation instance; no approximate/fallback/dev runtime carrier; no missing scripts/references.
- Static input-chain validation must prove the open prepare ancestry/raycast/CanvasGroup path reaches the exact BoardView/CardView, and the closed/battle path is locked. This evidence does not replace the user real-path handtest.
- User performs one corrected normal-Editor handtest only after development terminal: launch 1-1, confirm live Item lock; verify `加速` and `战斗日志` separately; open prepare and confirm battle freezes while the source-derived surface pulls up; exercise Tray -> Board, Board -> Board, Board -> Tray and four rotations including one invalid restore; close and confirm the same battle resumes; confirm background and no stale preview.
- Maximum terminal: `CORRECTED_BASE_INTERACTION_USER_HANDTEST_READY / FAITHFUL_PARTIAL`. It is not Item-detail completion, VFX P2 release, full battle-page parity or P0 completion.
- Stop once with an exact blocker if the exact Item Prefabs cannot remain nested/source-linked, a second Item/Battle owner would be required, or authoring requires a runtime fallback/copy of the BuildSandbox graph.

## Immutable player promise

The existing Unified bridge page must use the accepted BattleSandbox-authored bottom navigation and Item detail popup one-for-one. This is not a redraw, approximation, decorative Prefab, or runtime fallback UI.

- Bottom navigation preserves the accepted images, 1080x144 background carrier, dimensions, anchors, scale, positions, labels, text hierarchy, raycasts and sibling order.
- Clicking a real formal Tray or Board Item opens exactly one authored Item detail popup matching the accepted BattleSandbox hierarchy, images, proportions, ScrollRects, close control, sections/tabs and layout.
- Visible controls and item facts must be real. Dead buttons, sample/debug detail data, duplicate popups, stale selection and BuildSandbox runtime ownership are explicit non-completion cases.
- Clicking `整备` during a live formal battle must pause the authoritative Battle with zero hidden time advance, then enable the accepted formal Item arrangement interaction and reveal the authored Board/Tray. While live battle is running, Item movement is locked. Closing `整备` locks and clears Item interaction, hides the arrangement surface, then resumes the same paused Battle state.

The package is a partial formal-page carrier only. It must not claim full BattleSandbox page parity, realtime battle completion, or the P0 milestone.

## REV03 prepare fidelity correction — dispatch before further Phase B authoring

Latest user real-path evidence overrides the earlier Phase A technical close. The current Host only toggles Board/Tray visibility while `Update()` continues advancing realtime Battle. Phase A is therefore `USER_HANDTEST_FAIL / MILESTONE_FIDELITY_BLOCKED` for the prepare interaction row.

Released Item dependency:

- `C1FormalItemInteractionMode`: exactly `BATTLE_LOCKED / PREPARE_ENABLED`;
- immutable `C1FormalItemInteractionAuthorizationRequest`;
- immutable `C1FormalItemInteractionAuthorizationResult`;
- `C1ExactBattleSandboxItemArrangementPresenter.ApplyInteractionAuthorization(...)`;
- `C1ExactBattleSandboxItemArrangementPresenter.CurrentInteractionAuthorization`.

Existing Battle dependency is sufficient and remains read-only:

- `C1FormalRealtimeBattleSessionAdapter.TryPause(...)`;
- `C1FormalRealtimeBattleSessionAdapter.TryResume(...)`;
- paused `TryAdvanceBy(...)` rejects hidden time advancement.

Required correction:

1. Add the narrowest Mainline-loop pause/resume pass-through over its existing private realtime Battle adapter. It transports the accepted immutable Battle tick/cues and diagnostic only; it does not recreate pause truth.
2. Opening prepare is ordered and fail-closed: accepted Battle pause -> accepted exact Item `PREPARE_ENABLED` authorization -> show Board/Tray -> publish navigation state.
3. Closing prepare is ordered and fail-closed: exact Item `BATTLE_LOCKED` authorization, synchronously clearing interaction residue -> hide Board/Tray -> accepted Battle resume -> publish navigation state.
4. If Item enable fails after Battle pause, perform one bounded compensated resume. If compensation fails, remain paused with Item locked and the arrangement surface hidden; expose one stable diagnostic and never continue hidden time.
5. If resume fails, remain paused with Item locked and the arrangement surface hidden. Do not pretend the battle resumed.
6. Host `Update()` must not submit Battle advancement while prepare is open or the authoritative Battle reports paused. Waiting is not an error and must not spam diagnostics.
7. Reset, disable, re-entry, stage transition and terminal battle all default Item authorization to `BATTLE_LOCKED`, clear prepare-visible state and prevent stale pause/interaction state from crossing generations.
8. Speed control may retain its host-local 1x/2x state, but it cannot advance time while prepare is open.

Ownership remains fixed:

- Navigation Prefab: visuals and click surface only.
- Mainline Loop/Host: orchestration, authored visibility and state translation.
- Battle/Bridge: pause/resume and battle-time truth.
- Item Presenter/Authority: interaction authorization and placement truth.

REV03 write whitelist:

- `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/UnifiedBattleFormalSceneHost.cs`;
- `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/C1FormalObtainRebuildBattleLoop.cs`;
- `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1FormalObtainRebuildBattleLoopTests.cs`.

All Item implementation files, Battle/Bridge files, navigation Prefab/View/Presenter, Item-detail Phase B files, Scene, Prefab, BuildSettings, Save/Reward/Enemy and Assignment-external dirty changes are read-only for the correction.

REV03 verification is code-only:

- scoped Runtime/Editor compile;
- focused deterministic tests for pause-before-enable, lock-before-resume, zero advance while prepare/paused, duplicate toggle idempotence, failure compensation, reset/stale generation and stage-one/stage-two parity;
- no Unity, Scene/Prefab import/save, Fresh Play, batch loop or Git.

REV03 terminal is `PREPARE_ORCHESTRATION_CORRECTION_COMPILE_READY / MILESTONE_FIDELITY_BLOCKED_PENDING_USER_REAL_PATH`, or one exact blocker. It cannot claim user-visible PASS. Phase B authored popup continuation remains paused until this correction is Guard-accepted.

## REV04 base-admission correction — unfinished Item detail is not a core-route hard gate

Latest Fresh Play evidence is authoritative:

`[UnifiedFormalHost] UNIFIED_FORMAL_HOST_AUTHORED_REFERENCE_MISSING`

REV03 prepare orchestration is accepted, but the formal Loop cannot currently start because the safely stopped Phase B code made the un-authored `itemDetailPresenter` / `ItemDetailPopupSlot` mandatory for all Host admission.

Required correction:

1. Core Unified admission requires the completed Navigation, exact Item Board/Tray/Presenter, realtime Battle presentation and formal route bindings. It must not require an Item-detail carrier that is wholly absent because Phase B authoring is intentionally paused.
2. `ItemDetail` binding states are explicit:
   - `ABSENT`: both the Host presenter reference and Shell popup slot are absent. Core route is accepted; Item detail remains closed/unavailable.
   - `READY`: both exist, the presenter is mounted in the exact slot, and authored references validate. Bind it normally.
   - `PARTIAL_OR_INVALID`: only one side exists, parent/slot identity mismatches, or presenter validation fails. Reject formal admission with one stable Item-detail-specific diagnostic.
3. `ABSENT` must not create a popup, provider, relay, runtime hierarchy or fallback; it must not bind sample/debug data or report fake Item-detail success.
4. `TryAcceptContextOnce()` skips Item-detail bind only in the exact `ABSENT` state. It still binds all required core systems and starts the formal Loop.
5. Reset/disable/re-entry remain null-safe and cannot leave stale Item-detail selection or duplicate subscriptions.
6. Preserve REV03 pause -> Item unlock -> show and Item lock -> hide -> resume semantics unchanged.

REV04 write whitelist:

- `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/UnifiedBattleFormalSceneHost.cs`;
- `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1FormalObtainRebuildBattleLoopTests.cs`, only for the smallest focused admission-state tests.

All Item-detail Phase B implementation files, `UnifiedBattlePageShell.cs`, Scene, Prefab, Item, Battle/Bridge, navigation, Save/Reward/Enemy, BuildSettings and unrelated dirty changes are read-only.

REV04 verification:

- scoped Runtime/Editor compile;
- focused deterministic/static coverage for `ABSENT` core admission, `READY` binding, every `PARTIAL_OR_INVALID` combination, reset/re-entry null safety and unchanged REV03 orchestration tests;
- no Unity, Scene/Prefab import/save, Fresh Play harness, Git or Phase B authoring.

REV04 development terminal is `BASE_ADMISSION_DETAIL_GATE_CORRECTION_COMPILE_READY`, followed by one Mainline Guard close review. Only that review may release `BASE_INTERACTION_USER_HANDTEST_READY / FAITHFUL_PARTIAL`.

## Execution phases

### Phase A — dispatch now: exact bottom navigation

Source Scene is read-only:
`Assets/_Game/Scenes/Scene_TalismanBag_V04_BattleSandboxPreview.unity`

Exact source composition includes:

- `BattlePrepareBottomActions` background carrier, authored 1080x144;
- `V04BattlePrepareBottomActions`;
- `V04BattlePrepareBackButton` / `回首页`;
- `V04BattlePrepareStateButton`;
- `V04BattlePrepareToggleButton` / `整备`;
- `V04BattlePreparejiasu`;
- `V04BattlePreparezhandourizhi`.

Deliver:

1. One exact reusable authored bottom-navigation Prefab extracted from that composition.
2. One narrow formal NavigationBar View/Presenter with serialized Button/Text references.
3. Exactly one Prefab instance mounted into the existing Unified shell/page. No new Scene and no runtime-created final UI.
4. Real formal bindings:
   - back invokes the existing shared WorldMap navigation owner;
   - primary/state invokes the existing formal phase action only when legal;
   - arrange toggles the real formal arrangement surface without changing Item truth;
   - speed provides a real host-local 1x/2x realtime-session playback pacing control without modifying deterministic battle facts;
   - battle log opens a read-only view of accepted formal realtime cues/ledger.
5. If speed or battle-log cannot bind to an existing authoritative fact through a narrow host/presenter seam, stop with the first exact missing capability. Never leave the visible control inert and never import `BuildGridInteractionPreviewController` or legacy `AutoCombatController` ownership.
6. Re-entry/reset is idempotent: one instance, one listener set, no stale phase/speed/log state.

Phase A does not depend on Item detail projection. It may implement disjoint code immediately. Prefab/Scene authoring may run only after a fresh process/lock check shows one serialization owner and no conflicting lease.

### Phase B — dependency-gated: exact Item detail popup

Do not implement Phase B until Item Guard releases:
`V0.4-C1FormalItemDetailProjectionAndSelectionSeam01`.

Dependency release accepted:

- `C1FormalItemDetailSelectionRequest`;
- `C1FormalItemDetailProjection`;
- `C1FormalItemDetailSelectionResult`;
- `C1FormalItemDetailProjectionAndSelection.Evaluate(...)`;
- `IC1FormalItemArtworkResolver`;
- `C1ExactBattleSandboxItemArrangementPresenter.FormalItemDetailChanged` and current immutable result.

After release:

1. Compare the direct authored BattleSandbox `PopupLayer/ItemDetailPanel` against `Assets/_Game/Prefabs/TalismanBag/Items/ItemDetailPanel.prefab`.
2. Reuse/update that Prefab only if exact serialized parity is proven; otherwise perform one exact extraction/update without redesign.
3. Mount exactly one authored popup into a dedicated, lifecycle-safe Unified popup slot. Do not overload `StoryGuidePopupLayer` if its activation lifecycle conflicts.
4. Consume only the Item-owned immutable selected-instance detail projection/artwork result and call `ItemDetailPanelView.Bind(...)` / `Close()`.
5. Tray and Board clicks open actual selected formal instance data; reset/unbind/re-entry closes stale data. No BuildSandbox detail adapter, sample fixture, display-name inference or polling.

Phase B code may proceed while an external user Editor owns Unity. It must stop before any Prefab/Scene import or save and return `PHASE_B_AUTHORING_READY_WAITING_USER_UNITY_CLOSE`. It must never terminate or control the user Editor.

## Phase B write whitelist

Allowed new paths:

- `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/Presentation/ExactItemDetail/**` and required `.meta` files;
- one focused Phase B Editor authoring/validation file under `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/` and its `.meta`.

Allowed existing modifications only when necessary for exact binding/composition:

- `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/UnifiedBattleFormalSceneHost.cs`;
- `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/UnifiedBattlePageShell.cs`, only if a dedicated popup slot identity is required;
- `Assets/_Game/Prefabs/TalismanBag/Items/ItemDetailPanel.prefab`; its existing `.meta` GUID must remain unchanged;
- `Assets/_Game/Prefabs/TalismanBag/UnifiedBattle/UnifiedBattlePageShell.prefab`;
- `Assets/_Game/Scenes/Scene_TalismanBag_V04_UnifiedBattlePageShell.unity`.

Read-only during Phase B:

- the BattleSandbox source Scene and its direct `PopupLayer/ItemDetailPanel` source hierarchy;
- `ItemDetailPanelView`, `ItemDetailViewModel` and all released Item detail seam files;
- exact Board/Tray/Card Prefabs and their formal Presenter/Views;
- Phase A navigation Prefab/View/Presenter and its five button semantics;
- Battle, Enemy, Reward, Save, Progress, BuildSettings, ProjectSettings and BuildSandbox runtime owners.

The dedicated Item-detail popup slot may be authored beneath the Unified popup composition only if it has independent visibility/lifecycle control. It must not inherit `StoryGuidePopupLayer` activation if that can close or disable unrelated story/guide content.

## Phase A write whitelist

Allowed new paths:

- `Assets/_Game/Prefabs/TalismanBag/UnifiedBattle/C1ExactBattleSandboxBottomNavigation.prefab` and `.meta`;
- `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/Presentation/ExactNavigation/**` and required `.meta` files;
- one focused Editor authoring/validation file under `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/` and its `.meta`.

Allowed existing modifications only when necessary for serialized binding/composition:

- `Assets/_Game/Scripts/TalismanBag/UnifiedBattle/UnifiedBattleFormalSceneHost.cs`;
- `Assets/_Game/Scripts/TalismanBag/Editor/UnifiedBattle/C1FormalObtainRebuildBattleLoopSceneAuthoring.cs`;
- `Assets/_Game/Prefabs/TalismanBag/UnifiedBattle/UnifiedBattlePageShell.prefab`;
- `Assets/_Game/Scenes/Scene_TalismanBag_V04_UnifiedBattlePageShell.unity`.

Assignment, source BattleSandbox Scene, BuildGrid/Item/Battle/Enemy/Reward/Save/Progress/BuildSettings/ProjectSettings and Phase B Item-detail files are read-only during Phase A. Preserve unrelated dirty work.

## Ownership and carrier rules

- Scene/Unified shell owns composition and authored slots only.
- Prefab owns exact visual hierarchy and local input surfaces only.
- Mainline host owns formal phase orchestration and navigation translation.
- Battle owns realtime state/cues; Item owns roster/selection/detail facts.
- No second flow, Battle, Item, navigation or log truth owner.
- No `GameObject.Find`, Scene-name inference, runtime-created fallback hierarchy, dev panel, Editor-only Player dependency or copied Sandbox bootstrap/controller graph.

## QA and terminal states

Phase A:

- scoped Runtime/Editor compile;
- exact source-to-Prefab hierarchy/style/serialized-reference comparison;
- one bounded Unity authoring/validation pass only when serialization lease is clear;
- Unified contains exactly one nav Prefab and no duplicate/fallback bar;
- every visible control has a real binding; reset/re-entry is idempotent;
- source BattleSandbox Scene and protected Item carriers remain unchanged.

Phase B:

- exact source-to-Prefab hierarchy, images, RectTransforms, ScrollRects, close control, sections/tabs and serialized-reference parity;
- exactly one Item detail Prefab instance in a dedicated Unified popup slot, initial hidden;
- real formal Tray and Board selections consume the released immutable Item result and bind actual selected instance data/artwork;
- close, selection change, reset, unbind and re-entry produce no stale or duplicate popup state;
- no BuildSandbox adapter/runtime dependency, sample/debug data, inferred text, runtime fallback hierarchy or second Item owner;
- scoped Runtime/Editor compile plus one bounded Unity authoring/validation pass after the external user Editor and locks are fully released.

Do not run repeated Fresh Play, batch or harness loops. User performs one combined normal-Editor handtest after Phase B.

Allowed intermediate terminal:

- `PHASE_A_AUTHORING_READY_WAITING_SERIALIZATION_LEASE`;
- `PHASE_A_COMPLETE_WAITING_ITEM_DETAIL_RELEASE`;
- `PHASE_B_AUTHORING_READY_WAITING_USER_UNITY_CLOSE`;
- one exact missing-capability or write-conflict blocker.

Final package terminal is at most `EXACT_NAV_AND_ITEM_DETAIL_USER_HANDTEST_READY / FAITHFUL_PARTIAL`. It is not full battle-page or milestone completion.
