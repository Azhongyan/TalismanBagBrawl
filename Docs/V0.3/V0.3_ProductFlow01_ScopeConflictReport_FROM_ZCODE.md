# V0.3 ProductFlow01 范围冲突报告（ZCode 临时分析草案）

> **来源系统**：ZCode（GLM-5.2）
> **承接角色**：外部协同实测开发执行体（当前 `CROSS_SYSTEM_EXECUTOR_PROTOCOL` = `DISABLED`）
> **文档性质**：ZCode 临时分析草案 / 只读冲突报告。**非**已注册角色（producer tech pm / tech architect / codex task writer / guard-agents / RepoOps）的正式产物，不具治理效力。
> **日期**：2026-06-27
> **触发**：用户提供《V0.3 当前完整流程文档（EntryFlow / MainHome / Battle / Prepare / Boss / Forge 引导整合版）》并选择"扩大 V0.3 范围并实现完整流程"。

---

## 0. 结论先行

该流程文档描述的是一个**产品终态愿景**，其 Task 0-11 + 状态机离散类建议 + QA S/A 级标准**大量超出当前 V0.3 锁定范围**，并**命中多条 `DO_NOT_TOUCH` / `CORE_INVARIANTS` 红线**，按 `RISK_LEVEL_RULE.md` 默认**红灯**。

红灯规则（`RISK_LEVEL_RULE.md` 原文）："红灯不得直接开发。必须提交冲突报告，列出命中的锁、拟修改文件、原因、影响、风险、QA 与回滚方案，并等待用户针对该范围的明确授权。"

作为 ZCode（非治理窗口、跨系统执行体 `DISABLED`），我**无权**修改 `AGENTS.md` / `Docs/LOCKED/*`，**无权**授权红线，**无权**自任扩大范围的开发窗口，**无权** commit/tag/push。因此本报告是唯一可落盘的合规动作；写完后 **halt**，等待治理窗口授权链（见 §6）。

---

## 1. 流程文档摘要与用户意图

流程文档按真实玩家路径定义了 V0.3 完整首登主流程：

```text
Loading → 开始游戏页 → 服务器/区服占位 → 点击开始游戏 → 开场剧情
→ 首场教学战斗 → 战斗整备引导 → 1-10 Boss → Boss结算获得关键道具
→ 回首页 → 解锁锻造 → 完成首次升级 → 回首页 → 点击试炼 → 进入 2-1
```

文档将其拆为 **Task 0-11** + **状态机离散类建议**（EntryFlowState / HomeState / BattleState / ForgeState）+ **QA 验收标准**（S 级阻断 19 条、A 级严重 10 条），并自称"当前执行口径"。

用户已明确选择：**扩大 V0.3 范围并实现完整流程**。

---

## 2. 当前合规定位（ZCode 身份与授权）

- 入场审查已完成：14 份 `Docs/LOCKED/*` + `Docs/V0.3/V0.3_PACKAGE_QUEUE.md` + `Docs/V0.3/MultiTool/Assignments/ZCODE_CURRENT_ASSIGNMENT.md` 均已完整读取。
- `CROSS_SYSTEM_EXECUTOR_PROTOCOL.md` 当前 = `DISABLED / 暂停生效`。原文："ZCode 不能作为正式开发执行体、外部 QA 执行体或修复执行体进入 Codex 流水线"；"ZCode 产出的内容只能视为外部参考 / 临时草案"。
- 当前**唯一**授权任务 = `V0.3-TrialFlowUI01`（`ASSIGNED_DIRECT_TRIAL`，用户直安排一次性外部实测），边界原文："本包做'看懂试炼'，不做'改写试炼'。" 禁改 RunFlow/PageState/FormationState/V02RunFlowController 状态推进/Boss 触发规则/Save/Reward/Drop/数值/DamageText/V02FormationGridFrame/AGENTS/LOCKED/ProjectSettings/Packages，禁 commit/tag/push。
- 本报告**不属于** TrialFlowUI01 范畴；它是用户选择范围扩展后触发的**红灯冲突报告**，属 ZCode 只读分析草案。

---

## 3. 逐条冲突映射表

风险等级定义（`RISK_LEVEL_RULE.md` 摘要）：
- **绿**：不触核心链路，仅改当前版本允许范围，目标与回滚边界清晰。
- **黄**：可能影响页面流或既有系统，不直接触红线但需 Code Survey。
- **红**：触碰 V0.2 核心链路 / 稳定基线 / 红线，或需重构/拆分/替换受保护系统。

| 文档条目 | V0.2 现有实现 | 真正新建内容 | 命中的锁 | 风险 |
|---|---|---|---|---|
| **Task 0** EntryFlow（LoadingPage / StartGamePage / ServerPlaceholder / StartGameButton / OpeningStoryPanel / SkipButton / 首次进入判断 / 首战入口 / 非首次进入首页） | **不存在**。无任何 EntryFlow/Loading/StartGame/OpeningStory/BootFlow/MainMenu 系统（grep 零命中）。V02 战斗场景靠 `V02RunFlowController.Start()` → `MainTrialFlowService.GetStartupRoute()` 自动从存档路由。 | 全部新建：Loading/StartGame/OpeningStory/首战入口/首次进入判断字段 | `CURRENT_VERSION_SCOPE_LOCK` 范围外（该锁仅允许首页场景/热点/底栏/入口/ComingSoon）；"超出本文件范围的需求不得被'顺手实现'" | **黄**（新范围，需治理扩范围；不直接触红线但新增整条入口流） |
| **Task 1** MainHomeRoot（照灯小铺首页 / 底部导航 / 中部空间功能点占位 / 梦签中部不进底栏） | **已交付**：`V0.3-MainHomeScene01-Retry` `ACCEPTED`（RepoOps `db8484a`）；`V0.3-NavigationFlow01` + `FixPersistence01` `USER_ACCEPTED`。底栏 = 小店/试炼/炼符/探索/更多；梦签在中部空间未进底栏。 | 无（顺序/文案"锻造 vs 炼符"差异属未锁细节，`PAGE_FLOW_LOCK`："锁功能交付形态，不锁…导航先后顺序…文案"） | 无 | **绿/已完成** |
| **Task 2** 战斗页结构改造（上部战斗展示 / 中部棋盘 / 下部隐藏背包 / 底部状态栏） | V02RunFlowController 用 `V02RunState` enum + 方法式；战斗页 Root 为既有结构 | 重构战斗页 Root 为"展示+棋盘+隐藏背包+底栏"四段 | `DO_NOT_TOUCH`：战斗页 Root、V02RunFlowController；`CURRENT_VERSION_SCOPE_LOCK` 明确禁止"战斗状态机重构" | **红** |
| **Task 3** 整备升降（点击整备→背包上拉→棋盘上移→50% 压黑→继续战斗→背包下收→棋盘归位） | 既有 `EnterPrep` / `RequestPostBattlePrepare`（战后驻阵）方法式整备 | 背包上拉/棋盘上移/遮罩升降交互 | `DO_NOT_TOUCH`：战斗页 Root、FormationState、V02FormationGridFrame；`CORE_INVARIANTS`：战斗页须显示棋盘 | **红** |
| **Task 4** 整备道具栏（已有道具展示 / 滚动 / 分类标签 / 拖动到棋盘 / 非法/成功反馈） | 既有 runtime inventory + grid（`V02RewardInventoryAdapter` / `V02FormationGridFrame`） | 道具栏滚动/分类标签/拖动放置反馈 | `DO_NOT_TOUCH`：V02FormationGridFrame、道具触发逻辑、阵盘供能逻辑 | **红** |
| **Task 5** 整备引导（整备态 50% 遮罩 + 高亮道具 + 道具信息窗口 + 高亮棋盘格 + 引导拖动） | `TutorialGuideService` + `TutorialGuideConfig` 已存在（1-1~1-10 引导行） | "真实整备态内" 50% 遮罩 + 高亮 + 拖动引导 UX | `DO_NOT_TOUCH`：战斗页 Root（引导发生在真实整备态，须改战斗页结构） | **红/混合**（教程内容可作 config 增量，但 in-prepare-state 引导 UX 触红线） |
| **Task 6** Boss 节点引导（1-10 到 Boss / 战斗页压黑 50% / 高亮挑战 Boss 按钮 / 小地图 Boss 点 / 点击进 Boss 信息面板） | 1-10 Boss 奖励链 `OpenBossRewardPanel`/`ConfirmBossReward` 已存在；2-10 Boss 前停止 + 手动开战已锁定 | "Boss 节点引导态" 压黑 + 高亮入口 | `DO_NOT_TOUCH`：Boss 触发规则；`CORE_INVARIANTS`：2-9 后不自动开战、2-10 Boss 须手动 | **红** |
| **Task 7** Boss 信息面板（名称/信息/技能/威胁/推荐应对/挑战奖励/带词条道具奖励位预留/挑战按钮） | `BossInfoConfig`/`BossInfoViewModel`/`BossInfoPanel` 已存在（标题/机制/威胁/推荐/战前提示 + 调整背包/开始攻打按钮） | 新增"带词条道具奖励位预留"展示位 + 挑战奖励展示 | 表层展示位新增 = 黄；挑战按钮流程跳转 = 触 Boss 触发规则 | **混合**（表层可做，流程跳转红线） |
| **Task 8** Boss 结算（胜利结算页 / 关键道具突出 / 奖励 / 继续/回首页按钮） | `V02BossRewardPanel`（2-10）+ `OpenBossRewardPanel`（1-10）已存在 | "回首页"按钮触发锻造解锁引导（跨场景流程） | `DO_NOT_TOUCH`：V02RunFlowController、奖励表；跨场景回首页链路 | **红** |
| **Task 9** 首页锻造解锁引导（回首页 / 50% 压黑 / 高亮锻造 / 点击进升级页） | V03 首页 + 锻造入口已存在 | 首页引导遮罩 + 跨场景触发协调 | 引导遮罩表层 = 黄；"Boss 结算→回首页→触发锻造引导"跨场景流程 = 触 V02RunFlowController | **混合** |
| **Task 10** 锻造升级引导（升级页 50% 压黑 / 高亮关键道具 / 物品信息窗口 / 高亮升级按钮 / 完成首次升级） | `UpgradeService` + `TalismanUpgradePanel` 已存在，已作为 1→2 章强制培养门接入（`TryOpenChapterOneUpgradePanel` → `MarkChapterOneCultivationCompleted`） | 升级页引导遮罩 + 物品信息窗口 | 引导遮罩表层 = 黄 | **黄** |
| **Task 11** 升级后继续 2-1 引导（回首页 / 50% 压黑 / 高亮试炼 / 点击进 2-1） | V03 首页试炼入口已存在 | 首页引导遮罩 | 引导遮罩表层 = 黄 | **黄** |
| **状态机离散类建议**（BattleNormalState / BattlePrepareState / BossNodeGuideState / BossInfoState / BossBattleState / BossResultState / ForgeUpgradeGuideState 等 + IState/StateMachine 基类） | **不存在**任何 IState/StateMachine 基类；全是 enum + 方法式（`V02RunState` + `MainTrialPhase` + `MainTrialStartupRouteType`） | 把方法式状态重构为离散状态类 | `CURRENT_VERSION_SCOPE_LOCK` 明确禁止"战斗状态机重构"；`DO_NOT_TOUCH`：V02RunFlowController（重构=拆分/替换，锁原文："对'重构、拆分、替换…'的请求同样适用"） | **红** |

**汇总**：Task 1 已完成；Task 0 黄（新范围）；Task 2/3/4/6/8 + 状态机离散类 = **红**（触红线或明确禁止项）；Task 5/7/9 = **混合**（表层可做，流程部分红线）；Task 10/11 = **黄**（表层引导）。

---

## 4. 治理窗口需修改的 LOCKED 文件清单

> 仅列出。**ZCode 不起草其内容、不修改这些文件。** 修改须由治理窗口（原始 Codex 治理对话）在列出拟改文件/原因/摘要/风险/对 V0.2 影响后，等待用户明确审批（`MEMORY_FILE_APPROVAL_RULE.md`）。

| LOCKED 文件 | 处置需求（供治理窗口决策） |
|---|---|
| `Docs/LOCKED/CURRENT_VERSION_SCOPE_LOCK.md` | 扩 V0.3 范围纳入 EntryFlow / 战斗页结构 / 整备升降 / Boss 流程引导 / 锻造引导 / 2-1 入口；或决定将其移入新版本（如 V0.4）。处置"当前版本明确不做"清单中的"战斗状态机重构 / 奖励表调整 / 数值调优 / 存档结构调整"等项。 |
| `Docs/LOCKED/DO_NOT_TOUCH.md` | 对 战斗页 Root / FormationState / V02FormationGridFrame / V02RunFlowController / Boss 触发规则 / 奖励表 / 道具触发逻辑 / 阵盘供能逻辑 / 存档结构 / 1-10·2-10 数值 逐条决定：是否授权、授权范围、授权给哪个版本/包。 |
| `Docs/LOCKED/CORE_INVARIANTS.md` | 若"隐藏背包 / 整备升降 / 战斗页四段结构"影响"战斗页须显示棋盘"等不变量，需明确豁免边界或重定义不变量（重定义=高风险，须用户审批）。 |
| `Docs/LOCKED/PAGE_FLOW_LOCK.md` | 若新增 EntryFlow/StartGame/OpeningStory 页，或调整底栏职责/入口归属，需更新页面职责锁。 |
| `Docs/LOCKED/STABLE_BASELINE_LOCK.md` | 若范围扩展触碰 V0.2 主流程/1-10·2-10 闭环/奖励/数值/存档，需明确基线保护边界是否调整。 |

> 注意：`Docs/LOCKED/RISK_LEVEL_RULE.md` / `GOLDEN_PATH_QA.md` / `DELIVERY_ACCEPTANCE_GATE.md` / `CODEX_PREFLIGHT_CHECK.md` / `CODEX_VERSION_PIPELINE_LOCK.md` / `CODEX_ROLE_WINDOW_REGISTRY.md` / `MEMORY_FILE_APPROVAL_RULE.md` / `CROSS_SYSTEM_EXECUTOR_PROTOCOL.md` 本身不在本次"拟改"清单内——它们是**规则**本身，调整它们等于改规则，触发 Guard 例外，须 guard `GUARD_PASS` + 用户审批。本次不提议改规则。

---

## 5. 拟改红线文件清单（需用户逐文件/逐行为授权）

以下为实现流程文档所必需触碰的 V0.2 红线文件，**需用户针对具体文件与具体行为明确授权**后方可由开发窗口承接：

```text
Assets/_Game/Scripts/TalismanBag/V02/Run/V02RunFlowController.cs        （整备升降/Boss 节点/结算回首页/状态推进）
Assets/_Game/Scripts/TalismanBag/V02/Run/V02RunState.cs                 （若新增离散状态）
Assets/_Game/Scripts/TalismanBag/V02/CoreLoop/MainTrial/MainTrialFlowService.cs （阶段/路由扩展）
Assets/_Game/Scripts/TalismanBag/V02/CoreLoop/Save/MainTrialProgressData.cs （首次进入/教程步进字段；存档结构调整=红线）
战斗页 Root（Scene_TalismanBag_V02_FormationCounter.unity 战斗页结构）  （四段重构/隐藏背包）
Assets/_Game/Scripts/TalismanBag/V02/UI/V02FormationGridFrame.cs        （棋盘上移/道具栏拖动）
Assets/_Game/Scripts/TalismanBag/V02/CoreLoop/Boss/BossInfoPanel.cs     （流程跳转部分；表层展示位可分离）
Assets/_Game/Scripts/TalismanBag/V02/UI/V02BossRewardPanel.cs           （回首页链路）
奖励表 / 掉落表                                                         （关键道具奖励结构）
```

> `ZCODE_CURRENT_ASSIGNMENT.md` §6 明确把 `V02RunFlowController.cs` 列为只读 Survey、默认不得修改；§12 停线条件含"必须改 V02RunFlowController 状态推进逻辑"。故在授权链完成前，ZCode 对这些文件**只读**。

---

## 6. 授权链（必须全部满足才能动代码）

```text
a. 治理窗口（用户在原始 Codex 治理对话）修改 §4 所列 LOCKED 文件，逐条审批；
b. 用户对 §5 红线文件逐文件/逐行为明确授权；
c. 走版本流水线（CODEX_VERSION_PIPELINE_LOCK）：
     producer tech pm → tech architect 拆包(PACKAGE_ASSIGNMENT)
     → codex task writer 正式工单
     →（若触 Guard 例外：越界/红线/改规则/外部协同污染/异常失败）guard GUARD_PASS
     → 用户确认 → 开发窗口执行；
d. 开发窗口身份：ZCode 当前 DISABLED，须用户另行明确授权 ZCode 作为该扩大范围的开发执行体，
   或由 Codex 正式开发窗口承接。即使未来 ZCode 启用，也不得获治理权，
   不得承接 producer tech pm / tech architect / codex task writer / guard-agents / RepoOps 主责。
```

> 关键：`CODEX_VERSION_PIPELINE_LOCK` 规定 codex task writer 是开发窗口**唯一**正式工单来源；开发窗口不得把 producer tech pm 或 tech architect 输出当工单，也不得合并多角色并发回复为工单。ZCode 当前 assignment 本身就是"用户直安排、非 Task Writer 正式工单"的例外；扩大范围不应延续此例外，应回归正式流水线。

---

## 7. 建议的有序 Package Queue 结构（ZCode 临时草案）

把流程文档拆为候选包，避免一次性大改。**仅 ZCode 临时草案**，正式拆包须经 tech architect `PACKAGE_ASSIGNMENT` + codex task writer 正式工单。

| 候选包 | 覆盖文档条目 | 风险 | 前置 |
|---|---|---|---|
| ProductFlow01-EntryFlow | Task 0（Loading/StartGame/OpeningStory/首战入口/首次进入判断） | 黄（新范围） | 治理扩范围 |
| ProductFlow01-BattlePageRestructure | Task 2/3（战斗页四段/隐藏背包/整备升降） | 红 | 红线授权 |
| ProductFlow01-PrepareItemTray | Task 4（道具栏/分类/拖动/反馈） | 红 | BattlePageRestructure |
| ProductFlow01-PrepareGuide | Task 5（整备态引导：表层可做部分 + in-prepare 红线部分分离） | 混合 | PrepareItemTray |
| ProductFlow01-BossFlowGuide | Task 6/7/8（节点引导/信息面板表层/结算回首页链路） | 混合（表层可先做） | 红线授权（流程部分） |
| ProductFlow01-ForgeUnlockGuide | Task 9/10/11（锻造解锁/升级/2-1 引导） | 混合（表层黄） | BossFlowGuide |
| ProductFlow01-StateMachineRefactor | 状态机离散类建议 | 红（明确禁止项） | 治理决议是否移入后续版本 |

> 建议优先级：可先把"混合/黄"中的**表层引导遮罩 + 信息窗口**（Task 5 表层 / Task 7 表层 / Task 9/10/11 表层）与既有 `V0.3-TrialFlowUI01` 的"看懂试炼"只读 UI 对齐推进，**不触碰**流程跳转与状态机；红线部分（Task 2/3/4/6/8 + 状态机重构）单独排队，待治理授权。

---

## 8. QA 与回滚义务

- 任何触碰 V0.2 的包必须**完整回归战斗黄金路径**（`GOLDEN_PATH_QA.md`）：棋盘可见 / 伤害数字显示 / 敌人掉血 / 道具触发 / 供能有效 / 整备入口正常 / 2-9 后仍停 Boss 前 / 2-10 Boss 仍手动 / 奖励正常 / 主流程继续。
- 首页黄金路径：真实启动路径进入首页 / 默认可见 / 重启后仍可达 / 热点正确 / 试炼入口进当前主线 / 炼符入口进炼符页 / 未完成入口 ComingSoon / 底栏无"背包"/`BattleBackpack`。
- `GOLDEN_PATH_QA.md` 原文回滚铁律："任一核心不变量失败时立即停止，回滚本任务改动，不得继续叠加修复。"
- 场景/页面/UI 任务须遵守 `DELIVERY_ACCEPTANCE_GATE.md`：交付契约须写明进入路径/操作/可见结果 Scene·Page·Root/独立新场景 or 页内/默认 active/Play 首帧可见/重启后可见/真实完成 vs 占位；自动测试不得主动 `SetActive`/反射/临时实例化/切状态制造成功状态；最终验收仅以用户手测回复"通过"为准。

---

## 9. ZCode 能力边界声明

```text
可做：只读分析 / 冲突报告 / 临时草案 / Code Survey。
不可做：修改 AGENTS.md / Docs/LOCKED/*；授权红线；自任扩大范围开发窗口；
        commit / tag / push；把本产出冒充已注册角色正式产物。
worktree 注记：ZCODE_CURRENT_ASSIGNMENT §1 要求 ZCode 在
        F:\Porject\TalismanBagBrawl_zcode 工作；当前会话在主树
        F:\Porject\TalismanBagBrawl。本报告写入主树 Docs/V0.3/
        与现有 CrossSystem_Handoff_Proposal_FROM_ZCODE.md 等同一惯例；
        实际开发须切到 _zcode 树。
```

---

## 10. 下一步

本报告落盘后 **halt**。等待：

1. 用户在治理窗口对 §4 LOCKED 文件的逐条审批与修改；
2. 用户对 §5 红线文件的逐文件/逐行为授权；
3. 版本流水线正式拆包（tech architect `PACKAGE_ASSIGNMENT` + codex task writer 工单 + 必要时 guard `GUARD_PASS`）；
4. 开发窗口身份明确（ZCode 须另行授权，或由 Codex 正式开发窗口承接）。

在此之前，**不进入任何代码实现**。

> 附：当前唯一已授权可执行任务 `V0.3-TrialFlowUI01` 仍未开工（其交付目录仅含 README）。若用户希望在范围扩展授权链运行期间并行推进该只读 UI 任务，可另行指示——该任务本身不触红线，可独立进行。

---

*本文件为 ZCode 临时分析草案，不具治理效力。所有范围/红线/规则变更须由治理窗口在用户明确审批后写入 `Docs/LOCKED/*`。*
